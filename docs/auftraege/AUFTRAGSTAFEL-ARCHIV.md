# ⇒ AUFTRAGSTAFEL — ARCHIV (abgeschlossene Posten)

**Angelegt:** 25.07.2026 · **Vom:** Planner · **Anlass:** Yama, 25.07.: *„mach das"* — die Tafel war auf
39 Zeilen gewachsen und der eine aktive Posten darin nicht mehr auffindbar.

**Was hier steht:** Posten mit Status `ERLEDIGT` oder `ENTFÄLLT`, **wortgleich** aus der Tafel
herausgenommen — keine Zeile gekürzt, keine Formulierung geglättet. Ein archivierter Posten ist kein
gelöschter Posten. Wer einen Beleg sucht, findet ihn hier.

**Was hier nicht steht:** die Belege selbst. Die Wahrheit bleibt `docs/handoff-status.md` —
das Archiv ist Register, nicht Beweis (Tafel §0).

**Zurück ist erlaubt.** Zeigt eine Nachmessung, dass ein abgenommener Posten doch offen ist, wandert die
Zeile zurück auf die Tafel — mit Begründung im Ledger. Archiv heißt abgeschlossen, nicht unantastbar.

---

## Abgeschlossen — Stand 25.07.2026

> **Zweiter Zugang 25.07. abends:** acht Posten (AUF-2 · AUF-3 · AUF-9 · AUF-15a · AUF-16 · AUF-19 ·
> AUF-25 · AUF-31) standen auf der Tafel noch als „wartet auf Evaluator", obwohl sein Votum bereits
> committet war (`docs/abnahme-evaluator-haertung-2026-07-25.md`). Die Statuszelle wurde beim Umzug
> **neu geschrieben** (Votum, SHA, Testzähler, `sichtbar`/`Vorarbeit`) — der übrige Zeilentext ist unverändert.

| Nr | Auftrag | Rolle | Status | Datei / Beleg |
|---|---|---|---|---|
| **AUF-12** | **Dashboard v2 — Flächen des Werkzeug-Dashboards** (v2.1 Kontext-Options-Leiste, v2.2 Panel-Reiter, v2.3 Projektbrowser, v2.4 Prüfungscenter, v2.5 Befehlspalette). **Vorrang vor AUF-11** — Yama, 25.07.: „wir haben dashboard design fest gelegt sollst als erstes fertig gestellt werden v1 usw". Zwei Batches, Batch 1 wird berichtet und abgenommen, bevor Batch 2 beginnt. Ändert den Store **nicht** und liegt damit **außerhalb** des Sperrbereichs von AUF-1 | Generator | ``ERLEDIGT` — Batch 1 **freigegeben mit Auflage** (Evaluator-Votum 25.07.) · **Batch 2 `BERICHTET`** (`5092b10`) — wartet auf Evaluator, Spezifikation `evaluator-auftrag-dashboard-v2-batch2.md` | `generator-auftrag-dashboard-v2-flaechen.md` (Auftrag) + `docs/fahrplan-dashboard-versionen.md` (Fahrplan v1–v6) + `evaluator-auftrag-dashboard-v2-batch1.md` (Evaluator-Spezifikation, Planner 25.07.) |
| **AUF-1** | **A1-Abnahme wiederholen** (Gegenstand `c0ffe31`), unter den zwei neuen Auflagen E1 (erst messen, dann lesen) und E2 (voller Prüfrahmen, nicht nur N1–N7) | Evaluator, **frische Instanz** | `ERLEDIGT` — **Freigabe mit Auflage** (Votum 25.07., frische Instanz). Vier Auflagen binden **A2**: Kürzel `R`/`K` verriegeln · `zoneTools` memoisieren · Render-Pfad-Test · `herkunft` entscheiden | `evaluator-auftrag-wizard-welle-a1-werkzeug-praesentation.md` + Ledger-Block „ZWEI ERGÄNZUNGEN" (Z. 1103) |
| **AUF-4** | **Wizard-Welle A2** *(= dieser Posten; „A2" und „AUF-4" sind dasselbe)* — Leiste liest die Präsentationsschicht, inkl. P9-Memoisierung | Generator | `ERLEDIGT` — **FREIGABE MIT AUFLAGE** (`32b1862` · `728ae69`), 3/4 A1-Auflagen testverriegelt, Auflage 3 = Infra-Lücke → AUF-30. Gegenzeichnung AUF-29 läuft nebenher — umgesetzt 25.07. Das ist **L1** aus `fahrplan-frontend-layout-hausplaner.md` und der Engpass des gesamten Layouts. Die vier A1-Auflagen sind mit umzusetzen | `generator-auftrag-wizard-welle-a2-leiste-liest-praesentation.md` **inkl. §8** |
| **AUF-20** | **ID-Sprache entscheiden** — die gerenderte Registry ist deutsch (`wand`, `fenster`, `tuer`, `dach`, `decke`, `treppe`, `auswahl`, `loeschen`, `duplizieren`), Katalog und das neue 110er-Paket sind englisch (`wall`, `window`, `door`, …). Eine Wahrheit je Sachverhalt: entweder 9 Registry-IDs umbenennen (berührt Commands, Tests, Fixtures) oder 110 Paket-IDs. **Vor dieser Entscheidung wird kein Icon einsortiert** — sonst entstehen 110 Dateinamen, die man danach wieder anfassen muss | Planner | `ERLEDIGT` — **von Yama überstimmt 25.07.: alles deutsch.** Werkzeug-IDs bleiben deutsch, die 110 Paket-IDs werden eingedeutscht. Gespeicherte Datenwerte im Szenendokument bleiben unberührt (Datenschutz, keine Sprachfrage) | `docs/planner/inventur-werkzeug-icons-2026-07-25.md` §7 |
| **AUF-24** | **Die 9 Werkzeug-IDs auf Englisch umbenennen** — `auswahl→select · wand→wall · fenster→window · tuer→door · dach→roof · decke→ceiling · treppe→stair · loeschen→delete · duplizieren→duplicate`. Labels bleiben deutsch. 210 Treffer in ~30 Dateien (Registry, Aktivierung, Zonen, Commands, Fixtures, Tests). Berührt **kein** persistiertes Schema — die IDs stehen dort nicht (je 0 Treffer). **Vor I2 von AUF-21** | — | `ENTFÄLLT` — Yama 25.07.: alles deutsch, keine Umbenennung. Kette verkürzt sich auf drei Schritte | `docs/planner/entscheidung-id-sprache-werkzeuge.md` |
| **AUF-26** | **Panel- und Label-Kappung beheben** (Spur B) — bei 1375 px ist der vierte Reiter „Historie" unsichtbar, „↕ Oben/Unten" gekappt, Hinweistext bricht im Wort ab; Rail-Labels abgeschnitten ohne `title`. Reiterzeile bricht um oder scrollt, kappt nicht; kein `overflow:hidden` auf textführenden Flächen; gekappte Labels bekommen `title`. Pflicht-Viewports 1440/1024/375 | Generator (nativ) | `ERLEDIGT` — **FREIGABE** (`da50af4`): per iframe bei 1440/1371/371 px gemessen, alle vier Reiter sichtbar, keiner geklippt, Panel fest 268 px. **Der ~1375-px-Defekt ist AUF-34 zugeordnet** (dreizeilige Gruppenzeile treibt den waagerechten Überlauf) — meine frühere Zuordnung war falsch | `docs/planner/ux-befund-expertenmodus-2026-07-25.md` B3/B4 |
| **AUF-32** | **Kuratierung für I3 — nach Kategorie statt Werkzeug für Werkzeug** — `priority` taugt nicht (5 von 110 `primary`), `canPin` ist überall `true`. Vorschlag: die 110 über ihre **22 Paket-Kategorien** in Gruppen-Menüs zeigen, wie in `dashboard-tools-v1.html` entworfen (Ansicht · Bearbeiten · Transformieren · Anordnen · Messen · Bemaßen …), daneben die angehefteten. Aus 110 Einzelentscheidungen werden 22 | Planner → Yama | `ERLEDIGT` — **Yama 25.07.: „ja"**. Gruppierung nach den 22 Kategorien freigegeben, I3-Auftrag geschrieben | Planner-Befund 2 zu I2 |
| **AUF-3** | **T1-Abnahme** (Token-Konsolidierung + `decke → bau`) | Evaluator | `ERLEDIGT` — **FREIGABE** T1 (`9ec3b25`), Evaluator-Härtung 25.07.: voller Prüfrahmen, tsc 0 · schema 0 · test 696/696 · **build EXIT 0, Bundle == frischer Build**. Klassifikation **Vorarbeit** | `evaluator-auftrag-t1-token-konsolidierung-und-decke.md` **inkl. §10** |
| **AUF-2** | **T1 + `decke` committen** — die sechs gestagten Dateien als **eigener** Commit mit Pfadangabe | Generator (nativ) | `ERLEDIGT` — mit AUF-3 abgenommen: der freigegebene Stand `9ec3b25` **ist** dieser Commit | COMMIT-FREIGABE im Ledger (Z. 1006), Dateiliste dort |
| **AUF-9** | **Posten T2a** — der Kommentar `renderers/three-d/szene.ts:16` nennt `#93c21c`, drei Zeilen tiefer steht `FARBE_AUSWAHL = 0xa3e635`, das Token sagt `#7fae1c`. Kommentar auf den **tatsächlichen** Wert richtigstellen; **kein** Farbwert im Code ändern | Generator | `ERLEDIGT` — **FREIGABE** (`fbc5308`): Kommentar auf den tatsächlichen Wert `#a3e635` gerichtet, **kein Farbwert geändert**, T2b korrekt verwiesen. **Vorarbeit** | Ledger-Block „AUF-5 eingeordnet" (`e676023`), Messtabelle „drei Grüns" |
| **AUF-15a** | **Die 30 rohen Farbwerte ablösen** — wertgleich auf die vorhandenen Tokens in `studioDaten.ts`, Muster wie T1 (`9ec3b25`). Kein Farbwert ändert sich, nur seine Herkunft. Operanden-Gate: fehlt ein wertgleiches Token, wird der Fall zurückgegeben, nicht erfunden | Generator | `ERLEDIGT` — **FREIGABE** (`2d927fc`): Wertgleichheit jeder Ersetzung belegt, `#d1d5db`/`#9aa4af`/`#1a262a` korrekt **nicht** als Token erfunden (test 734/734). **Vorarbeit** | `generator-auftrag-dashboard-v2-nacharbeit.md` §N1 |
| **AUF-16** | **B1: Options-Leiste wird bei jeder Mausbewegung neu gemountet** — `KontextOptionenLeiste` ist als `const` im Rumpf von `HausplanerApp` definiert (`:298`) und als Element gerendert (`:835`); `onMouseMove` (`:873`) rendert fortlaufend. Wert geht nicht verloren, betroffen sind Fokus, Tastaturbedienung und DOM-Arbeit. Das Muster hat der Auftrag selbst angeordnet — bei `OpBtn` folgenlos, bei einem `<select>` nicht. **Vor der sichtbaren Freigabe von v2 zu entscheiden**, zusammen mit der ausstehenden Sichtprobe auf x64-nativ | Generator | `ERLEDIGT` — **FREIGABE** (`982384d`): Mutation `FENSTER_TYPEN→TUER_TYPEN` → 1 Test rot, verhaltensneutral (test 739/739). **Vorarbeit** | Evaluator-Votum Dashboard v2 Batch 1, Befund B1 |
| **AUF-19** | **Reiter-Muster vervollständigen** — B3: `role="tabpanel"`, `aria-controls`, `id`-Verknüpfung fehlen. B4: roving `tabIndex` ohne Fokusnachführung, nach ArrowRight liegen Fokus und Auswahl auseinander | Generator | `ERLEDIGT` — **FREIGABE** (`8587ce7`): `aria-controls`-Verknüpfung mutationsgeprüft, Kante „Mausklick löst kein `focus()` aus" (test 746/746). **Vorarbeit** | `generator-auftrag-dashboard-v2-nacharbeit.md` §N3 |
| **AUF-25** | **L4 — Fachplaner-Untermodule bekommen eine Fläche** statt des Toasts „Konfigurator folgt" (`HausplanerStudio.tsx` ~Z. 65). Je Fläche: Kopf · Zweck · **Feldstruktur-Vorschau** (deaktivierte Ein-/Ausgangsfelder) · Leerzustand mit `ZustandBadge`. Planner-Entscheidung zu Fahrplan §4.2: **tiefe Fläche, nicht flache** — eine flache müsste später ersetzt, eine tiefe nur verdrahtet werden. **Keine Vorbedingung**, berührt `HausplanerApp.tsx` **nicht** (dort läuft AUF-4) | Generator | `ERLEDIGT` — **FREIGABE** (`17c8be2`): 19 tiefe Flächen, Blindtext-Verbot testverriegelt (Mutation 1 rot), `HausplanerApp` unberührt (test 768/768). **sichtbar** | `generator-auftrag-l4-fachplaner-flaechen.md` · Fahrplan §3 L4 |
| **AUF-31** | **IDs der 110 eindeutschen** — `werkzeugPaket.ts` trägt die englischen Paket-IDs (`u-value`, `thermal-envelope`, `floor-heating` …), weil die Namenstabelle bei I2 noch uncommittet war. Vorlage: `docs/planner/eindeutschung-110-paket-ids.md` (führend). Umbenennen von `werkzeugPaket.ts` + den 110 Icon-Dateien; die 9 Bestands-IDs bleiben byte-genau; die 16 schema-gebundenen behalten ihren englischen Schutzwert im Adapter | Generator | `ERLEDIGT` — **FREIGABE** (`2deb6a5`): Bijektion 9+101=110 hält, GEGENPROBE grün (test 788/788). **Vorarbeit**. Residuum (2 Umlautfehler in der führenden Tabelle) hat der Generator mit AUF-34 berichtigt | Planner-Befund 1 zu I2 |
| **AUF-11** | **Layout-Fahrplan L1–L7** — gemessene Inventur der fünf Layout-Ebenen (17 Dateien / 3.343 Zeilen) plus Fahrplan; enthält drei Willensfragen an Yama (welche Engine wird Panel-Muster, wie tief eine leere L4-Fläche, darf L4 vorgezogen werden). **Reihenfolge abgelöst** durch AUF-12 — die Inventur bleibt gültig, die L1–L7-Abfolge ist es nicht mehr | Planner | `ERLEDIGT` — **Yama, 25.07.**: Von den drei Willensfragen waren zwei durch Ereignisse beantwortet (Feldtiefe → AUF-25 abgenommen; L4 vorziehen → geschehen). Die verbliebene, **welche Engine das Panel-Muster wird, ist entschieden: Treppe.** Entsperrt AUF-33 | `docs/fahrplan-frontend-layout-hausplaner.md` (Commit `5af3e18`) |
| **AUF-14** | **Styling-Strategie** — `build:hausplaner` erzeugt **keine** `hausplaner.css`; der Blade-Link bleibt ungesetzt, das gesamte Styling liegt inline im TSX. Vor v2.3–v2.5 zu klären, ob das so bleibt oder ob v2 die Ablösung mitnimmt | — | `ERLEDIGT` — **Yama, 25.07.: jetzt ablösen.** Gemessen: 331 `style={{` in 35 Dateien / 6.660 Zeilen; `build:hausplaner` erzeugt keine CSS, beide Blades sind mit `@if (file_exists(…))` bewacht (kein 404). Umsetzung als **AUF-38** | Ledger-Block „Planner-Messung 25.07." |
| **AUF-28** | **15 falsche Versprechen aus der Navi nehmen** (Spur A) — die Zone `weitere` zeigt dem Nutzer „Links ausrichten · Hand · Zoom · Freie Transformation …" als `in Entwicklung`. Die Icon-Inventur belegt: DTP-Erbe, kommt nie. Ausführbarer Teil: die 15 Regeln in `toolPresentation.ts` von `weitere` auf `versteckt`; die Navi braucht dann einen ehrlichen Leerzustand. **Willensteil bleibt bei Yama:** womit `weitere` neu belegt wird (Kandidat: Fach-Werkzeuge aus dem 110er-Paket, AUF-21/I2) | Generator | `ENTFÄLLT` — **durch Ereignisse erledigt, gemessen 25.07.**: die 15 DTP-Regeln existieren nicht mehr (Katalog-Tausch I2/`289ccc8`), `zone: 'versteckt'` kommt **0×** vor, „Links ausrichten" nur noch in `toolCatalogStillgelegt.ts`. Auch der Willensteil ist beantwortet: `weitere` trägt heute die **101 Fach-Werkzeuge** aus Yamas Paket — genau der dort genannte Kandidat. *Die Sperre „bis A2 abgenommen" stand noch, obwohl A2 (`acdb987`) längst freigegeben war* | dito, B2 |
| **AUF-30** | **Render-Pfad-Testinfra** — `node --experimental-strip-types` lädt keine `.tsx`, es gibt kein DOM; deshalb importiert keine der 80+ Testdateien eine `.tsx`. Auflage 3 aus dem A1-Votum ist damit **nicht erfüllbar**, nicht verletzt. Weg: esbuild-Loader in `test-hooks.mjs` (esbuild liegt in `node_modules`). Bis dahin gilt die Browser-Sichtprobe als Ersatzbeleg | Generator | `ERLEDIGT` — **FREIGABE** (`56cc734`, Votum `973f47b`): `.tsx` ist im Testlauf übersetzbar, der erste Test läuft durch den **echten Render-Pfad**. **Ohne jsdom, ohne Playwright, ohne neue Abhängigkeit** (`grep` auf `package.json`: keins von beiden). Schließt zugleich die offene **A2-Auflage 3** | A1-Votum Auflage 3, A2-Bericht |
| **AUF-33** | **Die 13 Rechen-Engines zu den Fachplaner-Flächen** — `engine-fbh · -heizkoerper · -heizkreis · -abwasser · -kueche · -pv · -uwert · -fensterprodukt · -sparren · -treppe · -holzmengen · -holzbauteile · -schifter` wandern zu den 19 L4-Flächen; danach fällt der Übergangs-Reiter „Fachplaner" ersatzlos weg. Gemessen: 3 klare Paare, 10 Engines ohne Entsprechung, 16 L4-Flächen ohne Engine — **keine Doppelung, zwei Sortierungen** | Planner → Generator | `ERLEDIGT` — **FREIGABE** L2 (`9d0c12a`, Votum `7293a2d`): `engine-treppe`-Panel als Muster. **L3 (die übrigen 12) ist damit freigegeben** und wird ein eigener Posten | `generator-auftrag-auf33-engine-panels.md` |
| **AUF-36** | **Funktionsvertrag der 110 Werkzeuge einhängen** — `~/Downloads/hausplaner_svg_tool_functions.zip`. Je Werkzeug `commandId · family · inputs · outputs · preconditions · sideEffects · undoable · auditRequired · serviceMethod`. **Vokabulare sind klein und damit abbildbar: 12 Vorbedingungen, 11 Seiteneffekte, 9 Familien, 110 commandIds.** Bindend: `preconditions` gehen als **Daten** in `resolveToolState` (kein zweites `resolveDisabledReasons`); `commandId`/`undoable`/`auditRequired` sind **Metadaten**, die Ausführung bleibt bei `applyCommand` mit inversen Patches (kein `runTool` daneben, sonst gehen Undo und `CommandAbgelehnt` verloren) | Planner → Generator | `ERLEDIGT` — **FREIGABE · SICHTBAR** (`d106445`, Votum `5dea3c2`): drei Grenzen gewahrt (keine zweite Aktivierungs-Engine, keine zweite Ausführungsschicht, kein erfundener Kontext), Grund-Text testverriegelt (Mutation 1 rot), 853/853, Bundle als eigener Rebuild | `generator-auftrag-auf36-funktionsvertrag.md` |
| **AUF-37** | **Bundle-Rebuild für AUF-27** — `894954a` (Reiter) ist **nicht ausgeliefert**: kein Rebuild danach, das servierte Bundle kennt die drei Reiter nicht. Zweites Bundle-Loch nach Batch 2. Rebuild als **eigener Commit** direkt nach dem Code-Commit, Bericht mit Rohausgabe: Größe/Zeitstempel + `grep -c` auf eine Zeichenkette aus dem Slice + Quell-Commit. Danach holt der Evaluator die Sichtprobe nach (iframe, 1440/1024/375) | Generator (nativ, x64) | `ERLEDIGT` — **FREIGABE** (`91d9592`, Votum `a2188e6`): Bundle == frischer Build, byte-identisch. **Alle drei Bundle-Löcher sind zu.** | Bundle-Regel `docs/agents/06-laufzeiten-und-takt.md` §8 |
| **AUF-34** | **Arbeitsbereiche statt 22 Gruppen nebeneinander** — die Leiste läuft bei 1440 px über drei Zeilen; `Bearbeiten` hat 13 Werkzeuge, `TGA` und `Sanitär` je eines. Die Ebene existiert ungenutzt im Code (`uiState.ts:23 activeWorkspace`, genau **ein** Wert; Paket-Werkzeuge tragen `supportedWorkspaces: []`). Entscheidung: 8 durchgängige Gruppen + 11 gebundene auf **fünf** Arbeitsbereiche aus Yamas Entwurf. Drei Lücken benannt und zurückgegeben (Dach, Heizlast, Bad/Küche) | Generator | `ERLEDIGT` — **FREIGABE, Auflage aufgelöst** (`8b2b9e6`, Votum `7fe6627` + `a2188e6`): Bilanz 15/110 ohne Verlust, Mutation 4 rot, 830/830; die deferred Sichtprobe ist per iframe bei **1371 und 375 px** nachgeholt — fünf Bereiche, **kein Überlauf, kein Wortumbruch** | Planner-Vorschlag, von Yama angenommen 25.07. |
| **AUF-27** | **Linke Spalte macht drei Jobs** (Spur A) — Werkzeuge + Fähigkeiten + Projektbrowser teilen sich 220 px und **eine** Scroll-Höhe; der Projektbrowser ist erst nach 20 Scroll-Ticks sichtbar. Verstoß gegen „ein Hauptjob je Fläche" und „Sidebar = Navigation, keine Daten". Entscheidung: drei Abschnitte mit je eigener Scroll-Höhe, Werkzeuge fix oben, Fähigkeiten und Projekt mit Kopf und Zähler | Generator | `ERLEDIGT` — **FREIGABE, Auflage aufgelöst** (`894954a`, Votum `ea61ea1` + `a2188e6`): 810/810, Mutation 4 rot, kein zweiter Tab-Mechanismus; Sichtprobe per iframe nachgeholt — **drei Reiter bei 1371 und 375 px sichtbar** | dito, B1 |
| **AUF-35a** | **„Markieren" — Flächen- und Zonenauswahl** (Yama, 25.07., aus Architektensicht). Drei Vorgänge, nur einer existiert: Objektauswahl **da** · **Flächenauswahl fehlt vollständig** (Wandseite, einzelne Dachfläche — Voraussetzung für Material/Fassade/Dachdeckung) · **Zone markieren**: Schema kennt **6** Typen (`room · underfloor_heating · pv_area · maintenance_area · sound_area · restricted_area`), das Paket hat dafür **ein** Werkzeug (`raum`), Grep `ZONE` in den Commands → **0 Treffer**. **Das ist die Naht zwischen Geometrie und Fachmodulen** und erklärt, warum die 13 Engines keinen Eingang haben. **Vor L2/L3, nach AUF-34.** Planner misst zuerst drei Fragen, dann Auftrag | Planner → Generator | `ERLEDIGT` — **FREIGABE** (`35fbfde`, Votum `134eb0e`): Markieren / Mehrfachauswahl. Entsperrt AUF-35b | Planner-Block „Markieren ist kein Label" |
| **AUF-21** | **Werkzeug-Paket einsortieren (I1–I4)** — 110 SVGs nach `public/hausplaner/icons/tools/`; Katalog-Austausch mit Adapter Paket→`ToolDefinition` (Konflikt-Regel: der neue Code passt sich dem Bestand an); die 47 DTP-Reste belegt stilllegen; `canPin`/`priority` in die Zonen-Kuratierung führen. **Gesperrt bis AUF-20** | Planner → Generator | `ERLEDIGT` — **alle vier Teile freigegeben**: I1 (`7bbf9ff`, Votum `33ad6d6`) · I2 (`289ccc8`) · I3 (`ccdc93b`) · I4 (`4932b36`). Das Werkzeug-Paket ist vollständig eingesortiert | `docs/planner/inventur-werkzeug-icons-2026-07-25.md` §7 | **Neue Priorität 25.07.: I2 + I3 sind Schritt 3 und 4 zur fertigen Werkzeugleiste — Vorrang vor L2/L3.**
| **AUF-39** | **L5 — Wizard-Schritte aus dem Modell ableiten** statt hartkodierter Status/Prüfungen. Steht seit `5af3e18` im Layout-Fahrplan und hatte bis 25.07. **keinen Posten** — gemessen: null Treffer auf `L5` in dieser Tafel. Guardrail aus dem Fahrplan: an vorhandene Services andocken, **kein zweiter Snapshot-/Hash-/Projektions-Mechanismus** **Playwright-Lauf 25.07. bestätigt und ergänzt:** von **elf Schritten sind auf Desktop nur ~fünf sichtbar**, der waagerechte Rest hat **keine erkennbare Scroll-Hilfe**. Der Stepper gehört mit in diesen Posten — er ist dieselbe Datei (`GuidedView.tsx`). | Planner → Generator | `ERLEDIGT` — **FREIGABE** (`b3a6210`, Bundle `cb3d17e`, Votum `486570b`): die elf Schritte kommen aus dem Modell statt aus `STEPS`. Damit ist der schwerste Befund des Abends behoben — „5 Räume erkannt" neben „Im Modell: 0 Fenster" gibt es nicht mehr | `generator-auftrag-auf39-wizard-schritte.md` |
| **AUF-41** | **Import-Recht fehlt im CRM** — der Generator hat es bei AUF-36 gemessen und zurückgegeben: `routes/web.php` kennt nur `Hausplaner,read` und `Hausplaner,update`, ein `Hausplaner,import` gibt es nicht. Die **acht** Import-Werkzeuge sind deshalb heute gesperrt mit dem Grund „Keine Berechtigung zum Importieren." Willensfrage: hängt Import an `update`, oder bekommt er ein eigenes Recht? **Planner-Empfehlung: eigenes Recht** — Import zieht fremde Daten (IFC/DXF/Bilder) ins Modell; wer zeichnen darf, muss nicht importieren dürfen. Berührt `routes/` und die Rechteverwaltung, also Tor 1 | — | `ERLEDIGT` — **Yama, 25.07.: eigenes Recht.** `Hausplaner,import` wird neu eingeführt; Import zieht fremde Daten ins Modell, wer zeichnen darf muss nicht importieren dürfen. Umsetzung als **AUF-53** | Ledger „GENERATOR-BERICHT AUF-36", Rückgabe 1 |
| **AUF-15b** | **K6 neu schneiden** — der Evaluator hat belegt: `app/` enthält **30** rohe Farbwerte außerhalb `studioDaten.ts` (ConfigWizard 2 · StartView 3 · DreiDBereich 4 · GuidedView 15 · HausplanerStudio 6). T1 war auf `HausplanerApp.tsx` geschnitten (50→0), nicht auf `app/*` (80→30). Entweder wird das Kriterium künftig auf die **geänderten Zeilen** bezogen (so formuliert es die `frontend-entwickler`-Linse ohnehin), oder die Ablösung der 30 Restwerte wird ein eigener beauftragter Posten | Planner → dann Yama | `ERLEDIGT` — **Yama, 25.07.: auf die geänderten Zeilen.** Jeder Generator haftet für das, was er anfasst, nicht für Altbestand. Belegt: 30 (24.07.) → 17 (nach AUF-15a) → **38 heute** — eine Regel hält, eine Aufräumaktion nicht. Wirkt ab sofort als Prüfrahmen-Kriterium, kein eigener Bau-Posten | Evaluator-Votum Dashboard v2 Batch 1 |
| **AUF-5** | Die zwei vom Generator **zurückgegebenen** Punkte einordnen: `treppeSvg.ts:38`, `szene.ts:16` | Planner | `ERLEDIGT` — beide Punkte zu: `szene.ts:16` wurde mit **AUF-9** richtiggestellt (Kommentar auf `#a3e635`), `treppeSvg.ts:38` ist mit AUF-10 entschieden. Umsetzung als **AUF-54** | Ledger „SCHRITT 2 ERGEBNIS" (Z. 1171); Einordnung im Ledger-Block „AUF-5 eingeordnet" (`e676023`) |
| **AUF-10** | **Posten T2b** — Palette in `geometry/`: soll die Treppen-Lauflinie überhaupt Markenfarbe sein, und darf `geometry/` Farben kennen? (`treppeSvg.ts:35-42`, neun Aufrufstellen, kein Parameter) | — | `ERLEDIGT` — **Yama, 25.07.: Farbe als Parameter.** `geometry/` bekommt die Farbe hereingereicht statt sie zu kennen; danach gibt es genau eine Farbwahrheit. Neun Aufrufstellen. Umsetzung als **AUF-54** | Ledger-Block „AUF-5 eingeordnet" (`e676023`), Abschnitt Willensfrage |
| **AUF-17** | **`Strg+K` war belegt** — `toolFuerShortcut('k')` liefert `decke`, und der Kürzel-Zweig in `taste()` prüfte keine Modifikatoren; vor `5092b10` setzte `Strg+K` das Werkzeug „Decke". Der Generator hat den Palette-Zweig davor einsortiert: `Strg+S` speichert unverändert, `K` allein setzt weiter „Decke", `Strg/⌘+K` öffnet die Palette. Bewusste Verhaltensänderung für genau eine Kombination — soll die Palette diese Kombination behalten? | — | `ERLEDIGT` — **Yama, 25.07.: behalten.** `Strg/Cmd+K` bleibt die Befehlspalette (überall Standard), `K` allein setzt weiterhin das Werkzeug „Decke". **Nichts zu bauen** — die Entscheidung bestätigt den Ist-Zustand | Generator-Bericht Dashboard v2 Batch 2 |
| **AUF-7** | `auto/hausplaner-ui-3a` — mergen oder bewusst überschreiben (fork `f3e38d6`, lokal `df0dbdb`) | — | `ERLEDIGT` — **Planner in Yamas Vertretung, 25.07. (delegiert: „nimm deine Empfehlungen an"): keine Aktion.** Gemessen: **jede** Datei aus `f3e38d6` liegt im Hauptzweig, und in `applyCommand.ts` und `hausplanerStore.ts` ist dieser **161 Zeilen voraus**. Der Branch ist überholt, sein Inhalt doppelt gesichert. Der fehlschlagende Push ist 1 von 54 und folgenlos. **Rückdrehbar:** nichts wurde verändert | Yamas Auftragsdatei, Auftrag 4 |
| **AUF-8** | Branch-Hygiene — welche der 27 Branches dürfen weg | — | `ERLEDIGT` — **Planner in Yamas Vertretung, 25.07.: nichts löschen.** Gemessene Lage: **18** der 27 Branches sind vollständig im Hauptzweig enthalten (davon `main` und `private/app-code-backup`, die ohnehin bleiben), **8** tragen eigenen, ungeprüften Inhalt (`hausplaner-dach-ui` 8 Commits, `strang/C` 3, u. a.). Löschen ist unumkehrbar, der Gewinn wäre Übersicht — alle 27 liegen auf beiden Sicherungen und kosten nichts. **Rückdrehbar:** die Liste steht hier, wenn die Zahl je stört | Yamas Auftragsdatei, Auftrag 5 |
| **AUF-13** | **Tote Naht Snapshots** — `objekt.blade.php:94` setzt `data-snapshots-url`, `routes/web.php:5003-5008` liefern drei Routen, `main.tsx:63` liest ausschließlich `dataset.speichernUrl`. Willensfrage: welcher Version wird die Anbindung zugeordnet — oder wird die Fläche bis dahin ehrlich als `in_entwicklung` ausgewiesen? | — | `ERLEDIGT` — **Planner in Yamas Vertretung, 25.07.: ehrlich ausweisen, später verbinden.** Gemessen unverändert tot: das Blade setzt `data-snapshots-url`, `main.tsx` liest **null** Snapshot-Verweise, drei Routen laufen ins Leere. Die Fläche wird als `in Entwicklung` gekennzeichnet (Muster AUF-25), die Anbindung wird ein eigener Posten. Umsetzung als **AUF-55** | Ledger-Block „Planner-Messung 25.07." (gemessen gegen `f60b923`) |
| **AUF-23** | **Elevation-/Overlay-Tokens fehlen** — 16 der 36 verbliebenen Rohwerte sind Schatten/Scrim (`rgba(28,40,48,.05)` u. a.); `studioDaten.ts` kennt keine Elevation-Rolle. Dazu: `T.surface` trägt nach N1 zwei Rollen (Fläche + Text-auf-Farbe, Kandidat `T.onFilled`), und ~8 Werte sind „nah dran" an vorhandenen Tokens — ihre Angleichung wäre eine **sichtbare** Farbänderung, also eine Willensfrage | — | `ERLEDIGT` — **Planner in Yamas Vertretung, 25.07.: zwei Token ja, Angleichung nein.** Gemessen: `rgba(28,40,48,.05)` kommt **9×** vor, `rgba(28,50,55,.10)` **3×** — zwei Elevation-Token lösen zwölf Rohwerte ab, **ohne dass sich ein Pixel ändert**. Die acht „nah dran"-Werte werden **nicht** angeglichen: das wäre eine sichtbare Farbänderung und damit eine echte Willensfrage, die Yama selbst treffen muss. Umsetzung als **AUF-56** | Generator-Bericht Nacharbeit, Operanden-Gate |
| **AUF-45** | **Der erste Schritt ist nirgends sichtbar** (Befunde B3 + B8) — gemessen: **78 von 110 Werkzeugen (71 %) sind beim Öffnen gesperrt**, mit aktivem Geschoss 44 (40 %), mit Auswahl 16 (15 %). Jede Sperre ist **korrekt** (AUF-36), aber nichts in der Oberfläche sagt, dass das Geschoss das Tor ist. Dazu B8: das Standardwerkzeug „Markieren" meldet sich beim Start als **„in Entwicklung"**, obwohl es nur schlicht keine Optionen braucht — der Platzhalter verwechselt „braucht nichts" mit „ist nicht fertig" | Planner → Generator | `ERLEDIGT` — **FREIGABE** (`b9861d7`, Bundle `ab7f2c1`, Votum `66128fe`): blind gemessen, 930/930, zwei Mutations-Gegenbeweise (1 rot / 2 rot). **B8 sichtbar behoben** — „Dieses Werkzeug braucht keine Optionen", null `in Entwicklung`-Badges im View. Der schlafende Wegweiser zählt ausdrücklich **nicht** gegen das Votum: der Evaluator ordnet ihn als **Planner-Spezifikationsfehler** ein (Folge: AUF-57) | `generator-auftrag-auf45-erster-schritt.md` |
| **AUF-43** | **Geschoss-Bedienung neu ordnen** (Befund B1) — gemessen: **13 Bedienelemente in einer Zeile**, vier voneinander unabhängige Aufgaben (Rückgängig · Geschoss-Navigation · Geschoss-Verwaltung · Ansichtsmodus · Speichern), und der Geschossname steht **zweimal nebeneinander** (Select 111 px + Textfeld mit demselben Wert). Höhenlage wird geführt, aber nicht gezeigt; Namen automatisch „Geschoss 3"; kein Bild vom Stapel. **Größter Einzelhebel: ein angelegtes Geschoss entsperrt 34 der 110 Werkzeuge** — das Tor dahin ist ein 111-px-Dropdown zwischen Rückgängig und Speichern | Planner → Generator | `ERLEDIGT` — **FREIGABE** (`43a287f`, Votum `e831bc2`): die Geschoss-Bedienung hat die Zeile verlassen; Höhenlage sichtbar, Name nur noch einmal, 375-px-Überlauf gemeldet statt geflickt | `generator-auftrag-auf43-geschoss-bedienung.md` |
| **AUF-51** | **Die Zeichenfläche ist verschiebbar erklärt, aber der Verschub wird verworfen** (Playwright meldete eine react-konva-Warnung; **Ursache selbst gemessen**) — `HausplanerApp.tsx:1280` setzt am `<Stage>` `draggable`, **ohne jeden Drag-Handler**. Die vier Node-Ebenen (Z. 1332/1386/1481/1521) haben `onDragStart` und `onDragEnd`, **die Bühne selbst hat keinen**. Zugleich sind `x={80}` und `y={hoehe - 80}` **gesteuerte Werte ohne Zustand dahinter** (`grep` nach `setPan`, `panX`, `panY`: **leer**), und `onMouseMove` ruft `setCursor(...)` — **jede Mausbewegung erzeugt ein Rendern**, das die Bühne auf `x=80` zurücksetzt. Der Nutzer schiebt, und es springt zurück. `weltPunkt` (Z. 626-634) liest `stage.x()`, also die **echte** Position — für die Dauer des Zurückspringens widersprechen sich Anzeige und Koordinate | Generator | `ERLEDIGT` — **FREIGABE** (`74fdcb4`, Votum `d5d0b2e`): Pan-Zustand eingeführt statt `draggable` entfernt, 938/938; Sichtprobe belegt den bleibenden Verschub. **Der Klick landet wieder dort, wo der Nutzer hinzeigt** | Ledger „Playwright-Audit nachgemessen" (25.07.) |
| **AUF-47** | **Speichern lügt auf der Testfläche** (externe Bewertung Nr. 4, nachgemessen) — `studio.blade.php:56` setzt **bewusst kein** `data-speichern-url`; `hausplanerStore.ts:168-172` steigt daraufhin **still** aus, sobald Szene **oder** `speichernUrl` fehlen — kein Status, keine Meldung. Der Knopf ist trotzdem **grün, primär und unbedingt aktiv** (`HausplanerApp.tsx:1061-1067`, kein `disabled`). **Verschärfung aus der Sichtprobe:** die Statusplakette daneben sagt **„Gespeichert · Rev. 1"** — auf einer Fläche, die nicht speichern kann, im Widerspruch zum Warnhinweis „Testfläche — wird NICHT gespeichert" in derselben Kopfzeile. Der Playwright-Lauf bestätigt: der Knopf sendet keinen Request und bleibt „erfolgreich" | Generator | `ERLEDIGT` — **FREIGABE MIT AUFLAGE** (`79bf47c`, Bundle `fca2fc6`, Votum `f301fdc`): Knopf **und beide** Statusanzeigen ehrlich. Die zweite (Studio-Kopfzeile, eigene Tabelle) fand der Generator **erst in der zweiten Sichtprobe** — der `save()`-No-Op selbst blieb unangetastet | `docs/planner/ux-befund-layout-alle-ebenen-2026-07-25.md` + Ledger „Externe Frontend-Bewertung" (25.07.) |
| **AUF-29** | **Blinde Gegenzeichnung A2** — die A2-Abnahme (`728ae69`) ist belastbar, stammt aber von einer anchored Instanz (vom Evaluator selbst offengelegt). Die frische Instanz, die AUF-1 zog, zeichnet gegen: erst messen, dann lesen. **Blockiert nichts** — eine zusätzliche unabhängige Abnahme kann eine Freigabe nur härten, nie weichmachen | Evaluator, frische Instanz | `ERLEDIGT` — **durch Ereignisse eingelöst, gemessen 25.07.** Der Zweck war eine **unabhängige** Zweitprüfung von A2. Die Härtungs-Instanz hat genau das geleistet: `acdb987` steht in ihrem eigenen Dokument (Z. 84) mit **FREIGABE**, blind gemessen („erst messen, dann den Generator-Bericht"), inklusive Mutations-Gegenbeweis (`useMemo` entfernt ⇒ 1 Test rot) und dem Nachweis `zoneTools('fix')` === `werkzeugTools()` — keine Icon-Wanderung. Ihr Kopf hält fest: **„Alle Voten konvergieren mit dem nativen Strang."** Und die damals offene **Auflage 3** (Render-Pfad-Test) hat **AUF-30** geschlossen (`56cc734`). Beide Fäden aus A2 sind zu | Planner-Entscheidung 25.07. im Ledger |
| **AUF-6** | stopp-1 Teil I — Re-Check fahren, Teil I schließen, Dokument nachziehen | — | `ERLEDIGT` — **Yama, 25.07.: Teil I schließen.** Die Messung hat die Frage beantwortet: Cut-over-Kriterium I.2 Nr. 2 ist erfüllt — `app/Services/Heizlast/` in ticket trägt **21 Services** und **17 Tests**, letzter Commit **18.07.** Der Transplant ist nicht mehr geplant, **er ist gelaufen**. Der Re-Check bleibt bewusst ungezogen: er würde die MySQL-Tauglichkeit von Code belegen, der die Quelle bereits verlassen hat. Dokument nachgezogen, Teil I geschlossen | Yamas Auftragsdatei, Auftrag 3 |
| **AUF-61** | **„Ansicht einpassen" — entfernen oder bauen?** (Rückgabe des Generators aus AUF-44) — von fünf toten „(geplant)"-Knöpfen waren vier Dubletten vorhandener Werkzeuge und sind entfernt. **Dieser eine hat kein Gegenstück im Katalog.** Ihn zu entfernen hieße, die Funktion ganz aus der Oberfläche zu tilgen statt eine Kopie; ihn zu bauen wäre ein neues Werkzeug. Er steht heute inert in der Zeile | — | `ERLEDIGT` — **Yama, 25.07.: bauen.** „Ansicht einpassen" bleibt und bekommt eine Funktion, statt zu verschwinden. Umsetzung als **AUF-62** | Ledger „GENERATOR-BERICHT AUF-44" |
| **AUF-53** | **Import-Recht `Hausplaner,import` einführen** (Entscheidung Yama, 25.07., aus AUF-41) — gemessen kennt `routes/web.php` heute nur `Hausplaner,read` und `Hausplaner,update`; die **acht** Import-Werkzeuge sind deshalb dauerhaft gesperrt mit dem Grund „Keine Berechtigung zum Importieren." Das neue Recht in die Rechteverwaltung, an die Import-Routen hängen, und die Fähigkeit an `AktivierungsKontext.permissions` reichen — **die Vorbedingung `permission.import` steht bereits im Funktionsvertrag** und geht von selbst auf grün. **Kein Werkzeug wird freigeschaltet, das das Recht nicht hat** | Planner → Generator | `ERLEDIGT` — **FREIGABE** (`b4e5f03`, Votum `1c41ec6`): die Falle bestätigt, `Hausplaner,add` zugeordnet, `routes/` und Migrationen **null Zeilen**. Der Evaluator hat die §4-Rückgabe **gegengeprüft**: nur ein `update`-Selbstgrant in der Insel, `add` wird **nirgends** selbst erteilt — die Rückgabe ist vollständig | `generator-auftrag-auf53-import-recht.md` + `evaluator-auftrag-stapel-2026-07-25.md` |
| **AUF-49** | **Dialog-Fokus und Tastatur unvollständig** (externe Bewertung Nr. 6, nachgemessen) — `FachFlaeche` hat `role="dialog"`, `aria-modal` und Escape, aber **keinen Fokuswechsel beim Öffnen, keine Fokusfalle, keine Fokus-Rückgabe beim Schließen**. `ConfigWizard.tsx:56` trägt die Dialogsemantik gar nicht. Dazu gemessen: **8×** `role="button"`, **10×** `key === "Enter"`, **1×** Leertaste — selbstgebaute Schaltflächen reagieren fast durchweg nicht auf die Leertaste (WCAG 2.1.1) **Playwright-Lauf 25.07., nachgemessen:** `ConfigWizard.tsx` hat **weder `role="dialog"` noch `aria-modal` noch einen Escape-Handler** (`grep` = 0 Treffer). Fokus bleibt nach dem Öffnen auf dem Hintergrundknopf, der erste Tab-Sprung landet **hinter** dem Dialog, Escape schließt nicht, Fokus wird weder eingeschlossen noch zurückgegeben. **Zum Vergleich:** `FachFlaeche.tsx:125,139` (AUF-33, heute gebaut) hat Escape, `role="dialog"` und `aria-modal` — die neue Schicht ist richtig, die Schuld liegt in der alten. Dazu **Zielgrößen**: Chips 27–40 px, CAD-Schaltflächen ~30×32 px (WCAG 2.5.5 verlangt 44). | Generator | `ERLEDIGT` — **FREIGABE** (`f83cf11`, Bundle `c4e8cc4`, Votum `73d801b`): eine Fokusregel für **drei** Dialoge, `ConfigWizard` bekommt Dialogsemantik, Leertaste an sieben Stellen. Browser-Fokus-Sichtprobe geführt. Die **Zielgrößen** (44 px) hat der Generator als eigenen Layout-Posten zurückgegeben | `evaluator-auftrag-stapel-2026-07-25.md` |
| **AUF-59** | **Die Icon-Zeile mischt drei Zustände, die man nicht auseinanderhalten kann** (Yama, 25.07.: „einige Icon, die sind eingerahmt, manche sind aktiv manche nicht"). **Gemessen bei 1440 px:** 15 quadratische Knöpfe in einer Zeile, 9 gesperrt. Drei Zustände — **(1)** Schalter EIN: grüner Rand, grüner Grund (Raster, Fang) · **(2)** bedienbar: weiß, grauer Rand, dunkles Icon · **(3)** gesperrt: weiß, grauer Rand, blasses Icon. **Zustand 2 und 3 unterscheiden sich ausschließlich in der Icon-Farbe** (`rgb(35,42,49)` gegen `rgb(167,174,183)`) und im Cursor; Rahmen, Hintergrund und Deckkraft sind **identisch**. Die Zeile liest sich als Reihe gleichwertiger Kästchen. **Dazu die Dublette:** „Grundriss spiegeln" existiert **zweimal** — als Icon mit Tooltip in dieser Zeile (32x30) und als ausgeschriebener Textknopf im Eigenschaften-Panel (117x43, **ohne** `title`). Yamas Punkt: die Icon-Form gibt es bereits, der Text ist die Dopplung | Generator | `ERLEDIGT` — **FREIGABE** (`8f34fc5`, Bundle `ece8e43`, Votum `5522cf3`): drei Zustände an **drei** Merkmalen statt an einem, Rahmen nur noch am Schalter, Text-Dublette den Icons gewichen. **Keine Sperre geändert** — das war das härteste Kriterium | Planner-Sichtprobe 25.07. 23:10 + `evaluator-auftrag-stapel-2026-07-25.md` |
| **AUF-44** | **Zwei „(geplant)"-Knöpfe und das Spiegeln im leeren Plan** (Befund B2) — `title` sagt es selbst: „Auswahl um 90° drehen **(geplant)**", „Als PDF-Planblatt exportieren **(geplant)**". Dieselbe Sorte falsches Versprechen, die I2 aus dem Katalog entfernt hat, in der Icon-Zeile stehengeblieben. Dazu nehmen „Grundriss spiegeln links/rechts + oben/unten" den prominentesten Platz im Eigenschaften-Panel ein — bei **0 Räumen · 0.00 m²** | Generator | `ERLEDIGT` — **FREIGABE** (`47addd1`, Bundle `0bde0d9`, Votum `a2403c4`): fünf statt der zwei aus meiner Tafelzeile, vier davon Dubletten. Entfernt wurde die tote Kopie, **nicht das Werkzeug** — Bilanz unverändert **110** | `docs/planner/ux-befund-layout-alle-ebenen-2026-07-25.md` + `evaluator-auftrag-stapel-2026-07-25.md` |
| **AUF-46** | **Startseite läuft bei 375 px um 283 px über** (Befund B5) — gemessen im iframe: `documentElement.scrollWidth = 658` bei `innerWidth = 375`, **43 Elemente** ragen über den rechten Rand. Bei **1024 px sauber** (`scrollWidth 1009`), bei 1440 sauber. Damit ist einer der drei Pflicht-Viewports aus L7 hart verletzt **Erweitert 25.07. (externe Bewertung Nr. 5, nachgemessen):** die Navigation klappt unter 900 px ein, aber Geführte Planung, Konfigurator und Expertenmodus tragen weiterhin **feste Spaltenbreiten** (`320px`, `300px`, `220px`, Panel 268px). Der Posten definiert die Breiten **390 · 768 · 1024 · 1440** verbindlich, statt nur die 375er-Kappung zu flicken **Ursache gefunden (Playwright + Code, 25.07.):** `GuidedView.tsx:59` setzt `gridTemplateColumns: '1fr 320px'` — eine **feste zweite Spalte**. Bei 390 px passt `1fr + 320px + 20px` nicht mehr; das Aufgaben-`aside` legt sich über den Inhalt und **fängt die Zeigerereignisse ab**: „Fenster konfigurieren" ist sichtbar, aber **nicht anklickbar**. Das ist kein Schönheitsfehler, sondern eine tote Schaltfläche. Bei **768 px** kein äußerer Überlauf, aber die festen Seitenleisten lassen nur einen schmalen Streifen für 2D/3D, und Teile der Werkzeugleiste liegen in versteckten Überläufen. | Generator | `ERLEDIGT` — **FREIGABE** (`1ee27a4`, Bundle `a02b52f`, Votum `f845d78`): bei 390 px **656 → 375**, Elemente über dem Rand **47 → 0**, die tote Schaltfläche ist weg. Vier Breiten nachgemessen. **Zurückgegeben:** der Expertenmodus bei 768/390, mit Zahlen | `docs/planner/ux-befund-layout-alle-ebenen-2026-07-25.md` |
| **AUF-57** | **Der Wegweiser braucht einen Anlass und einen Ort** (Rückgabe des Generators aus AUF-45) — der Mechanismus ist gebaut, gemessen und verriegelt, **erscheint aber nie**: `activeLevel.exists` ist nie verletzt, weil eine Szene immer mindestens ein Geschoss trägt. **Die Zahlen meines Auftrags waren überholt** (78/44/16 stammten aus der Zeit vor AUF-34; gemessen sind es 73/53/28). Der real größte Block sind **28 Werkzeuge, die einem anderen Arbeitsbereich gehören** — nicht das Geschoss. Der Planner entscheidet Anlass und Ort; Vorschlag: ein Hinweis am Arbeitsbereich-Wähler, der die messbare Zahl nennt, statt eines globalen ersten Schritts | Planner → Generator | `ERLEDIGT` — **FREIGABE** (`7cac7cb`, Bundle `e391e73`, Votum `5886267`): die Anzeige hängt am **Ort**, nicht mehr am hartkodierten Grund (`grep` = 0). Auswahl-Anlass mit **25** gemessen, Sichtprobe zeigt den Wegweiser **genau einmal** in der Werkzeug-Schiene. **Der Arbeitsbereich als Anlass ist widerlegt** — jeder Wechsel weg von Architektur sperrt mehr; der Mechanismus lehnt ihn von selbst ab, ohne Ausnahme im Code | `generator-auftrag-auf57-wegweiser-ort.md` |

---

## Notizen, die mit diesen Posten mitgewandert sind

**Zu AUF-12 (Vorrang):** Yama hat am 25.07. entschieden, dass das Dashboard-Design steht und
**zuerst fertiggestellt** wird — versionsweise, v1 ist gebaut, v2 ist dieser Auftrag. Damit rückt AUF-12
vor AUF-11: nicht weil die L1–L7-Inventur falsch wäre, sondern weil ihre **Reihenfolge** einer anderen
Entscheidung folgte. Die Zuordnung v1–v6 → L1–L7 steht in `docs/fahrplan-dashboard-versionen.md` §4;
kein Posten aus L1–L7 fällt weg, jeder bekommt eine Versionsnummer.

**Warum AUF-12 nicht hinter AUF-1 gesperrt ist:** AUF-1 sperrt AUF-4, weil A2 `toolPresentation.ts`
liest. Dashboard v2 fasst weder `toolPresentation.ts` noch den Store noch Zod an — gemessen und in §5
des Auftrags als Guardrail festgeschrieben. Die beiden Arbeitsflächen überschneiden sich nicht.
| **AUF-64** | **🔴 `objekt/203` ist kaputt — PHP-ParseError, Route nicht benutzbar** (Planner-Messung 26.07., 00:40). `e0d1144` (AUF-60) hat einen **`@php … @endphp`-Block** in `objekt.blade.php:97-103` eingefügt. Dieselbe Datei trägt in **Zeile 62** ein **inline** `@php($szeneLeer = …)`. **Beleg aus dem Kompilat** (`storage/framework/views/a3018339….php`): dort steht `<?php($szeneLeer = empty(…))` — **ohne Semikolon, ohne schließendes `?>`**; PHP frisst das folgende HTML und stolpert über `class` im `<form>`. Ignition meldet deshalb Zeile 63. **Die Blade-Änderung ist committet** — der Fehler liegt im Hauptzweig, nicht in einem Arbeitsbaum. `/studio` ist **nicht** betroffen (eigenes Blade, kein Fehler in der Konsole) | Evaluator | `BERICHTET` — **`1b2b26d` + Bericht `fc28a56`, 26.07., 01:05.** Die Route lädt wieder; das schließende Gegenstück ist aus der Datei verschwunden (`grep -c` = **0**, selbst nachgemessen). Belegt: HTTP **200**, 13 663 Bytes, **0** Fehlerspuren, `data-rechte` mit allen vier Rechten. Gates tsc 0 · schema 0 · test 0 · build 0; Insel 1008 → **1009**; PHP-Suite `tests/Feature/Hausplaner` 33 → **38** grün. Mutation (Block-Form zurück) ⇒ **2 rot**. **Code-Commit ohne `public/*`** — selbst geprüft, null Zeilen. **Offener Beleg: die Konsolenprüfung im echten Browser** — die Route liegt hinter `auth`, der hinterlegte lokale Zugang wird abgewiesen. Steht als Willensfrage bei Yama (§3c) | `generator-auftrag-auf64-objekt-route.md`, Ledger „GENERATOR-BERICHT AUF-64" |
| **AUF-60** | **Die Insel muss die Rechte des Nutzers kennen** (Rückgabe aus AUF-53 §4) — gemessen: die React-Insel kennt **genau ein** Recht (`RECHT_BEARBEITEN`), und es stammt **nicht aus dem angemeldeten Nutzer**, sondern wird gesetzt. Damit ist die Zuordnung aus AUF-53 richtig und **wirkungslos zugleich**. **Keine Sicherheitslücke** — der Server schützt jede Route weiter über `CheckUserPermission` — sondern eine **Anzeige-Lüge in beide Richtungen**: ein Werkzeug erscheint bedienbar, das der Server verweigern wird, oder gesperrt, obwohl der Nutzer es dürfte. Der Weg ist die vorhandene Blade-Naht, die schon `data-speichern-url` trägt | Evaluator | `NACHBESSERN → erneut zur Prüfung`. Votum `e4f2a25`: die Rechte-Logik freigabereif (fehlendes Attribut ⇒ **leere** Liste = Minimum, `rechte.ts:31`; 1008/1008; 15 Untertests), blockierend war allein die Blade-Regression aus demselben Commit. **Die ist mit `1b2b26d` behoben und committet.** Damit ist die Bedingung des Evaluators erfüllt — es fehlt nur noch sein zweiter Blick | `generator-auftrag-auf60-rechte-durchreichen.md` |
| **AUF-68** | **Die drei Gruppenwörter aus der Icon-Zeile entfernen** (Wunsch Yama, 26.07., wörtlich: *„kannst du die Wörter Ansicht, Messen & Export sowie Bearbeiten weg machen"*) — gemessen sind es **genau drei Aufrufe** einer Hilfsfunktion (`HausplanerApp.tsx:1144, 1160, 1170`) und die Funktion selbst (`:651`), die **keinen weiteren Aufrufer** hat. **Die Gruppierung geht dabei nicht verloren:** die Trennstriche (`opSep`) liegen bereits zwischen den Gruppen. Rund **150 px** der Zeile werden frei | Generator | `ABGENOMMEN` — **FREIGABE** (`373dfe9`, 26.07., 01:32) für `b5c231e` + Bundle `d59dd8d`. `opLbl` restlos weg (Aufrufe **und** Helfer), sichtbare Gruppenwörter **0**, jede Gruppe mit `role="group"` und nichtleerem `aria-label`. Tests **1010 → 1020**, 0 Skip; 10 neue Zusagen in `opGruppen.test.ts`. Gegen-Beweis: Gruppen-Label auf leer ⇒ **rot** (9/1). Sichtprobe 1440: kein waagerechter Überlauf. **Nebenbefund des Evaluators, selbst gerechnet:** der Trennstrich hat **1,09–1,14:1** Kontrast — WCAG 1.4.11 verlangt 3:1 für bedeutungstragende Grafik. **Die Gliederung trägt also der Abstand (21 px zwischen, 6 px innerhalb), nicht der Strich.** Übernommen als Kriterium in AUF-70 | `generator-auftrag-auf68-gruppenwoerter.md` |
| **AUF-69** | **Die Rechteberechnung zieht aus dem Blade in den Controller** — der Weg, den mein AUF-64-Auftrag entschieden hatte und den der Generator aus gemessenem Grund nicht gegangen ist. **Kein Fehler mehr, sondern Aufräumen:** `auth()->user()`, eine `collect()`-Kette und vier `hasPermission`-Aufrufe sind Anwendungslogik und gehören nicht in eine Vorlage; `HausplanerController::seite()` reicht ohnehin schon drei Variablen durch. Im Blade bliebe **eine** Zeile `data-rechte="{{ $hpRechte }}"` | Evaluator | `ABGENOMMEN` — **FREIGABE** (`54a7c74`, 26.07., 01:26) für `ea60d9e`. Rechteberechnung sitzt im Controller, im Blade bleibt eine Zeile. PHP-Suite 38 → 44 grün, `public/*` null Zeilen. Der erste Posten nach Aufnahme der Blade-Regel (§9) — und er hält sie ohne Erinnerung | Ledger „PLANNER 26.07. — Evaluator-Votum AUF-60" |
| **AUF-22** | **Kollisionsschutz zur Regel machen** — am 25.07. haben zwei Generator-Instanzen (nativ + Cowork) gleichzeitig an `generator-auftrag-dashboard-v2-nacharbeit.md` gearbeitet; `HausplanerApp.tsx` war unter der einen bereits umgebaut, ein fremder untracked Test lag im Baum. Nur weil beide freiwillig vorher auf der Tafel gezogen haben (`c3249d4`, `ca4153b`), ist nichts überschrieben worden. §1 der Tafel schreibt das Ziehen vor — durchgesetzt wird es von nichts. Vorschlag zu bewerten: Ziehen als Vorbedingung im Auftragstext jedes Generator-Auftrags, plus Pflicht-`git status`-Prüfung auf fremde untracked Dateien vor dem ersten Schreibzugriff | Planner | `ERLEDIGT` — **entschieden 26.07., 07:55.** Die drei Punkte stehen jetzt als **§10 Kollisionsschutz** in `docs/agents/06-laufzeiten-und-takt.md`: Ziehen ist **Vorbedingung**, nicht Höflichkeit; `git status` vor dem ersten Schreibzugriff, und **fremde untracked Dateien sind ein Haltesignal**; wer merkt, dass der HEAD sich unter ihm bewegt, hört auf zu messen. Dazu die schon geltende Staging-Regel. **Keine technische Sperre** — eine Sperrdatei wäre selbst ein geteilter Zustand und stünde bei einem Absturz im Weg; die Tafel trägt die Belegung ohnehin | `docs/agents/06-laufzeiten-und-takt.md` §10 |
| **AUF-18** | **Drei zurückgegebene Punkte einordnen** — (a) `RouteNode` (Leitungen) hat keine Gruppe im Projektbaum; §32 legt sechs fest, heute erzeugt kein Werkzeug Routen. (b) Befund-Historie mit `grund`/Zeitstempel/Bauteilbezug braucht eine Store-Änderung → Kandidat v3. (c) `Enter` auf `loeschen`/`duplizieren` ruft die vorhandenen Funktionen — vom Auftrag nicht ausbuchstabiert, Rückbau wäre eine Zeile | Planner | `ERLEDIGT` — **entschieden 26.07., 07:55, alle drei Punkte.** **(a) `RouteNode` bekommt keine Gruppe im Projektbaum** — gemessen erzeugt heute **kein** Werkzeug Routen; eine Gruppe für einen Knotentyp, den niemand herstellt, ist eine leere Schublade und sieht wie ein Versprechen aus. Sie entsteht mit dem ersten Werkzeug, das Routen erzeugt, und gehört in dessen Posten. **(b) Befund-Historie mit `grund`/Zeitstempel/Bauteilbezug: vertagt** — sie braucht eine Store-Änderung und damit K4; das ist kein Nebenbei. **Nicht im Arbeitsvorrat**, sondern ausdrücklich als v3-Kandidat benannt, damit sie nicht als offene Arbeit mitgezählt wird. **(c) `Enter` auf `loeschen`/`duplizieren` bleibt** — es ruft die vorhandenen Funktionen und ist das Standardverhalten eines fokussierten Knopfes im Browser. Ein Rückbau wäre eine Zeile und würde die Tastaturbedienung schlechter machen, nicht sauberer | Ledger „PLANNER 26.07. — zwei Schulden beglichen" |
| **AUF-70** | **Eine Werkzeugzeile — und der gesperrte Zustand wird ablesbar** (Wunsch Yama, 26.07.: *„alle Werkzeuge auf eine Linie … auch 2D, Split, 3D, Rückgängig"*, dazu *„Rückgängig und das Gegenteil funktionstüchtig machen"*). **Der zweite Teil entfällt: Rückgängig und Wiederholen funktionieren** — im laufenden Programm durchgespielt (Befehl ⇒ frei; Rückgängig ⇒ Wiederholen frei; Wiederholen ⇒ Rückgängig frei). **Der Befund ist die Darstellung:** ein gesperrter Knopf dieser Zeile ist von einem freien **nicht zu unterscheiden** — Deckkraft 1, Mauszeiger `pointer`, Schrift, Rahmen und Hintergrund gemessen identisch. `knopf()` kennt einen Zustand; **AUF-59 hat die drei Zustände nur für `OpBtn` hergestellt.** Dazu die Zusammenlegung: heute stehen Werkzeuge in zwei Zeilen (y 179 und y 269) | Evaluator | `ABGENOMMEN` — **FREIGABE** (`031b07d`) für `4c1ce13` + Bundle `0045ea2`, dazu der Nachtrag `3b9d4ec` für Kriterium 13. **Eine Werkzeugzeile:** 5 Gruppen, **16** Knöpfe, alle auf y 269; die Dokumentzeile oben trägt keine Werkzeuge mehr. `docOverflowX` **0 bei 1440 und 1024**. **Gesperrt ablesbar:** Rückgängig 0,6 · `not-allowed` · rgb(167,174,183) · rgb(242,244,246) gegen Split 1 · `pointer` · rgb(35,42,49) · rgb(255,255,255) — **vier** Werte, vorher **keiner**. **Eine Wahrheit belegt:** `knopf()` **liest** `opKnopfBild` statt eine zweite Beschreibung danebenzustellen. Tests 1020 → **1033**, 0 Skip, 13 neue Zusagen; Gegen-Beweis **3 rot** in der neuen Datei. **Abstands-Kriterium erfüllt:** **21 px** zwischen allen Gruppen gegen 6 px innerhalb — bei 16 Knöpfen **nicht verengt**, exakt wie bei 11. Die zwei neuen Gruppen tragen ebenfalls `role="group"` und `aria-label` | `generator-auftrag-auf70-eine-werkzeugzeile.md` · Commit-Nachricht `4c1ce13` |
| **AUF-62** | **„Ansicht einpassen" bauen** (Entscheidung Yama, 25.07., aus AUF-61) — der einzige der fünf toten „(geplant)"-Knöpfe **ohne Gegenstück im Katalog**; die anderen vier waren Dubletten und sind mit AUF-44 entfernt. Er soll die Ansicht so setzen, dass der **gesamte Grundriss des aktiven Geschosses** sichtbar ist. **Technisch klein:** es braucht kein Modell und keinen Command — nur die Bounding-Box der Knoten des Geschosses, den vorhandenen Zoom und den **Pan-Zustand aus AUF-51** (`74fdcb4`), der genau dafür jetzt existiert. **Kein Schreiben ins Dokument** — die Ansicht ist Anzeige, kein Modellzustand | Evaluator | `ABGENOMMEN` — **FREIGABE** (`84942b0`). Blind gegen `bae4596` gemessen, dann der Bericht. **Der Test rechnet nach**, statt zu glauben: jeder Weltpunkt über `aufSchirm()` gegen die Bühne, 17 Untertests über die sechs Kanten. `einpassen.ts` ist **rein** — `grep` auf `getState`, `applyCommand`, `document`, `window` leer. Tests 1034 → **1051**. Gegen-Beweis: y-Spiegelung in der Pan-Formel gebrochen ⇒ **7 rot** — *„ich fuhr die stärkste"*. Sichtprobe: Zoom 12 % → 6 %, der ganze U-Grundriss im Bild. **Die drei geerbten Zusagen als nachgezogen bestätigt, keine ersatzlos gestrichen** | `generator-auftrag-auf62-ansicht-einpassen.md` |
| **AUF-72** | **🔴 Die Bühne ist höher als das Fenster** — **Rückgabe des Generators aus AUF-62**, von mir unabhängig nachgemessen. `HausplanerApp.tsx:1058` rechnet `window.innerHeight - 96`; die **96** stammt aus einer Zeit mit **einer** Leiste über der Bühne. Seither sind Arbeitsbereich-Wähler (AUF-34), Werkzeugzeile und Optionszeile dazugekommen — **gemessen stehen heute 323–369 px darüber**. **Generator bei 1440×900: 227 px unter dem Fenster, 72 % sichtbar. Planner bei 1440×813: 273 px, nur 62 % sichtbar** — und die Seite scrollt nur 46 px, der Rest ist **unerreichbar**. Je kürzer das Fenster, desto schlimmer; Laptops sind kürzer als 900 px | Evaluator | `ABGENOMMEN` — **FREIGABE MIT AUFLAGE** (`fe2eb6b`). **Substanz abgenommen:** Messansatz statt Schätzung, `grep` auf `innerHeight` und die feste `-96` = **0**, Ersatzhöhe 700, Mindesthöhe 200, und das härteste Kriterium **testverriegelt: der Verschub des Nutzers überlebt jede Höhenänderung**. Tests 1051 → **1060**; Gegen-Beweise 2 rot und 1 rot. **Auflage:** die Aussage „Überstand 0" ist widerlegt — **konstant 18 px bei 900 und 813** im gewöhnlichen Arbeitszustand. Canvas-Oberkante **369** (Evaluator und Planner unabhängig) gegen **323** beim Generator; der Unterschied ist die **Werkzeug-Optionen-Zeile**. Kernfehler 227 → 18 px = **92 %**; kein Blocker, der Rest ist über Einpassen und Verschub erreichbar. **Auflage als AUF-73 beauftragt, Weg A** | `generator-auftrag-auf72-buehnenhoehe.md` |
| **AUF-65** | **Das Aufgaben-Panel ist leer und „Freigegeben" ist zu stark** (UX-Bewertung 26.07.) — zwei Befunde auf einem Schirm. **(a)** `GuidedView` rendert `s.aufgaben`; leitet `ableitenSchritte` für den Zustand keine ab, bleibt das große Panel **leer und nimmt trotzdem die halbe obere Fläche**. **(b)** `studioDaten.ts:158` bildet `status: ok` auf das Wort **„Freigegeben"** ab — gemessen. Das ist zu stark: `ok` heißt „im Modell vorhanden", **nicht** „von jemandem freigegeben". Der Schirm sagt gleichzeitig, Import und Maßstab seien unbekannt. **Zu bauen:** leeres Panel **verschwindet** statt leer dazustehen (Muster AUF-45: der Wegweiser geht, wenn er erfüllt ist), und die Statuswörter werden präzise — Vorschlag „vollständig" statt „Freigegeben". **Die Aufgaben selbst kommen aus dem Modell**, jede mit einer direkten Aktion: „keine Fenster ⇒ Fenster konfigurieren", „zwei Geschosse ohne Treppe ⇒ Treppe auslegen" | Evaluator | `ABGENOMMEN` — **FREIGABE** (`13be5cb`). `STATUS_LABEL.ok` = **Vollstaendig**, Schluessel unveraendert, kein Schritt wechselt seinen Status. Leere Aufgabenliste ergibt keine leere Ueberschrift. Tests 1060 auf **1068**; Gegen-Beweis: das alte Wort zurueck ergibt **rot**. **Die Rueckgabe von (b) hat er bestaetigt** — die Praemisse meines Auftrags war gemessen falsch, und er hat es sauber getrennt wie bei AUF-45. **Und er hat einen eigenen Fehler offengelegt:** sein breiter `grep` fand drei Treffer, alle legitim — *Grep zu breit, offengelegt*. Sichtprobe nach **Regel 11 mit Zustand** berichtet | `generator-auftrag-auf65-gefuehrte-planung-ehrlich.md` |
