# AUFTRAGSTAFEL — ausgelagerte Staende (AUF-83), Juli 2026

> **HISTORIE, NICHT VERBINDLICH.** Seit 04.08.2026 gelten ausschließlich die
> [`verbindlichen Arbeitsregeln`](../ARBEITSREGELN.md). Alte Prozessangaben in dieser Datei sind
> aufgehoben.

*Ausgelagert am 30.07.2026 um 21:33 aus der `AUF-83`-Zelle der Auftragstafel.*

**Warum:** die Standspalte war auf **17.989 Zeichen** gewachsen — neun gestapelte
`[vormals: …]`-Staende in **einer** Tabellenzelle. Ich habe jeden neuen Stand vorne
angehaengt und den alten nach hinten geschoben, ohne je zu kuerzen.

*Das ist kein Schoenheitsfehler. Der Zweck der Tafel ist, dass jede Instanz in zehn Sekunden
sieht, was dran ist. Eine Zelle, die man scrollen muss, erfuellt ihn nicht — und ich habe sie
in dem Glauben gefuellt, ordentlich zu arbeiten.*

**Nichts ist verloren.** Die Staende stehen unten unveraendert, die Tafel traegt den Verweis
hierher. Wer die Entwicklung nachlesen will, liest hier — wer wissen will, was JETZT dran ist,
liest die Tafel.

**Regel ab jetzt:** in der Tafel steht der aktuelle Stand und **hoechstens einer** davor.
Was aelter ist, wandert hierher, wenn der naechste Stand geschrieben wird.

---



---

### [vormals: STAND 19:48]* **Heute gesichert (8):** T3/T3-N1 · T5 · AUF-87 · AUF-88-P1 (2) · AUF-50-S1 · AUF-48-S1 `f7441518` · **AUF-48-S2 `59e91b50`** (8 Ableitungen, 2447→2375 Zeilen, Tests 1456→1476). **HausplanerApp.tsx: 2511 → 2375 Zeilen in zwei Scheiben.** **GENERATOR:** `generator-auftrag-auf48-s3-tasten-und-zustand.md` (NEU) — Taste→Absicht auslagern; die vier Zustandsfunktionen (`waehleBereich`, `klappeSchiene`, `oeffnePalette`, `schliessePalette`) BLEIBEN stehen und bekommen Zusagen. Die vier `localStorage`-Zugriffe werden nicht verschoben. **EVALUATOR, sieben Pruefstaende:** `05d490ea..40fa52de` · `56ff2c9e..74ad1075` · `ea2bbf16..fba60e6e` · `df445125..f45b9f22` · `b24b7752..e903ce36` · `39db4dc2..f7441518` · `28c34c11..59e91b50`. 

---

### [vormals: STAND 19:17]* **Heute gesichert (7):** T3/T3-N1 `40fa52de` · T5 `74ad1075` · AUF-87 `2d37d141` · AUF-88-P1 `fba60e6e`+`f45b9f22` · AUF-50-S1 `e903ce36` · **AUF-48-S1 `f7441518`** (2511→2447 Zeilen; **Nebenbefund: die Flaeche war vollstaendig unverriegelt** — sieben Mutationen, keine von 1440 Zusagen rot; er hat 16 Zusagen nachgeschrieben). **GENERATOR:** `generator-auftrag-auf48-s2-ableitungen.md` (NEU) — 18 Ableitungen zwischen `setWerkzeug` und `bandVon` in `ableitungen.ts`, Huellen bleiben stehen. **K-04 verlangt die Mutation VOR den Tests.** **EVALUATOR, sechs Pruefstaende:** `05d490ea..40fa52de` · `56ff2c9e..74ad1075` · `ea2bbf16..fba60e6e` · `df445125..f45b9f22` · `b24b7752..e903ce36` · `39db4dc2..f7441518`. 

---

### [vormals: STAND 18:47]* **Heute gesichert:** T3/T3-N1 `40fa52de` · T5 `74ad1075` · AUF-87 `2d37d141` · AUF-88-P1 `fba60e6e` + `f45b9f22` · **AUF-50-S1 `e903ce36`** — die Werkzeug-Landkarte steht: **fehlt 21 · deckt 41 · ohne-modell 42 · stillgelegt 6**, Summe 110. **83 von 110 brauchen keinen neuen Modellbefehl.** **GENERATOR:** `generator-auftrag-auf48-s1-das-reine.md` (NEU) — sieben reine Funktionen aus `HausplanerApp.tsx` (2511 Zeilen) in ein eigenes Modul, `navGrp`/`navHub`/`navSub` fallen (je 1 Vorkommen), **`navItem` BLEIBT** (2). Schnittkanten ueber NAMEN. **EVALUATOR, fuenf Pruefstaende:** `05d490ea..40fa52de` · `56ff2c9e..74ad1075` · `ea2bbf16..fba60e6e` · `df445125..f45b9f22` · `b24b7752..e903ce36` (**K-01 gegen 110, nicht 111**). 

---

### [vormals: STAND 18:24]* **Gebaut und gesichert:** T3/T3-N1 `40fa52de` · T5 `74ad1075` · AUF-87 `2d37d141` · AUF-88-P1 Backend `fba60e6e` · **AUF-88-P1 Frontend `f45b9f22`** (Basis `df445125`, 17 Dateien, 892/194, 0 fremde Pfade) — der PDF-Grundriss ist als kalibrierte Referenz im Planer. **EVALUATOR, vier Pruefstaende:** `05d490ea..40fa52de` (T3/T3-N1, insbes. K-08) · `56ff2c9e..74ad1075` (T5) · `ea2bbf16..fba60e6e` (AUF-88-P1 Backend) · `df445125..f45b9f22` (AUF-88-P1 Frontend). **GENERATOR:** naechstes Blatt ist **AUF-50-S1** (`generator-auftrag-auf50-s1-werkzeug-landkarte.md`). 

---

### [vormals: STAND 12:42]* **Gebaut und gesichert:** T3/T3-N1 `40fa52de` · T5 `74ad1075` · AUF-87 `2d37d141` · AUF-88-P1 Backend `fba60e6e` (Basis `ea2bbf16`, 7 Dateien, 457/17). Der Arbeitsbaum ist bis auf zwei Dokumentdateien leer — zum ersten Mal heute. **EVALUATOR — drei Pruefstaende offen, kein Votum seit 10:10:** `05d490ea..40fa52de` (T3/T3-N1, insbes. K-08 Buehne > 594 px) · `56ff2c9e..74ad1075` (T5) · `ea2bbf16..fba60e6e` (AUF-88-P1 Backend). **GENERATOR:** (1) **AUF-88-P1 Frontend-Teil** — `grep -c 'Konva.Image|<Image'` = **0**, die gesperrte Unterlage unter der Zeichnung und die Kalibrierung ueber eine bekannte Strecke fehlen; K-03/K-04/K-05/K-07 des Blattes sind offen. (2) danach **AUF-50-S1**. 

---

### [vormals: T5 IST COMMITTET]* **T5 = `74ad1075`, Basis `56ff2c9e`**, 17 Dateien, 1041/239 Zeilen, 0 fremde Pfade. Escape-Rangfolge steht: Palette > Dialog > Menue > Schiene > Reset. **GENERATOR, in dieser Reihenfolge:** (1) **AUF-87 sichern** — `scripts/auftrag-pruefen.mjs`, `scripts/auftrag-pruefen.sh`, `scripts/__tests__/` liegen seit 07:29/08:14 unverfolgt, zwei Commits sind daran vorbeigegangen; `git add -- <die drei Pfade>` und `git commit -- <die drei Pfade>`. (2) **AUF-88-P1** ist entsperrt — der Sperrgrund nannte den BAU von T3 UND T5, beide sind committet. (3) danach **AUF-50-S1** (`generator-auftrag-auf50-s1-werkzeug-landkarte.md`). **EVALUATOR:** T3/T3-N1 auf `40fa52de` ist weiter offen; T5 ist ein eigener Pruefstand `git diff 56ff2c9e..74ad1075`. 

---

### [vormals: GENERATOR ZIEHT JETZT T5]* **REIHENFOLGE DAHINTER, damit niemand wartet (Planner, 30.07., 10:12):** **AUF-87 committen → T5 → AUF-50-S1** (`generator-auftrag-auf50-s1-werkzeug-landkarte.md`, NEU, Spur B, die Werkzeug-Landkarte: 111 Vertraege, 33 `umkehrbar: false`, 19 case-Zweige in `applyCommand` — gemessen gegen `ba69f432`) **oder AUF-88-P1**, das mit dem BAU von T5 selbsttaetig entsperrt. **T1b IST GEBAUT:** `@extends` steht jetzt in BEIDEN Hausplaner-Blades (vorher 0) — das Blatt fuehrt sich noch als `aktiv`, der Befund gehoert zum laufenden Evaluator-Durchgang auf `40fa52de`. **Zwei Dinge, sofort ausfuehrbar, kein neues Blatt noetig:** **(1)** die drei AUF-87-Dateien liegen seit 07:29/08:14 **unverfolgt** im Baum — `scripts/auftrag-pruefen.mjs` (17597 B), `scripts/auftrag-pruefen.sh`, `scripts/__tests__/`. Unter Fassung B werden sie **jetzt committet** (`git commit -- scripts/auftrag-pruefen.mjs scripts/auftrag-pruefen.sh scripts/__tests__`), Basis- und Ergebnis-SHA melden. **(2)** danach **T5** ziehen: `generator-auftrag-auf83-t5-schienen-klappbar.md`. **Die Sperre auf T5 endete mit dem BAU von T3** — T3 ist seit `40fa52de` gebaut UND committet, die Vorbedingung ist erfuellt. **T3/T3-N1 sind gleichzeitig beim EVALUATOR** (Worktree `eval-auf83-t3` auf `40fa52de`) — das ist kein Parallelbetrieb, sondern genau der Zyklus: sein Pruefstand ist ein Commit und kann vom Weiterbauen nicht zerstoert werden. **WARNUNG ZU DEN PRUEFBEFEHLEN:** `npm run test:hausplaner:dom` schlaegt aus dem Cowork-Container fehl — `esbuild` in `node_modules` ist fuer die andere Plattform gebaut. **Das ist kein rotes Ergebnis, sondern eine kaputte Umgebung.** Auf Yamas Rechner gilt das Ergebnis, hier nicht. 

---

### [vormals: UMGESETZT UND COMMITTET — BALLBESITZ EVALUATOR]* **Generator-SHA `40fa52de`, Basis-SHA `05d490ea`**, 18 Dateien, 1465/239 Zeilen, 0 fremde Pfade. **EVALUATOR:** `git worktree add ../eval-auf83-t3-n1 40fa52de` und `git diff 05d490ea..40fa52de` — K-01 bis K-09 aus T3 plus N-01/N-02 aus T3-N1, **insbesondere K-08 (Buehne hoeher als 594 px)**. Nebenbefund zu pruefen: das `public/hausplaner`-Buendel liegt im Commit (388 Zeilen) — passt es zum Quelltext DIESES Commits? **Merge nach `main` bleibt Tor 2 und gehoert Yama.** 

---

### [vormals: PRODUKTIONSMODUS — SOFORT AUSFUEHREN]* **GENERATOR:** die 13 Dateien / 891 Zeilen im Baum jetzt kontrolliert committen. Reihenfolge: `git status --short` und `git diff --stat` lesen · gehoert jede Datei zum Auftrag? fremde oder erzeugte Dateien dabei? Produkt- und Testdateien getrennt? · `git diff` **vollstaendig lesen**, nicht nur Exitcodes · `grep -R 'TODO\|FIXME\|console\.log\|dd(\|dump(' <pfade>` · Pflichtpruefungen fahren (Syntax · `tsc:hausplaner` · betroffene Unit- und DOM-Tests · `php artisan test` fuer `tests/Feature/Hausplaner/`) · **nur freigegebene Pfade** stagen, nie `git add .` · `git commit -- <pfade>` · dann melden: **Auftrag · Branch · Basis-SHA · Generator-SHA · Produktdateien / Testdateien getrennt · ausgefuehrte Pruefungen mit Rohausgabe · Restpunkte · Scope-Abweichungen**. **Der Zweig ist bereits isoliert:** `auto/hausplaner-integration`, 103 Commits vor `main`, 0 dahinter — **es muss kein neuer Zweig angelegt werden.** **Niemals nach `main` mergen, niemals pushen, den eigenen Commit nie freigeben.** **EVALUATOR:** Eingang quittieren, dann `git worktree add ../eval-auf83-t3-n1 <generator-sha>` und `git diff <basis-sha>..<generator-sha>` unabhaengig pruefen — der Commit ist der Pruefstand, den R22 verlangt. **`>>> AUF-83-T3-N1: ZEILE 1 VERSCHLANKEN — K-08 IST ROT, DAS ZIEL IST VERFEHLT <<<`** Yama hat Entwurf 2 freigegeben und ausdruecklich das Vorziehen verlangt: *„jetzt sieht es besser aus und ich gebe das jetzt frei bitte diese aufgabe vorziehen und ich moechte das sehen auf den dashboard bzw bildschirm“*. **AUSLEGUNG DER FREIGABE, offengelegt damit er sie mit einem Satz korrigieren kann:** ich lese sie als Freigabe des **ganzen** Entwurfs **einschliesslich T1b** (Blades an die Ticket-Shell). **Tor 2 — Merge nach `main` und Deploy — bleibt bei ihm.** **REIHENFOLGE — NEU GEORDNET (Planner, 29.07., 10:05), UND DIE SPERRE AUF T1b FAELLT.** **Yama: *„er hat nichts zu tun weil du faul bist staendig aufgaben zu erstellen“* — berechtigt, und der Fehler ist benennbar:** ich hatte T1b `GESPERRT bis T1a ABGENOMMEN` geschrieben. **Der Grund der Sperre war aber ein technischer:** *ohne T1a gaebe der Blade-Umbau einen zweiten Bildlauf.* **T1a ist seit 09:58 GEBAUT** (`97a2e2a4`) — die Vorbedingung ist erfuellt, die Abnahme aendert daran nichts. **Zum dritten Mal eine Sperre, die mehr sperrt als ihr Grund traegt** (00:06 Scheibe 5, 01:28 Scheibe 7, jetzt T1b). **REGEL DARAUS, ab sofort: eine Sperre endet mit dem BAU der Vorbedingung, nicht mit ihrer ABNAHME** — es sei denn, der Sperrgrund nennt die Abnahme ausdruecklich (z. B. weil ein Votum eine Entscheidung enthaelt, die den Folgeauftrag umschreibt). *Geht die Vorbedingung rot, wird der Folgeauftrag mit nachgebessert; das kostet weniger als Stillstand.* **DREI AUFTRAEGE LIEGEN JETZT VOR DER FRONT:** **T1b** (*vormals ⚡*, Sperre gefallen, `generator-auftrag-auf83-t1b-ticket-shell.md`) → **T2** (`generator-auftrag-auf83-t2-zweite-navigation.md`, gesperrt bis T1b **gebaut**) → **T3** (`generator-auftrag-auf83-t3-kopfleiste-arbeitszeile.md`, gesperrt bis T2 **gebaut**) → T5. **T2** laesst die zweite und dritte Navigation fallen — 11 `hp-navi`-Vorkommen in der Insel, 9 in der CSS, dazu Blade-Leiste, Marke und die doppelte Testflaechen-Anzeige. **T3** baut die eine Kopfleiste und die Arbeitszeile; **die 13-teilige Geschosszeile faellt**, Anlegen/Duplizieren/Loeschen wandern ins Menue des Waehlers, und die **Hoehenlage wird sichtbar** — `geschossStapel.ts` fuehrt `elevation` bereits und zeigt sie nirgends (**zweiter Fall dieser Art nach den Griffen in `auswahlDarstellung.ts`**). **BEIDE TRAGEN DIE R9-BARRIERE VON 10:00:** geerbte Zusagen werden ueber die **Liste der Testdateien, die die betroffene Datei einlesen**, gesucht — nicht ueber ein Muster. **WARUM T1a UND NICHT DIE KOPFLEISTE:** *„der Zeichenbereich soll mehr Platz bekommen“* — **Aufraeumen allein gibt keinen Platz.** Die Planbreite kommt aus `HausplanerApp.tsx:369` (`innerWidth - 220 - 268`), die Hoehe aus `calc(100vh - 46px)` in beiden Blades. **Beide messen gegen das Fenster statt gegen den Behaelter** — deshalb ist die Insel weder einbettbar noch overlay-faehig. **Es sind nicht drei Aufgaben, sondern dreimal dieselbe**, und T1a ist zugleich der erste Zerlegungsschritt von **AUF-48**. **T1b IST GESPERRT MIT GEMESSENEM GRUND:** `@yield('content')` sitzt in `.main-content-scroll` — ohne T1a ergaebe der Blade-Umbau einen **zweiten Bildlauf** und einen Zeichenbereich **unter der Falz**, also das Gegenteil des Ziels. **AUFLAGE FUER BEIDE:** `HausplanerApp.tsx` traegt **78 offene Inline-Stellen** (Scheibe 7, gesperrt) und wird von AUF-48 beansprucht — **angefasst wird ausschliesslich die Layout-Rechnung.** Faellt beim Bauen eine Inline-Stelle im Weg auf: **melden, nicht mitnehmen.** **NICHT ANGEFASST WIRD DIE SHELL SELBST** (`app.blade.php`, 11.189 Zeilen, traegt jede CRM-Ansicht) — sie wird **benutzt**, ueber ihre fuenf Anschlusspunkte, und nicht geaendert. **Ebenfalls nicht: die fehlende `permission:Hausplaner,read` auf `hausplaner.studio`** — eigener P2-Posten; sie hier mitzunehmen hiesse, eine Rechteaenderung in einem Layout-Auftrag zu verstecken. **DIE KORREKTUR, DIE ALLES VERSCHIEBT: die Ticket-Navigation existiert nicht.** Beide Hausplaner-Blades haben **0x `@extends`** — sie sind eigene HTML-Dokumente mit eigenem `<head>`, waehrend jede andere Admin-View `@extends('admin.layouts.app')` nutzt; `studio.blade.php:11` sagt selbst *„STANDALONE-Testflaeche“*. **Punkt 1 des Auftrags ist damit kein Erhaltungs-, sondern ein Herstellungsauftrag** — und der einzige Spur-A-Teil, der Auth, Rollen und Routing beruehrt. **Er erklaert zwei von Yamas Beobachtungen als Folge statt als Ursache:** der Testflaechen-Hinweis und die Hausplaner-Bezeichnung stehen doppelt, **weil zwei Schichten unabhaengig eine Kopfleiste zeichnen** — Blade und Insel. Wer nur in der Insel aufraeumt, entfernt jeweils die Haelfte. **DER TECHNISCHE KERN (M4):** `HausplanerApp.tsx:369` rechnet `innerWidth - 220 - 268`. **Solange diese Zeile steht, kann kein Panel als Overlay laufen** — klappt eins zu, erfaehrt der Canvas es nicht. Und **die Formel ist testverriegelt** (`Z1442`), also derselbe Bautyp, der uns in AUF-38 fuenfmal begegnet ist: eine geerbte Zusage, die den alten Zustand festhaelt. **Von drei Flaechen ist heute eine klappbar** (`navZu`); Werkzeugleiste und Eigenschaften-Panel haben **keinen Zustand**, letzteres steht im Code als *„immer sichtbar“*. **Escape gibt es fuenfmal ohne Rangfolge.** **Nutzerzustand liegt in `localStorage`, nicht am Nutzer** — *„pro Nutzer und Workspace“* ist ein Backend-Anschluss und damit **Tor 1**, wie AUF-78 und AUF-40 B. **NICHT IN DIESEM AUFTRAG: Kontextmenues.** Gemessen **0 `onContextMenu`, 0 `onDoubleClick`** in der ganzen Insel — das ist ein neues Bedienmuster, kein Aufraeumen; sonst wird aus *aufraeumen* ein Neubau. **KOLLISION (§13):** `HausplanerStudio.tsx` ist **frei** (Scheibe 4 seit 00:45 abgenommen), `ConfigWizard.tsx` laeuft gerade als Scheibe 5 und wird nicht angefasst — aber **`HausplanerApp.tsx` traegt AUF-38 Scheibe 7 UND AUF-48**, und genau dort liegen M4 und M5. **T1-T3 (Blades + `HausplanerStudio.tsx`) sind sofort baubar** und liefern das sichtbare Aufraeumen; **T4-T5 liefern den Platz** und haengen an AUF-48. **ZWEI FRAGEN AN YAMA:** (1) **T1** — die Blades an die Ticket-App-Shell haengen, mit Auth/Rollen/Routing: das ist ausdruecklich seine Entscheidung. (2) **AUF-48 vorziehen** und T4/T5 auf zerlegten Dateien bauen, **oder** T4/T5 in der ungeteilten Datei? **Meine Empfehlung: das Zweite, mit Auflage** — T4 so schneiden, dass die Breitenrechnung an **einer** Stelle landet (ein Modul, eine Wahrheit); dann ist die Arbeit fuer AUF-48 Vorarbeit statt Nacharbeit **NACHTRAG 01:15 — ENTWURF LIEGT, MARKE STEHT AUF `WARTET AUF ENTWURFSFREIGABE`.** Yama: *„kannst du die aenderung als entwurflayout hier zeigen damit ich es freigeben kann bevor es umgesetzt wird“*. **Entwurf:** `docs/planner/entwurf-studio-kopfleiste-2026-07-29.html` — drei Leisten uebereinander gegen eine, mit den **gemessenen** Hoehen aus der echten CSS (`.hp-bar` 46 · `.hp-studio-kopf` 62 · `.hp-experte-leiste` ~33 = **141 px** heute gegen **~48 px**). **Der Gewinn im Entwurf ist geschaetzt und als Schaetzung ausgewiesen** — gemessen wird er als K-05 per `getBoundingClientRect`, und **waechst die Buehne nicht, ist das der Befund** statt eines Hakens. **BLATT LIEGT, ABER GESPERRT:** `generator-auftrag-auf83-t2t3-kopfleiste.md`, acht Kriterien, Spur B. **Der Zurueck-Link in der Blade BLEIBT** — solange es keine Ticket-Navigation gibt, ist er der einzige Weg aus dem Studio; die Leiste wird ausgeduennt, nicht entfernt. **MEINE ABHAENGIGKEIT WAR ZU ENG:** ich hatte T2 an T1 gehaengt — es haengt aber nur der *Wegfall* der Leiste an T1, nicht das Entfernen der Doppelung. **T1 IST EIN ZWEISCHRITT GEWORDEN, und der erste loest zugleich T4** — siehe `docs/planner/t1-entscheidungsgrundlage-ticket-shell-2026-07-29.md`. **Neuer Befund M8:** die Blades setzen `min-height: calc(100vh - 46px)`, und `@yield('content')` sitzt in `.main-content-scroll` — **`100vh` in einem Scroll-Container erzeugt einen zweiten Bildlauf und schiebt den Zeichenbereich unter die Falz.** **Das ist dieselbe Fehlerklasse wie M4:** die Insel misst gegen **das Fenster** statt gegen **ihren Behaelter** — einmal in der Breite (`innerWidth - 220 - 268`), einmal in der Hoehe. **Solange das so ist, ist sie weder einbettbar noch overlay-faehig.** Daraus: **T1a** (Masse vom Behaelter, kein Blade-Umbau, keine Rechte) **ist zugleich T4 und der erste Zerlegungsschritt von AUF-48** — drei Posten, ein Stueck Arbeit. **T1b** (der Blade-Umbau) wird dadurch kleiner und bleibt bei Yama. **ENTWARNUNG zur Auth:** beide Routen liegen bereits hinter `auth` (`routes/web.php:4983/4988`) — es fehlt **nur die Layout-Vererbung**, nicht die Absicherung. **Nebenbefund (P2, eigener Posten, nicht T1):** `hausplaner.index` traegt `permission:Hausplaner,read`, **`hausplaner.studio` nicht** — nur `auth`. Kein Sicherheitsloch (die Flaeche ist persistenzfrei, `save()` ist No-Op, keine Kundendaten), aber eine **Asymmetrie ohne begruendeten Anlass**, die genau dann auffaellt, wenn die Flaeche doch ein Speicherziel bekommt.
