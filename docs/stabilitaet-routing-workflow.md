# ticket — Stabilität: Routing / Verknüpfungen / Workflow (priorisiert)

**Stand:** 2026-06-28 · **Methode:** Agententeam, 1 Agent je Navigationsbereich + 1 Agent für die Vertriebskette quer (25 Einheiten), gezielt auf Routing / Verknüpfungen / Workflow-Ketten, am **aktuellen** Code verifiziert; bestehende Audits als Basis. **Reine Analyse — kein Code geändert.**

> Kontext: produktiv, 40 Nutzer, 3000 Kunden. Reihenfolge nach Schwere; innerhalb gleicher Stufe zuerst die **genutzten Kernbereiche** (Leads, Kalender, Berichte, Projektplanung, Kunden). Faktura/Buchhaltung nachrangig.

## Überblick
- **109 Befunde** in 25 Einheiten.
- **Schwere:** 🔴 P1 16 · 🟠 P2 33 · 🟡 P3 44 · ⚪ P4 16
- **Klasse:** 🧭 Routing 51 · 🔗 Verknüpfung 27 · 🔄 Workflow 31
- **Aufwand gesamt:** klein 85 · mittel 21 · groß 3

## 🔴 P1 — KRITISCH (bricht genutzte Kernfunktion: Absturz / toter Pfad / Datenverlust)

| # | Navi-Punkt | Klasse | Was bricht | Datei:Zeile | Aufw. | Nutzung |
|--:|---|---|---|---|:--:|---|
| 1 | **CRM › Anfragen › Anfragen (MAIN) → Anfragen-Profil: Status-Kanban** | 🧭 Routing | Route POST /inquiry/{id}/status (inquiry.status) ruft InquiryController@updateStatus auf — diese Methode existiert nicht im Controller. Jeder Status-Drag im Kanban-Board der Anfragen-Profilseite (Unpublished → progress → junk) schlägt mit HTTP 500 fehl. Die Statuszustandsmaschine für die Anfrage-Detailansicht ist vollständig außer Funktion. | `routes/web.php:4021 / app/Http/Controllers/Inquiry/InquiryController.php (Methode updateStatus fehlt vollständig) / resources/views/admin/inquiry/contact_profile.blade.php:1433-1444` | S | 🟢 Kernbereich |
| 2 | **CRM › Leads / Kunden › Leadliste / Kundenakte / Neuer Lead** | 🧭 Routing | Der Button 'Wiederherstellen' für gelöschte Objekte (Alternatives) in der Leadliste ruft per JS POST /lead/objects/{id}/restore-deleted auf. Diese Route existiert weder in der lead.objects.*-Gruppe (nur junk, restore-junk, delete registriert) noch gibt es eine Controller-Methode dafür. Jeder Klick auf 'Wiederherstellen' bei einem gelöschten Objekt endet mit HTTP 404/405 — die Wiederherstellung gelöschter Objekte ist vollständig funktionslos. | `resources/views/admin/new_leads/customer_view.blade.php:5878 (ROUTES.restoreDeleted), :6250 (.js-object-restore-deleted), :5924 (restoreDeleted-Action-Config); routes/web.php:780-784 (lead.objects.*-Gruppe ohne restore-deleted); NewLeadsController: kein restoreDeletedObject-Method` | S | 🟢 Kernbereich |
| 3 | **(Workflow-Kette quer) › Auftrag → Neuer Auftrag anlegen (Modal)** | 🧭 Routing | Route deal.store (POST customer/deal/store) zeigt auf DealController::dealStore() und Route deal.save (POST customer_deal_save) auf DealController::store(). Beide Methoden existieren NICHT in DealController (3622 Zeilen, nur storeCustomerNote als store-ähnliche Methode). Ebenfalls fehlend: info(), price(), jump() — alle in web.php registriert. Das Formular #dealStore in customer_view.blade.php:2666 posted auf deal.store → 500/BadMethodCallException sofort beim Absenden. | `app/Http/Controllers/Customer/Deal/DealController.php:32 (kein store/dealStore/info/price/jump); routes/web.php:4168-4228; resources/views/admin/deal/customer_view.blade.php:2666` | L | 🟢 Kernbereich |
| 4 | **(Workflow-Kette quer) › Externe Firmen → Neue Firma anlegen** | 🧭 Routing | Das 'Neu'-Modal (Titel: 'Nue') in external.blade.php:63 postet auf route('external.update') statt route('external.store'). Die update()-Methode ruft ExternalPersonal::find($request->id)->update() auf; beim Erstellen ist $request->id null → find(null) gibt null zurück → ->update() auf null → TypeError/500. Neue externe Firmen können nicht angelegt werden. | `resources/views/admin/employee/external/external.blade.php:63; app/Http/Controllers/Product/Brand/ExternalPersonalController.php:104,118` | S | 🟢 Kernbereich |
| 5 | **(Workflow-Kette quer) › Mitarbeiter-Dashboard → Tab Projekte / Tab Angebote** | 🔗 Verknüpfung | EmployeeDashboardController::loadTab() ruft $this->getProjects() (Zeile 421) und $this->getOffers() (Zeile 423) auf. Beide Methoden sind in der Klasse und ihren Elternklassen nicht definiert. Klick auf Tab 'projects' oder 'offers' → BadMethodCallException/500 sofort. | `app/Http/Controllers/EmployeeDashboardController.php:421-423 (Aufruf); Zeile 440-614 (nur getTasks, getAppointments, getAllContent, getAdminContent definiert)` | M | 🟢 Kernbereich |
| 6 | **(Workflow-Kette quer) › Förderungen (BEG) → Neue Förderung speichern** | 🔗 Verknüpfung | BegFundingsController::store() (Zeile 54) ruft BegFunding::create() auf, aber die importierte Klasse heißt BegFundings (Zeile 6, use App\Models\BegFundings). BegFunding (ohne 's') existiert nicht als Model. PHP-Fehler: Class 'App\Http\Controllers\Customer\BegFunding' not found → 500 bei jedem Speicherversuch. | `app/Http/Controllers/Customer/BegFundingsController.php:6 (use BegFundings); :54 (BegFunding::create)` | S | 🟢 Kernbereich |
| 7 | **CRM › Anfragen › Neue Anfrage / Kundenanfragen / Meine Anfragen / Veröffentlichte Anfragen: Anfrage bearbeiten** | 🔄 Workflow | InquiryController@update (POST inquiry_update) setzt in Zeile 1360 $inquiry->status = 'Unpublished' bedingungslos. Jede Bearbeitung einer bereits verifizierten (Published) oder freigegebenen Anfrage überschreibt deren Status auf Unpublished — die Anfrage verschwindet aus der 'Veröffentlichten Anfragen'-Liste und verliert den Verifikationsstatus. Derselbe Fehler gilt für finalizeDraft (Zeile 2519). Bereits als Severity kritisch in befunde-bestaetigt.csv Row 7 belegt. | `app/Http/Controllers/Inquiry/InquiryController.php:1360 (update), :1072 (store), :2519 (finalizeDraft)` | S | 🟢 Kernbereich |
| 8 | **Berichte › Einzelseiten › Tagesberichte** | 🔄 Workflow | DailyReportController::isAdmin() (Zeile 1651–1656) queried user_rolls.user_id = int(auth()->user()->name). user_rolls.user_id ist FK auf users.id (Migration 2023_06_14_131732, Zeile 24), aber auth()->user()->name enthält die employees.id (codebase-weiter Hack). Da users.id ≠ employees.id für jeden realen Nutzer, gibt isAdmin() immer false zurück. Folge: Admin-Nutzer sehen in daily_report() (Zeile 1688–1699) und EmployeeListSearch() (Zeile 1338) nur ihre eigenen Daten statt aller Mitarbeiter. Die Übersichtsfunktion (Kernzweck der Seite für Leitungsebene) ist vollständig blockiert. | `app/Http/Controllers/Report/DailyReportController.php:1651` | S | 🟢 Kernbereich |
| 9 | **CRM › Kommunikation › E-Mail-Konten** | 🧭 Routing | Die Blade-View ruft action() mit falschem Namespace 'App\Http\Controllers\EmailConfigurationController@index/store/update' auf. Der tatsächliche Controller liegt unter 'App\Http\Controllers\Email\EmailConfigurationController'. Laravel wirft zur Laufzeit eine InvalidArgumentException ('Action … not defined'), sobald die Seite /email_configuration geöffnet wird — alle Formulare (Suche, Neu anlegen, Bearbeiten) sind damit vollständig defekt. | `resources/views/admin/leads/configuration/configuration.blade.php:26,67,283` | S | 🟡 mittel |
| 10 | **CRM › Partner › Externe Firmen** | 🧭 Routing | Das "Neue hinzufügen"-Formular postet an route('external.update') statt route('external.store'). ExternalPersonalController::update() führt ExternalPersonal::find($request->id)->update(...) aus — da kein id im Request vorliegt, gibt find(null) null zurück und ->update() auf null wirft einen PHP Fatal Error. Jeder Versuch, eine neue Externe Firma anzulegen, schlägt mit 500 ab. | `resources/views/admin/employee/external/external.blade.php:63` | S | 🟡 mittel |
| 11 | **CRM › Partner › Alle Kontakte** | 🔗 Verknüpfung | AllContactController::brand() (Z. 994) ruft Brand::withTrashed()->where('id',...)->restore() auf; ::distributor() (Z. 1001) entsprechend für Distributor. Weder Brand noch Distributor verwenden den SoftDeletes-Trait (nur HasFactory); die Migrations 2023_06_21_101726 und 2023_07_26_082336 definieren keine deleted_at-Spalte. Jeder POST auf /global-restore/brand oder /global-restore/distributor wirft eine BadMethodCallException — die Restore-Aktion aus der Kontaktübersicht ist komplett defekt. | `app/Http/Controllers/Contacts/AllContactController.php:994,1001` | M | 🟡 mittel |
| 12 | **Artikel & Lager › Artikel › Favoriten** | 🔗 Verknüpfung | ProductFavoriteListController::authorizeOwner() und authorizeView() lesen Auth::user()->employee_id — dieses Attribut existiert nicht in der users-Tabelle (users hat nur: id, name, email, is_admin, is_active, image, password). PHP gibt null zurück. Folge: authorizeOwner() prüft ($list->employee_id !== null), was für jeden realen Listeneintrag true ist → immer abort(403). Betroffen: update() (PUT /admin/ajax/products/favorite-lists/{list}) und destroy() (DELETE /admin/ajax/products/favorite-lists/{list}) werfen für ALLE Nutzer immer 403. Bearbeiten und Löschen von Favoritenlisten ist komplett gebrochen. Zusätzlich: attachProduct() und detachProduct() nutzen authorizeView() — für nicht-geteilte Listen ebenfalls immer 403 (Produkte können privaten Listen nicht hinzugefügt/entfernt werden). attachProduct() speichert außerdem employee_id=null, da Auth::user()->employee_id null ergibt, was die Mitarbeiter-Attribution der Items dauerhaft korrumpiert. | `app/Http/Controllers/Product/ProductFavoriteListController.php:275-276 (authorizeOwner), :283-284 (authorizeView), :143 (update), :161 (destroy), :233 (attachProduct), :261 (detachProduct); database/migrations/2014_10_12_000000_create_users_table.php:14-25 (kein employee_id-Feld)` | S | 🟡 mittel |
| 13 | **Artikel & Lager › Lager › Übergaben** | 🔗 Verknüpfung | HandoverController importiert App\Models\Assets (Zeile 8), diese Klasse existiert nicht — nur App\Models\Asset. Betrifft handover() (Neue-Übergabe-Seite, Zeilen 97/107), store() (Lagerbestand-Abzug, Zeile 151), next() (Zeile 159), destroy() (Zeilen 234/236): alle werfen PHP-ClassNotFoundException. Nutzer kann Übergaben anlegen, hinzufügen und löschen nicht mehr ausführen. Die Listenseite (index) lädt noch, da sie Assets nicht direkt benutzt — aber der gesamte Schreib-Workflow ist tot. | `app/Http/Controllers/HandoverController.php:8 (import), :97,:107,:151,:159,:234,:236 (Verwendungen); app/Models/Asset.php (korrekter Klassenname)` | S | 🟡 mittel |
| 14 | **Projekte › Wartung › Wartungsverträge › Bearbeiten (edit-Workflow)** | 🔄 Workflow | Der Edit-Button auf der Show-Seite öffnet den Wizard via CustomerMaintenanceContractController@edit(), welcher die View admin.maintenance.wizard mit mode='edit' zurückgibt. Die View ignoriert $mode und $contract vollständig: das Formular postet immer POST an $storeAction = admin.maintenance.contracts.store (kein @method('PUT'), kein Vertrag-ID-Feld, kein Update-Pfad). Jedes Absenden des Bearbeiten-Formulars legt einen neuen Wartungsvertrag an statt den bestehenden zu aktualisieren — Datenduplikat, kein Update. | `resources/views/admin/maintenance/wizard.blade.php:6-8,483 (storeAction immer store; kein mode-/contract-Handling); app/Http/Controllers/Customer/Maintenance/CustomerMaintenanceContractController.php:1487 (edit() gibt wizard zurück ohne form-action-Überschreibung)` | M | 🟡 mittel |
| 15 | **Artikel & Lager › Lager › Lagerausgaben** | 🔄 Workflow | InventoryRequestOutController@store() zieht den Lagerbestand sofort beim Erstellen einer Anfrage ab (inventory.quantity -= quantity, Zeile 256), obwohl der Datensatz mit Status 'Unpublished' angelegt wird (Zeile 251 — also noch nicht genehmigt). Weder update() noch destroy() stellt den Bestand zurück: destroy() löscht nur den Datensatz-Record (Zeile 320), keine Rückbuchung. Stornierung oder Ablehnung einer Lagerausgabe führt damit zu permanentem, unreversiblem Lagerbestandsverlust. Bei mehreren täglichen Ausgaben akkumuliert sich dieser Fehler. | `app/Http/Controllers/Inventory/InventoryRequestOutController.php:251-257 (store), :305-320 (destroy ohne Rückbuchung), :275-303 (update ohne Rückbuchung)` | M | 🟡 mittel |
| 16 | **Vertrieb › Aufträge › Übersicht (deal.all.list) — Aktionen Löschen / Junk / Un-Junk per Dropdown-Link** | 🧭 Routing | Routen deal_junk/{id}, deal_unjunk/{id}, deal_delete/{id}, deal_restore/{id} verweisen auf DealController@junk, @unjunk, @destroy, @restore — keine dieser vier Methoden existiert im Controller (verifiziert: alle 30 public methods aufgelistet). Klick auf 'Löschen', 'Junk' oder 'Un-Junk' im Dropdown der Auftragsübersicht wirft sofort einen 500-Fehler (BadMethodCallException). Bulk-Action (deal.bulk.action POST) funktioniert als Workaround, ist aber für Einzelaktionen nicht der primäre Pfad. | `routes/web.php:4233-4243 + app/Http/Controllers/Customer/Deal/DealController.php (kein @junk/@unjunk/@destroy/@restore in 3622 Zeilen) + resources/views/admin/deal/partials/list.blade.php:683-689` | S | ⚪ selten |

## 🟠 P2 — HOCH (Fehler in genutzter Funktion, kein Datenverlust)

| # | Navi-Punkt | Klasse | Was bricht | Datei:Zeile | Aufw. | Nutzung |
|--:|---|---|---|---|:--:|---|
| 1 | **CRM › Anfragen › Website-Leads (fusion.forms.index → FusionFormSubmissionController@index)** | 🧭 Routing | Route GET /admin/fusion-forms/import (fusion.forms.import, routes/web.php:658) verweist auf FusionFormSubmissionController@importFromGoneo — diese Methode existiert nicht (nur ajaxImportFromGoneo existiert). Route ist aktuell nicht aus der Sidebar oder der Index-View verlinkt, aber ein Direktaufruf der URL (z.B. über Bookmarks oder externe Verlinkung) ergibt HTTP 500. Zusätzlich: Route POST /fusion/import/one (fusion.import.one, web.php:669) ruft importFusionEntryToInquiry auf, die ebenfalls nicht existiert. | `routes/web.php:658 / routes/web.php:669 / app/Http/Controllers/Wordpress/FusionFormSubmissionController.php (Methoden importFromGoneo und importFusionEntryToInquiry fehlen vollständig)` | S | 🟢 Kernbereich |
| 2 | **CRM › Leads / Kunden › Gelöscht** | 🧭 Routing | GET /restore_leads/{id} ist eine zustandsändernde Aktion (SoftDelete-Restore via NewLeadsController@restore, Zeile 5104-5114), die als einfacher <a href>-Link ohne CSRF-Token eingebunden ist (reason_list.blade.php:1026). Ein eingeloggter Nutzer kann über einen manipulierten Link (z.B. in Chat-Nachricht, E-Mail) die Wiederherstellung jedes beliebigen gelöschten Leads auslösen, ohne dass der empfangende Nutzer eine CSRF-geschützte Aktion bestätigt. Betrifft den 'Gelöscht'-Bereich mit 40 Nutzern unter Realbetrieb. | `routes/web.php:845 (Route::get '/restore_leads/{id}'); resources/views/admin/new_leads/reason_list.blade.php:1026 (<a href ohne @csrf>); NewLeadsController.php:5104` | S | 🟢 Kernbereich |
| 3 | **Projekte › Termine › Terminübersicht (customer/appointments)** | 🧭 Routing | Route GET customer/appointments/{appointment}/reports (routes/web.php:4846) liegt AUSSERHALB der Route::middleware(['auth'])-Gruppe (die bei Zeile 4844 schliesst). Sie überschreibt die intern auth-geschützte Route customer.appointments.reports.list (AppointmentReportController@list, web.php:4842), die laut php artisan route:list nicht mehr registriert ist (tot). Der ungeschützte Endpunkt liefert alle Appointment-Reports und -Kommentare als JSON ohne Login — jeder der eine Appointment-ID kennt, kann Kundendaten abrufen. | `routes/web.php:4846 (außerhalb group), routes/web.php:4842 (überschrieben, nicht in route:list sichtbar), app/Http/Controllers/Appointment/CustomerMainAppointmentController.php:1228` | S | 🟢 Kernbereich |
| 4 | **Arbeitsbereich › Einzelseiten › Dashboard** | 🧭 Routing | Route::get('/', ...) und Route::get('/home', ...) liegen bei Route-Tiefe 0 — vollständig außerhalb jeder auth-Middleware-Gruppe. Kein ->middleware('auth') gesetzt. EmployeeDashboardController@index ruft in Zeile 62 auth()->user()->name direkt auf. Unauthentifizierte Nutzer (Session abgelaufen, direkter Link) erhalten PHP-TypeError (null->name) → HTTP 500 statt Redirect auf /login. Die Authenticate-Middleware existiert und würde korrekt auf /login umleiten — sie wird aber nie ausgeführt, weil die Route keiner auth-Gruppe angehört. | `routes/web.php:605-606` | S | 🟢 Kernbereich |
| 5 | **CRM › Leads / Kunden › Leadliste / Kundenakte** | 🔗 Verknüpfung | NewLeadsController@updateField (POST /update-beg-funding, Zeile 6333) referenziert auf Zeile 6411 die Variable $oldValue im logActivity()-Aufruf, die in der Methode nirgends definiert wird. $alternative->$dbField wird direkt überschrieben (Zeile 6402), ohne den Altwert vorher zu speichern. In PHP 8 ergibt dies eine Deprecation-Notice (undefined variable → null), wodurch jeder Audit-Log-Eintrag 'from: null' enthält statt des tatsächlichen Vorwerts. Bei strikten Fehler-Einstellungen kann die JSON-Antwort fehlschlagen. Die Route wird aufgerufen wenn Förder-Felder im Lead-Profil per AJAX aktualisiert werden. | `app/Http/Controllers/Customer/NewLeadsController.php:6389 ($alternative geladen), :6402 (Wert überschrieben ohne Speichern), :6411 (['from' => $oldValue] — $oldValue nie definiert); routes/web.php:872` | S | 🟢 Kernbereich |
| 6 | **Projekte › Notizen › Persönliche Notizen** | 🔗 Verknüpfung | user_id wird in personal_notes als auth()->user()->name gespeichert (ein String, der die employee_id enthält), nicht als numerische users.id. Alle Abfragen (index, list_index, trash, filter, search, category_search) filtern auf personal_notes.user_id = auth()->user()->name. Wenn users.name (employee_id) nicht eindeutig ist oder sich ändert, werden Notizen anderen Nutzern zugeordnet oder verschwinden. Dasselbe gilt für note_categories.user. Der FK-Hack users.name = employees.id ist projektbekannt (Sidebar-Kommentar), aber hier fehlt jede Konsistenzabsicherung. | `/Users/yamanuri/Documents/ticket/app/Http/Controllers/Employee/Note/PersonalNoteController.php:37 (user_id = Auth::user()->name), 254, 639 / NoteCategoryController.php:24 (where user = auth()->user()->name)` | M | 🟢 Kernbereich |
| 7 | **Projekte › Termine › Terminübersicht › Termindetail (show.blade.php)** | 🔗 Verknüpfung | Im Report-Loop (Zeile 1621–1627) und Kommentar-Loop (Zeile 1965–1971) wird $r->employee->image / $r->employee->name sowie $c->employee->image / $c->employee->name ohne optional() oder ?-> aufgerufen. $reports wird mit with('employee:id,name,lastname,image') eager-loaded, gibt aber null zurück wenn der Mitarbeiter soft-deleted oder regulär gelöscht ist. Dann bricht die gesamte Termindetailseite mit PHP Fatal / null-pointer — betrifft jeden Termin, bei dem ein Berichts- oder Kommentarautor zwischenzeitlich gelöscht wurde. | `resources/views/admin/appointments/show.blade.php:1621-1627 (Report-Loop), :1965-1971 (Kommentar-Loop); app/Http/Controllers/Appointment/CustomerMainAppointmentController.php:1181-1185` | S | 🟢 Kernbereich |
| 8 | **Projekte › Termine › Terminübersicht › Termindetail (show.blade.php)** | 🔗 Verknüpfung | storeComment() speichert comment_by = currentEmployeeId() (gibt null zurück wenn auth()->user()->name nicht numerisch ist, z.B. bei admin-Bypass-User). Direkt danach greift der Code auf $comment->employee->id (Zeile 714-717) ohne Null-Guard zu — führt zu PHP Fatal in der AJAX-Response des Kommentar-Speicherns. Neuer Kommentar erscheint nicht im UI, stattdessen JS-Error. Tritt auf wenn ein Nutzer als User ohne Employee-Datensatz angemeldet ist (is_admin-Bypass-User, admin-Login laut MEMORY.md). | `app/Http/Controllers/Appointment/CustomerMainAppointmentController.php:696-718 (currentEmployeeId() → null → $comment->employee->id fatal)` | S | 🟢 Kernbereich |
| 9 | **Arbeitsbereich › Einzelseiten › Dashboard** | 🔗 Verknüpfung | EmployeeDashboardController@loadTabContent (Route GET\|POST /dashboard/load-tab) ruft in Zeile 421 $this->getProjects() und Zeile 423 $this->getOffers() auf — beide Methoden existieren im Controller nicht (kein grep-Treffer). Wird der Endpunkt aufgerufen, folgt PHP-Fatal 'Call to undefined method'. Parallel dazu referenziert getTabCounts() (Zeile 683-684) Task::count() und Appointment::count(), obwohl weder App\Models\Task (nicht existent) noch App\Models\Appointment (nicht importiert) verfügbar sind — zweiter Fatal-Pfad auf Route GET /dashboard/tab-counts. Beide Routen haben zudem kein auth-Middleware (Tiefe 0). Die aktuelle Live-Dashboard-View (mobile.blade.php) ruft diese Endpunkte NICHT auf — die Bugs sind latent, können aber durch direkte URL-Eingabe oder veraltete Caches getriggert werden. | `app/Http/Controllers/EmployeeDashboardController.php:421-423 (getProjects/getOffers), :683-684 (Task/Appointment), routes/web.php:607-608` | M | 🟢 Kernbereich |
| 10 | **Berichte › Einzelseiten › Berichts-Übersicht** | 🔗 Verknüpfung | OverdueCenterController::markReportNotificationRead() (Zeile 595) und markAllReportNotificationsRead() (Zeile 609) schreiben 'employee_id' als auth()->user()->name (String) direkt in overdue_report_reads.employee_id (unsignedBigInteger laut Migration 2026_04_27_125404). Das funktioniert technisch durch PHP-Cast, ist aber inkonsistent zu currentEmployeeId() (Zeile 2401–2424), die gegen die employees-Tabelle validiert. Falls ein Nutzer kein employees-Record hat (z.B. reiner Admin-User ohne employees-Eintrag), schreibt markReportNotificationRead() eine ungültige employee_id und alle Benachrichtigungen bleiben dauerhaft als ungelesen markiert — Notification-Zähler im Header stimmt nicht. | `app/Http/Controllers/Report/OverdueCenterController.php:595` | S | 🟢 Kernbereich |
| 11 | **Projekte › Einzelseiten › Meine Aufgaben** | 🔗 Verknüpfung | Die Sidebar-URL url('personal/task/' . $employeeId) landet bei PersonalTaskController@index (alte Route). Diese Methode übergibt 'leadStages' NICHT an die View (return view mit 8 Schlüsseln, 'leadStages' fehlt). Die View task_view.blade.php setzt $taskLeadStages = collect($leadStages ?? []) → leere Collection. Konsequenz: Das 'Lead Stage'-Dropdown im Aufgaben-Erstellungsformular ist leer – beim Anlegen einer Aufgabe kann keine Lead-Stage verknüpft werden. Die Filterfunktion nach Lead-Stage ist ebenfalls funktionslos. Betroffen sind ALLE 40 Nutzer, die den Nav-Link verwenden. Der neue Controller (PersonalTaskBoardController@index unter /admin/todo/personal) übergibt 'leadStages' korrekt (Zeile 862), wird aber vom Nav nicht angesteuert. | `app/Http/Controllers/Task/PersonalTaskController.php:298-309 (kein 'leadStages'-Key); app/Http/Controllers/Task/PersonalTaskBoardController.php:862 (korrekt); resources/views/admin/todo/personal/task_view.blade.php:11; resources/views/admin/layouts/sidebar.blade.php:792` | S | 🟢 Kernbereich |
| 12 | **CRM › Leads / Kunden › Kundenakte** | 🔄 Workflow | Der Workflow-Schritt 'Neues Projekt erstellen' (Angebot→Auftrag) aus der Aufträge-Übersicht (deal/customer_view.blade.php:2666) postet an route('deal.store') → DealController::dealStore(). Die Methode dealStore() existiert nicht in DealController (app/Http/Controllers/Customer/Deal/DealController.php — nur index, all, junk_list, delete_list, history, getDealId, profile-Methoden vorhanden). Ebenso fehlen store() (routes/web.php:4168, deal.save), info() (4174, deal.info) und price() (4177, deal.price). Jeder POST auf deal.store bricht mit HTTP 500 (BadMethodCallException). Die Brücke Lead→Auftrag über die Aufträge-Übersicht ist vollständig blockiert. | `routes/web.php:4168-4178 (store/dealStore/info/price-Routen); app/Http/Controllers/Customer/Deal/DealController.php: kein store(), dealStore(), info(), price()-Method; resources/views/admin/deal/customer_view.blade.php:2666 (action={{ route('deal.store') }})` | L | 🟢 Kernbereich |
| 13 | **Projekte › Notizen › Persönliche Notizen** | 🔄 Workflow | initNoteSelect2() ist innerhalb $(document).ready() als lokale Funktion definiert (Zeile 1506), wird aber von openNoteModal() (Zeile 1079, außerhalb des ready-Blocks) und loadCategories() (Zeile 1493) aufgerufen. Beim ersten Öffnen des Erstellen-Modals wirft der Browser einen ReferenceError: initNoteSelect2 is not defined — das Kategorie-Dropdown bleibt ein rohes <select> ohne Select2-Initialisierung. Der '+'-Button (.add_category, Zeile 884) zum Schnellanlegen einer Kategorie aus dem Notiz-Modal heraus hat keinen einzigen Click-Handler im gesamten Script — toter Button, Nutzer kommt nicht weiter ohne Seite zu verlassen. | `/Users/yamanuri/Documents/ticket/resources/views/admin/notes/notes/note.blade.php:1071-1082 (openNoteModal ruft initNoteSelect2), 1506 (initNoteSelect2 nur lokal in ready), 884 (add_category Button ohne Handler)` | S | 🟢 Kernbereich |
| 14 | **Berichte › Einzelseiten › Berichts-Übersicht** | 🔄 Workflow | recentReportsIndex() (Zeile 770–778) übergibt 'canViewAll' => true hart codiert, unabhängig von tatsächlichen Nutzerrechten. canViewAllEmployees() (Zeile 2426–2449) prüft $user->is_admin, $user->admin, $user->role — users-Tabelle hat die Spalte is_admin als Integer (Migration 2014_10_12). Für Nutzer mit is_admin = null oder 0 wird canViewAll in der View als true gesetzt, obwohl sie keine Admin-Rechte haben. Die Seitenansicht zeigt Mitarbeiter-Dropdown und Organisations-Auswahl; das serverseitige Fetch (recentReportsFetch, Zeile 2481) nutzt dagegen canViewAllEmployees() korrekt und filtert dort. Resultat: UI zeigt Filter an, die keinen Effekt haben; Mitarbeiter sehen Daten aller Kollegen in der UI-Auswahl, Fetch-Ergebnisse werden aber korrekt eingeschränkt. Widersprüchliches Verhalten. | `app/Http/Controllers/Report/OverdueCenterController.php:776` | S | 🟢 Kernbereich |
| 15 | **(Workflow-Kette quer) › Anfrage → Anfrage anlegen / bearbeiten** | 🔄 Workflow | InquiryController::store() (Zeile 1072) und update() (Zeile 1360) setzen status immer auf 'Unpublished' — unabhängig vom Vorgang. Jede neu erstellte Anfrage landet sofort in 'Unpublished' und ist in der Standard-Ansicht nicht sichtbar (Index filtert whereNotIn 'unpublished'). Das Update einer Anfrage setzt deren Status ebenfalls auf Unpublished zurück, auch wenn sie vorher Published war. Erfordert manuellen Publish-Schritt nach jeder Anlage/Bearbeitung. | `app/Http/Controllers/Inquiry/InquiryController.php:1072 (store), :1360 (update)` | S | 🟢 Kernbereich |
| 16 | **(Workflow-Kette quer) › Lead→Angebot: LeadProductList-Status nicht aktualisiert** | 🔄 Workflow | OfferWizardController::createOffer() legt Offer + OfferFolder + OfferDetail an, aktualisiert aber lead_product_lists.status NICHT (kein Update auf 'offer' oder 'angebot'). Das Kanban-Board zeigt den Lead damit weiterhin als 'Lead' ohne Angebot. Erst wenn das Angebot explizit auf 'deal' gesetzt wird (changeDocumentStatus), springt der Status. Der Zwischenschritt 'Angebot erstellt' fehlt im Lead-Phasen-Tracking. | `app/Http/Controllers/Customer/Offer/OfferWizardController.php:400-631 (gesamte createOffer-Transaktion ohne lead_product_lists-Update)` | S | 🟢 Kernbereich |
| 17 | **(Workflow-Kette quer) › Auftrag → Auftragsbestätigung** | 🔄 Workflow | Die Kette Angebot→Auftrag (changeDocumentStatus) erzeugt einen Deal-Datensatz mit status='active'. Ein dedizierter Schritt 'Auftragsbestätigung' (AB-Nummer, Bestätigungsdokument, Status-Transition) existiert weder als Route noch als Controller-Methode. Die Kette bricht nach dem Deal-Anlegen ab — es gibt keinen Folgeschritt zur Auftragsbestätigung; Rechnung (DealInvoice) ist direkt nächster Schritt, was im deutschen Handelsrecht eine Lücke hinterlässt. | `app/Http/Controllers/Customer/Offer/OfferFolderController.php:3116-3173 (Angebot→Auftrag, kein AB-Schritt); routes/web.php:4331-4335 (deal.invoice direkt nach deal ohne AB)` | L | 🟢 Kernbereich |
| 18 | **(Workflow-Kette quer) › Urlaubsantrag → Genehmigung (approve) ohne Tagereduzierung** | 🔄 Workflow | LeaveController::approve() (GET /leave_approve/{id}) setzt nur status='accept' und approved='Yes', reduziert aber employees.remaining_day NICHT. Nur der Self-Service-Endpoint save() (Zeile 1143) reduziert remaining_day bei Antragstellung. Der Admin-Genehmigungsweg über approve() hat also keinen Effekt auf den Urlaubskontostand. Mitarbeiter behalten nach Genehmigung vollen Urlaubsanspruch. Außerdem ist leave.approve ein GET-Request für eine Schreiboperation (CSRF-fragil). | `app/Http/Controllers/Employee/Profile/LeaveController.php:230-259 (approve ohne remaining_day-Update); routes/web.php:1798 (GET statt POST)` | M | 🟢 Kernbereich |
| 19 | **CRM › Kommunikation › Posteingang** | 🧭 Routing | Der 'Sent'-Ordner-Link im Sidebar der email_view.blade.php zeigt auf url('send_email_view'). Diese Route ist in keiner routes-Datei registriert — ein Klick darauf ergibt eine 404-Antwort. Damit ist der gesamte Gesendet-Pfad im Posteingang ein toter Link. | `resources/views/admin/leads/email/email_view.blade.php:57` | S | 🟡 mittel |
| 20 | **Vertrieb › Angebote › Vorlagen** | 🧭 Routing | Nach erfolgreichem POST auf /offer-templates/{template}/use leitet useTemplate() auf url('/wizard') weiter — generiert die URL /wizard, die nicht existiert (Route nur unter /offers/wizard registriert). Der Nutzer erhält nach dem Erstellen eines Angebots aus einer Vorlage einen 404-Fehler statt ins Wizard-Frontend weitergeleitet zu werden. Die gesamte Vorlage-Anwenden-Funktion endet im Fehlerzustand. | `app/Http/Controllers/Customer/Offer/OfferTemplatePickerController.php:413` | S | 🟡 mittel |
| 21 | **Projekte › Wartung › Wartungs-Checklisten › Neue Checkliste (Erstellen-Button)** | 🧭 Routing | Der Sidebar-Link endet auf '#new-checklist' (sidebar.blade.php:864). Dieses Hash-Anchor-Target existiert nirgends in der View (kein id='new-checklist', kein Hash-Listener in JS). Der Nutzer landet still oben auf der Seite, das Erstellen-Modal öffnet sich nicht. Zusätzlich: Der 'Neue Checkliste'-Button (id='mcl-btn-create', Zeile 1602) hat keinen einzigen JS-Event-Handler in der gesamten Codebase — der Button ist vollständig tot. Neue Checklisten können über die Navi nicht angelegt werden. | `resources/views/admin/layouts/sidebar.blade.php:864 (Hash #new-checklist); resources/views/admin/maintenance_checklists/index.blade.php:1602 (Button ohne Handler); grep -rn 'mcl-btn-create' resources/ ergibt 0 JS-Handler-Treffer` | S | 🟡 mittel |
| 22 | **Support › Tickets › Ticket-Übersicht** | 🧭 Routing | Der Löschen-Button in der Ticket-Übersicht (problem_board.blade.php) baut die URL /problem_destroy/${ticket.id} als <a href> auf. Die Route problem_destroy ist als Route::get definiert (routes/web.php:1919) — kein HTTP-DELETE-Verb, kein CSRF-Token. Jeder authentifizierte Nutzer kann durch direkten URL-Aufruf oder per manipuliertem Link beliebige Tickets ohne serverseitige CSRF-Absicherung löschen. Der onclick-Confirm ist clientseitig und triviell umgehbar. | `routes/web.php:1919 / resources/views/admin/problem/problem_board.blade.php:1992–2044` | M | 🟡 mittel |
| 23 | **CRM › Kommunikation › Bitrix24-Chat** | 🔗 Verknüpfung | Die Route chats.view (GET chats/{user}) führt zu MessageController@index, das ausschliesslich legacy, read-only Bitrix-Importdaten aus der bitrix_chats-Tabelle anzeigt. Der $user-Parameter wird im Controller komplett ignoriert. Der Sidebar-Badge 'chat_unread' zählt dagegen ungelesene Nachrichten des modernen Chat-Systems (App\Models\Chat). Nutzer sehen also einen Ungelesen-Zähler für den modernen Chat, landen per Klick aber im falschen, inaktiven Bitrix-Archiv ohne Schreibzugriff. Der funktionierende moderne Chat (ChatController@index unter admin/chat) ist im Navi-Menü nicht direkt verlinkt. | `app/Http/Controllers/MessageController.php:74-83 / resources/views/admin/layouts/sidebar.blade.php:491 / routes/web.php:4520` | S | 🟡 mittel |
| 24 | **CRM › Partner › Externe Firmen** | 🔄 Workflow | Die Suche in ExternalPersonalController::index() verknüpft company_name LIKE '%search%' AND admin_name LIKE '%search%' (Z. 27-28) als chained where()-Aufrufe. Eine Suche nach einem Firmennamen allein liefert nur Treffer, bei denen admin_name denselben Begriff enthält — faktisch immer leere Ergebnisse. Mitarbeiter können externe Firmen über die Suche nicht mehr auffinden. | `app/Http/Controllers/Product/Brand/ExternalPersonalController.php:27-28` | S | 🟡 mittel |
| 25 | **Personal › Mitarbeiter › Zeitpläne** | 🔄 Workflow | TimeManagementController@updateStatus() (POST time-management/{plan}/status) hat die Berechtigungsprüfung vollständig auskommentiert (Zeilen 208-210: '// Example permission:'). Jeder authentifizierte Nutzer kann beliebige Zeitpläne anderer Mitarbeiter genehmigen oder ablehnen — ein Mitarbeiter kann seinen eigenen Plan selbst genehmigen. Ebenso fehlt in submit() (Zeilen 181-182) und save() (Zeilen 17-18 im index) jede Ownership-Prüfung. Der Genehmigungsworkflow ist damit wirkungslos. | `app/Http/Controllers/Employee/TimeManagement/TimeManagementController.php:208-210 (updateStatus), :181-182 (submit)` | S | 🟡 mittel |
| 26 | **Personal › Mitarbeiter › Krankheit & Urlaub** | 🔄 Workflow | LeaveController@approve() (Route GET /leave_approve/{id}, routes/web.php:1798) setzt leave.approved='Yes' und leave.status='accept', dekrementiert aber employees.remaining_day NICHT. Mitarbeiter können unbegrenzt genehmigte Urlaubstage akkumulieren ohne Tage-Verbrauch. Zusätzlich ist die Genehmigung als GET-Route implementiert (kein CSRF-Schutz). Bereits in befunde-bestaetigt.csv #9 erfasst — hier als Beleg für den Mitarbeiter-Navigationsbereich dokumentiert. | `app/Http/Controllers/Employee/Profile/LeaveController.php:230-258 (approve: kein decrement); routes/web.php:1798 (GET-Route für Genehmigung)` | M | 🟡 mittel |
| 27 | **Personal › HR-Daten › Urlaubsanspruch** | 🧭 Routing | Die gesamte Seite /leave_day_view (route: leave.day.info) wirft zur Laufzeit eine InvalidArgumentException beim View-Rendering, weil das Blade-Template dreimal action() mit dem nicht existierenden Namespace App\Http\Controllers\LeaveDayController aufruft. Der echte Controller liegt unter App\Http\Controllers\Employee\Profile\LeaveDayController. Betroffen: Suchformular (Z.26), Anlegen-Modal (Z.54) und Bearbeiten-Modal (Z.158). Da action() bereits beim Rendern des Templates ausgewertet wird, ist die Seite vollständig unzugänglich – kein Datensatz kann eingesehen, angelegt oder bearbeitet werden. | `resources/views/admin/employee/holiday/leave_day.blade.php:26,54,158` | S | ⚪ selten |
| 28 | **Artikel & Lager › Artikel-Daten › Rabattgruppen** | 🧭 Routing | Nach Anlegen (store) oder Bearbeiten (update) einer Rabattgruppe wirft Laravel eine RouteNotFoundException, weil redirect()->route('discount.group.info') aufgerufen wird — die Route heißt aber 'discount_group.info' (Unterstrich statt Punkt). Der Nutzer sieht einen 500-Fehler; der Datensatz wurde zwar gespeichert, aber der Redirect schlägt fehl. Bug N-C3 ist im aktuellen Code noch nicht behoben. | `app/Http/Controllers/Product/DiscountGroupController.php:59,94` | S | ⚪ selten |
| 29 | **Admin › System › Filialen** | 🔗 Verknüpfung | Alle drei Inline-Permission-Checks in branch.blade.php vergleichen user_rolls.user_id (Integer-FK auf users.id) gegen auth()->user()->name (speichert employee_id als String). Die Abfragen liefern immer null → Schaltflächen 'Neue Filiale' (Zeile 599), 'Bearbeiten' (Zeile 803) und 'Löschen' (Zeile 858) werden für alle normalen Nutzer nie angezeigt. Nur Nutzer mit is_admin-Bypass umgehen das Problem. Das gesamte CRUD der Filialen-Verwaltung ist für reguläre Admins unsichtbar/gesperrt. | `resources/views/admin/branch/branch.blade.php:599,803,858` | S | ⚪ selten |
| 30 | **System / Sonstiges › Feedback** | 🔗 Verknüpfung | FeedbackController::isProgrammer() (Zeile 29) fragt user_rolls.user_id (unsignedBigInteger FK auf users.id) mit auth()->user()->name (String-Spalte, die employee_id enthält) ab. Typ-/Wert-Mismatch → EXISTS liefert immer false. Folge: update() (Feedback beantworten, Z. 205), changeStatus() (Z. 248) und destroy() (Z. 356) verweigern jedem Nutzer den Zugriff mit HTTP 403 — unabhängig von der tatsächlichen Rolle. Auch baseFeedbackQuery() (Z. 52) filtert zu Unrecht auf eigene Einträge statt Alle: der Programmer-Tab in der UI ist für niemanden funktionsfähig. | `app/Http/Controllers/System/FeedbackController.php:29` | S | ⚪ selten |
| 31 | **System / Sonstiges › Datenbankbereinigung** | 🔗 Verknüpfung | GarbageController::ensureDeletePermission() (Zeile 29) fragt user_rolls.user_id (unsignedBigInteger FK auf users.id) mit $user->name (String, enthält employee_id) ab — identisches Muster wie FeedbackController. user_rolls.user_id ist per Migration 2023_06_14_131732 als unsignedBigInteger mit FK-Constraint auf users.id definiert. Der Typ-Mismatch liefert immer EXISTS=false → abort_unless(false, 403) → jeder Zugriff auf /admin/garbage wird mit HTTP 403 abgewiesen. Datenbankbereinigung ist für alle Nutzer komplett funktionsunfähig. Bereits als befunde-bestaetigt.csv Zeile 12 (Severity kritisch) dokumentiert. | `app/Http/Controllers/Admin/GarbageController.php:29` | S | ⚪ selten |
| 32 | **Vertrieb › Aufträge › Rechnungen (Aufträge) — Auftrag-zu-Rechnung Workflow-Kette** | 🔄 Workflow | Der Menüpunkt 'Rechnungen' führt zu InvoiceController@index (Tabelle: invoices, neues System seit 2026). Das Deal-Profil (deal/{deal}/profile) liest Rechnungen jedoch ausschließlich aus der alten Tabelle deal_invoices (DealController::profile() Z. 2826-2829). Rechnungen, die über den neuen InvoiceController / Canvas-System erstellt werden, erscheinen nicht im Reiter 'Rechnungen' des Auftrags-Profils — die Auftrag→Rechnung-Kette ist unterbrochen. Zusätzlich fehlt im Deal-Profil jeder Button oder Link, um eine neue Rechnung im neuen System anzulegen; der einzige Einstieg in das Canvas ist die Rechnungs-Übersicht selbst. | `app/Http/Controllers/Customer/Deal/DealController.php:2826-2829 (deal_invoices) vs. routes/web.php:4464-4465 (admin.invoices.index → InvoiceController, Tabelle invoices) + database/migrations/2026_06_09_123236_add_deal_link_and_history_to_invoices.php (deal_id in invoices-Tabelle vorhanden, aber nicht für Profile-Query genutzt)` | M | ⚪ selten |
| 33 | **Personal › Organisation › Organigramm** | 🔄 Workflow | DOMContentLoaded wird in organization.blade.php zweimal registriert (Zeilen 698 und 1224). Beide Handler rufen loadBranches(), loadAllEmployees() und orgApplyZoom() auf; der zweite initialisiert zusätzlich den Recycle-Bin. Beim Seitenaufruf feuern zwei parallele AJAX-Requests auf chart/branches, der Branches-Select wird doppelt befüllt, loadDepartments() zeichnet den Baum zweimal in #tree, und initializeEmployeeDragAndDrop() legt auf allen .employees ul-Containern doppelte Sortable-Instanzen an (nur der Recycle-Bin ist durch sortableInit=1 geschützt). Resultat: Organigramm-Baum erscheint doppelt, Drag-and-Drop der Mitarbeiter in Abteilungen ist unzuverlässig, weil zwei Sortable-Instanzen auf denselben Elementen kollidieren. | `resources/views/admin/employee/department/organization.blade.php:698 und :1224` | S | ⚪ selten |

## 🟡 P3 — MITTEL (brüchig/fragil, noch nicht akut)

| # | Navi-Punkt | Klasse | Was bricht | Datei:Zeile | Aufw. | Nutzung |
|--:|---|---|---|---|:--:|---|
| 1 | **CRM › Anfragen › Anfragen (alle Listen) → GET-Route für Löschen** | 🧭 Routing | Route GET /inquiry_delete/{id} (inquiry.delete, web.php:4010) ist als HTTP GET definiert. Der Delete-Button in contact_list.blade.php setzt diese URL als href in einem Bestätigungs-Modal (nicht als AJAX-POST). Ein Browser-Prefetch, ein eingebetteter Link oder ein Fehler-Klick ohne Modal-Bestätigung kann dadurch einen Datensatz ohne CSRF-Schutz unwiderruflich soft-löschen. Analog: GET /inquiry_publish/{id} (inquiry.publish, web.php:4011) ruft zudem InquiryController@publish auf, die nicht existiert (500). | `routes/web.php:4010-4011 / resources/views/admin/inquiry/contact_list.blade.php:2393` | M | 🟢 Kernbereich |
| 2 | **Projekte › Notizen › Kategorien** | 🧭 Routing | Löschen einer Kategorie erfolgt per GET-Link (Route::get '/note_category_destroy/{id}') ohne CSRF-Schutz. Der Delete-Link im View ist ein einfaches <a href=...> ohne Form/Token. Jeder, der die URL mit einer gültigen ID kennt, kann fremde Kategorien löschen. Zusätzlich fehlt im destroy()-Controller eine Ownership-Prüfung: NoteCategory::find($id)->delete() ohne WHERE user=auth()->user()->name — jede Kategorie beliebiger Nutzer kann gelöscht werden. | `/Users/yamanuri/Documents/ticket/routes/web.php:4607 / /Users/yamanuri/Documents/ticket/resources/views/admin/notes/category/note_category.blade.php:825-826 / /Users/yamanuri/Documents/ticket/app/Http/Controllers/Employee/Note/NoteCategoryController.php:127-133` | S | 🟢 Kernbereich |
| 3 | **Projekte › Termine › Termine (MAIN-Header)** | 🧭 Routing | MAIN-Header hat url: '' — kein Klick-Ziel, kein Redirect, keine Landing-Page. Nutzer der Sidebar sieht einen toten Label ohne Funktion. Bekanntes Muster laut navigation-konzept-audit.md:61. | `groups.json[7].items[0]:url='', navigation-konzept-audit.md:61` | S | 🟢 Kernbereich |
| 4 | **Arbeitsbereich › Einzelseiten › Lead-Kanban** | 🧭 Routing | Route POST /lead/kanban/update-stage/{leadProductId}/{stage} (routes/web.php:921) mappt auf LeadOverviewController@updateStage(Request $request, LeadStage $stage). Die Controller-Methode erwartet Route-Model-Binding auf {stage} (LeadStage-ID) und ignoriert {leadProductId} vollständig. Die Route ist für Admin-Phasen-Umbenennung gedacht (kein Kanban-Karten-Drag), wird aber unter /lead/kanban/* geführt. Das kanban.js verwendet stattdessen /lead-product/change-stage/{customer_id}/{alternative_id}/{product_id} für Phasenwechsel — das ist korrekt. Der Mismatch zwischen URL-Parametern und Controller-Signatur erzeugt kein Runtime-Error (Laravel löst nur {stage} per Model-Binding auf), ist aber irreführend und fehleranfällig bei zukünftiger Nutzung von {leadProductId}. | `routes/web.php:921 vs. app/Http/Controllers/Customer/Kanban/LeadOverviewController.php:4544` | S | 🟢 Kernbereich |
| 5 | **Berichte › Einzelseiten › Überfällige Berichte** | 🧭 Routing | admin/report/index.blade.php (Zeile 978) enthält einen Breadcrumb-Link auf url('/employee_dashboard'). Die Route employee_dashboard existiert zwar (routes.txt Zeile 1175), ist aber nicht der App-Standard-Home-Pfad (url('/') → home-Route). Für Nutzer die direkt auf /admin/reports landen, zeigt der Breadcrumb 'Home' als employee_dashboard, nicht als tatsächliches Dashboard — kein Absturz, aber irreführende Navigation bei Klick wenn der Nutzer nur url('/') erwartet. | `resources/views/admin/report/index.blade.php:978` | S | 🟢 Kernbereich |
| 6 | **Arbeitsbereich › Einzelseiten › Dashboard** | 🔗 Verknüpfung | EmployeeDashboardController@getMyData (Route GET /employee/my-dashboard-data) rendert in Zeile 1850 view('admin.dashboard.employee.partials.my_customers_modal') und in Zeile 1897 view('admin.dashboard.employee.partials.my_projects_modal'). Beide View-Dateien existieren nicht im aktuellen Partials-Verzeichnis (resources/views/admin/dashboard/employee/partials/) — sie liegen ausschließlich im veralteten Old/partials/-Unterordner. Aufruf des Endpunkts würde ViewNotFoundException auslösen. Die aktuelle Live-Dashboard-View (mobile.blade.php, 13.425 Zeilen) und das kompilierte kanban.js referenzieren diesen Endpunkt nicht — Befund ist latent, kein akuter Tagesbruch. | `app/Http/Controllers/EmployeeDashboardController.php:1850,1897 / resources/views/admin/dashboard/employee/partials/ (8 Dateien vorhanden, my_customers_modal + my_projects_modal fehlen)` | S | 🟢 Kernbereich |
| 7 | **Berichte › Einzelseiten › Tagesberichte** | 🔗 Verknüpfung | DailyReportController::EmployeeListSearch() (Zeile 1338) filtert Mitarbeiter mit ->when(!$isAdmin, fn($q) => $q->where('id', $authId)) wobei $authId = auth()->user()->name (String). employees.id ist unsignedBigInteger; MySQL akzeptiert den String-Vergleich mit implizitem Cast, aber die Kombination aus falschem isAdmin() (Bug oben) und String-Vergleich bedeutet: Ein nicht als Admin erkannter Admin-Nutzer mit employees-id '12' sieht nur Mitarbeiter ID 12 (korrekt), aber wenn users.name nicht exakt der employees.id entspricht (Leerzeichen, Null-Wert), bekommt er keine Ergebnisse. Kein harter Fehler, aber fragile Verknüpfung. | `app/Http/Controllers/Report/DailyReportController.php:1338` | M | 🟢 Kernbereich |
| 8 | **(Workflow-Kette quer) › Kanban-Filterung mit users.name als Employee-ID** | 🔗 Verknüpfung | DealController::applyDealFilters() (Zeile 381) und storeCustomerNote() (Zeile 1394) lesen auth()->user()->name als Employee-ID. Der Filter 'Meine Aufträge' vergleicht deals.employee_id = users.name (String). Falls users.name kein numerischer Employee-ID-Wert ist (z.B. bei normalen User-Accounts), findet der Filter keine Treffer und der Mitarbeiter sieht keine eigenen Aufträge. Außerdem werden DealNotes mit created_by=users.name statt employees.id gespeichert, was Lookup-Verknüpfungen bricht. | `app/Http/Controllers/Customer/Deal/DealController.php:381, :1394, :1444-1445` | M | 🟢 Kernbereich |
| 9 | **CRM › Anfragen › Website-Leads (fusion.forms.index) — Workflow Anfrage → Angebot** | 🔄 Workflow | FusionFormSubmissionController@index gibt nur return view('admin.fusion.index') ohne Datenbankabfrage zurück. Die View lädt Formulare per AJAX (fusion.forms.list → listForms), zeigt Einträge als rohe Tabelle. Import-Button 'Import All' ruft fusion.import.all → importAll() auf (existiert, funktioniert). Import-Button 'importEntry' ruft fusion.import.single → importSingle() auf (existiert). Die Route fusion.import.one (importFusionEntryToInquiry, fehlt) ist nicht aus der View verlinkt. Kein Fehler-Empty-State, keine Pagination-UI, kein Hinweis wenn API nicht erreichbar. Hardcodierte API-URL mit Token im Klartext im Controller (solar-aspekt.de). | `app/Http/Controllers/Wordpress/FusionFormSubmissionController.php:32-35 / resources/views/admin/fusion/index.blade.php:95-130` | M | 🟢 Kernbereich |
| 10 | **CRM › Leads / Kunden › Kundenakte** | 🔄 Workflow | Die Kundenakte-URL ($leadHistorySidebarUrl) ist kontextabhängig: Ohne aktiven Lead im Request-Kontext fällt die Sidebar auf url('new_lead_view') zurück (sidebar.blade.php:380-382). Der Nutzer der direkt auf 'Kundenakte' klickt — ohne zuvor eine Detailseite aufgerufen zu haben — landet auf der Leadliste statt auf einer Kundenakte. Erst nach manuellem Öffnen eines Leads wird die Kundenakte korrekt verlinkt. Die Navi-Position verhält sich inkonsistent und erzeugt Verwirrung bei täglichen Nutzern. | `resources/views/admin/layouts/sidebar.blade.php:363-382 ($currentLeadHistoryCustomerId-Logik, Fallback url('new_lead_view')); navigation-konzept-audit.md Zeile 102 (bestätigt)` | M | 🟢 Kernbereich |
| 11 | **Projekte › Notizen › Persönliche Notizen** | 🔄 Workflow | make_task() (aufgerufen wenn 'Zum Kalender hinzufügen' aktiviert wird) erstellt PersonalTask mit hartkodiertem progress=40 und start_time='09:00:00' unabhängig von Nutzereingaben. Der erstellte Task hat außerdem start_date=now() statt dem Fälligkeitsdatum der Notiz als Startdatum — das Fälligkeitsdatum der Notiz wird als end_date des Tasks verwendet, was semantisch falsch ist. Nutzer, die eine Notiz mit Kalenderintegration anlegen, erhalten einen Task mit vorgetäuschtem 40%-Fortschritt und fixierter 09:00 Uhr Startzeit, ohne Einfluss darauf. | `/Users/yamanuri/Documents/ticket/app/Http/Controllers/Employee/Note/PersonalNoteController.php:298 (start_time hardcoded), 308 (progress: 40 hardcoded), 311 (start_date: now() statt Notiz-Deadline)` | S | 🟢 Kernbereich |
| 12 | **Projekte › Termine › Mein Kalender (tasks/calendar/personal)** | 🔄 Workflow | Das Duplizieren eines Termins (ROUTE.duplicateAppointment → POST appointment/duplicate) hat die Validierungsregel 'new_date' => 'required\|date\|after_or_equal:today'. Nutzer können damit keine Termine auf vergangene Daten duplizieren (z.B. für Nacherfassung). Validation schlägt mit 422 fehl, SweetAlert zeigt nur 'Validation failed' ohne erklärendes Feedback. Nicht kritisch, aber im produktiven Tagesgeschäft (Nacherfassung) störend. | `app/Http/Controllers/Appointment/MainAppointmentController.php:205 ('after_or_equal:today'), resources/views/admin/todo/personal/calendar.blade.php:8462 (JS-Call)` | S | 🟢 Kernbereich |
| 13 | **Projekte › Termine › Mein Kalender (tasks/calendar/personal)** | 🔄 Workflow | Der Kalender lädt Termindaten über ROUTE.getCalendar = '/get_personal_task_calendar' (hardcodierte URL, kein route()-Aufruf). Die Route personal.get.calendar (web.php:3704) existiert und ist auth-geschützt — kein Routing-Fehler. Jedoch ist personal.get.my.calendar (web.php:3708) ebenfalls auf PersonalTaskController@myCalender gemappt (gibt die komplette HTML-View zurück, nicht JSON-Events). Diese Route ist in keiner View referenziert — toter Alias, der bei Aufruf die falsche Response liefern würde wenn sie versehentlich als Event-Endpunkt genutzt wird. | `routes/web.php:3704 (personal.get.calendar → calendar()), routes/web.php:3708 (personal.get.my.calendar → myCalender() fälschlich), resources/views/admin/todo/personal/calendar.blade.php:4447` | S | 🟢 Kernbereich |
| 14 | **Arbeitsbereich › Einzelseiten › Dashboard** | 🔄 Workflow | EmployeeDashboardController@index (Zeile 60-231) rendert admin.dashboard.employee.mobile — exakt dieselbe View wie die separate mobile()-Methode (Zeile 235-367). Die View (mobile.blade.php, 13.425 Zeilen) ist die vollständige Produktions-Dashboard-View mit Widget-System, Kalender, Abteilungs-Cockpit und Shortcuts. Die Datei dashboard.blade.php (180 Zeilen) enthält dagegen hartcodierte Platzhalterdaten ('14 aktive Leads', 'Angebot WP-2024-08') und kein @extends-Layout — sie ist ein nicht eingebundenes Design-Mockup. Das Benennen der Produktionsview als 'mobile' und des Mockups als 'dashboard' erzeugt dauerhaftes Entwickler-Missverständnis. Kein akuter Laufzeitfehler, da die korrekte View geladen wird — aber nächste Änderung am falschen Dateinamen würde die falsche Datei treffen. | `app/Http/Controllers/EmployeeDashboardController.php:215 (return view mobile), resources/views/admin/dashboard/employee/mobile.blade.php (Produktionsview), resources/views/admin/dashboard/employee/dashboard.blade.php (Mockup mit Hardcode-Daten)` | S | 🟢 Kernbereich |
| 15 | **Projekte › Einzelseiten › Allgemeine Aufgaben** | 🔄 Workflow | Die Claim-Modal-Komponente (claim-modal.blade.php, id='gtClaimModal') ist in den Index eingebunden, wird aber nirgends geöffnet: kein Button/Link ruft gtOpenModal('gtClaimModal') auf, und das Formular (id='gtClaimForm') hat keinen JavaScript-Submit-Handler in der gesamten Codebase (verifiziert via grep über alle admin-Views). Der explizite 'Aufgabe übernehmen'-Workflow (mit Notiz, geplanter Zeit, Fälligkeitsdatum) ist damit vollständig unerreichbar. Die Route general-tasks.claim (POST /general-tasks/{generalTask}/claim) existiert und ist korrekt implementiert, aber kein UI-Pfad führt dazu. Mitigation: Das Drag-and-Drop von 'Offen' nach 'In Bearbeitung' triggert via GeneralTaskController@move() ein implizites Auto-Claim (claimed_by wird gesetzt), sodass der Kernworkflow noch läuft – die Aufgabenübernahme mit Notiz/Zeitangabe ist aber tot. | `resources/views/admin/general_tasks/partials/claim-modal.blade.php:1-39 (Modal ohne Trigger); resources/views/admin/general_tasks/index.blade.php:342 (include ohne Trigger); routes/web.php:3782 (Route existiert); app/Http/Controllers/Task/GeneralTaskController.php:203-241 (claim-Methode implementiert)` | S | 🟢 Kernbereich |
| 16 | **(Workflow-Kette quer) › Zeitplan-Freigabe (TimeManagement)** | 🔄 Workflow | TimeManagementController::submit() (Zeile 182) und updateStatus() (Zeile 210) haben die Rechteprüfungen explizit auskommentiert (TODO-Kommentar). Jeder authentifizierte Nutzer kann beliebige Zeitpläne einreichen und genehmigen/ablehnen — unabhängig von Rolle oder Vorgesetzten-Beziehung. Kein Datenverlust, aber Freigabe-Workflow ohne Zugriffsschutz. | `app/Http/Controllers/Employee/TimeManagement/TimeManagementController.php:16-17 (index), :181-182 (submit), :208-210 (updateStatus)` | M | 🟢 Kernbereich |
| 17 | **(Workflow-Kette quer) › Termine aus Termin-Erstellung → Neue Anfrage immer Unpublished** | 🔄 Workflow | MainAppointmentController::store() erzeugt bei Bedarf eine neue Inquiry mit status='Unpublished' (Zeile 510) und branch_id=$request->branch_id ?? 1 (hartcodierter Fallback). Wird kein branch_id übergeben, landen alle aus Terminen erzeugten Anfragen in Branch 1. Außerdem bleibt die erzeugte Inquiry dauerhaft Unpublished (kein Auto-Publish), sodass sie aus dem Standard-Anfragen-Index herausfällt. | `app/Http/Controllers/Appointment/MainAppointmentController.php:510 (status Unpublished), :514 (branch_id ?? 1 Fallback); ebenso :1359, :1364` | S | 🟢 Kernbereich |
| 18 | **CRM › Partner › Alle Marken / Partner** | 🧭 Routing | Route brand.destroy ist als Route::get('/brand/destroy/{id}') registriert (routes/web.php:2500) und wird in brand.blade.php:809 als einfacher <a href>-Link ohne CSRF-Token aufgerufen. Ein GET-Request kann durch Prefetch-Browser, geloggte URLs oder einen eingebetteten Link in einer externen Seite ohne Benutzeraktion ausgelöst werden und löscht den Datensatz irreversibel ohne CSRF-Schutz. | `routes/web.php:2500 / resources/views/admin/product/brand/brand.blade.php:809` | S | 🟡 mittel |
| 19 | **Support › Tickets › Fehlerkatalog** | 🧭 Routing | Das Legacy-Bootstrap-Modal #deleteModal in error_type.blade.php enthält einen <a href="{{ route('error.destroy', $item->id) }}"> Link. Die Route error.destroy ist als Route::delete registriert (routes/web.php:2109). Ein GET-Request an diese URL liefert 405 Method Not Allowed. Kein sichtbarer Button öffnet dieses Modal (der aktive .delete-btn nutzt AJAX/DELETE korrekt), aber der Modal-Code ist im DOM vorhanden und bricht, falls er aufgerufen wird. | `resources/views/admin/error_type/error_type.blade.php:955 / routes/web.php:2109` | S | 🟡 mittel |
| 20 | **Personal › Mitarbeiter › Arbeitsorte** | 🧭 Routing | Route work.place.index liegt in Route::group(['middleware'=>'web']) ohne 'auth' (routes/web.php:1859). Alle CRUD-Endpunkte (index, store, edit, update, destroy) für /admin/daily_report/work_place sind ohne Login abrufbar. Dazu: store() nimmt $request->all() ohne jede Validierung direkt in DailyReportWorkPlace::create() (Mass-Assignment). Ferner semantischer Bruch: 'Arbeitsorte' im HR-Bereich verweist auf Tagesbericht-Kategorien (branch/warehouse/customer-Konfiguration für DailyReport), nicht auf HR-Stammdaten zu Einsatzorten von Mitarbeitern. | `routes/web.php:1859-1867 (middleware ohne auth); app/Http/Controllers/Report/DailyReportWorkPlaceController.php:27-31 (store ohne Validierung)` | S | 🟡 mittel |
| 21 | **Artikel & Lager › Lager › Übergaben** | 🧭 Routing | handover_details.blade.php Zeile 166: Löschen eines Handover-Eintrags erfolgt per GET-Link (url('/handover_destroy/').$item->id), Route handover.delete ist ebenfalls GET (web.php:2710). CSRF-Schutz greift nicht, Löschung per einfachem Link-Aufruf möglich (Browser-Prefetch, Link-in-E-Mail). Zusätzlich: wenn Assets-Klasse (P1-Befund oben) behoben ist, bleibt dieses Problem bestehen. | `resources/views/admin/product/handover/handover_details.blade.php:166; routes/web.php:2710` | S | 🟡 mittel |
| 22 | **CRM › Kommunikation › Posteingang** | 🔗 Verknüpfung | Im Blade-Template werden in einer @foreach-Schleife über alle E-Mails zwei inline DB::table()-Abfragen pro Zeile ausgeführt: (1) DB::table('leads') für die Empfängerzählung in der Sidebar, (2) DB::table('new_leads') für den Kundenabgleich je E-Mail-Zeile. Bei vielen E-Mails entsteht ein schweres N+1-Muster vollständig im View ohne Controller-Logik — kein Eloquent-Model, keine Paginierung, keine Caching-Möglichkeit. | `resources/views/admin/leads/email/email_view.blade.php:45,109` | M | 🟡 mittel |
| 23 | **Personal › Mitarbeiter › Teams** | 🔗 Verknüpfung | TeamController@create() (Route teams.create, GET /admin/teams/create) gibt view('admin.teams.create') zurück, die Datei resources/views/admin/teams/create.blade.php existiert nicht — InvalidArgumentException beim direkten Aufruf der URL. Ebenso ist TeamController@show() nicht implementiert, obwohl Route teams.show (GET /admin/teams/{team}) via Route::resource registriert ist. Beide Routen sind nicht aus der Navi oder dem Index-View verlinkt (Erstellen erfolgt über Modal in index.blade.php, Show wird nicht genutzt), daher kein akuter Absturz-Pfad. | `app/Http/Controllers/Employee/Profile/TeamController.php:44-48 (create: view missing), kein show()-Method; resources/views/admin/teams/ (nur index.blade.php und edit.blade.php)` | S | 🟡 mittel |
| 24 | **CRM › Partner › Nachunternehmer** | 🔄 Workflow | Das Bearbeiten-Modal in der geteilten brand.blade.php zeigt ein Typ-Dropdown mit allen sieben Typen (brand, architect, sub_contractor, contractor, bank, insurance, other — Z. 44, 888-890). Beim Bearbeiten eines Nachunternehmers kann der Nutzer den Typ auf z.B. 'bank' setzen und speichern: der Datensatz verschwindet aus der sub_contractor-gefilterten Ansicht ohne Warnung oder Bestätigung. Identisches Problem betrifft Architekten, Banken und Versicherungen (alle nutzen dieselbe View). | `resources/views/admin/product/brand/brand.blade.php:44,888 / app/Http/Controllers/Product/Brand/BrandController.php:45-48` | S | 🟡 mittel |
| 25 | **Projekte › Wartung › Wartungs-Checklisten › Duplizieren (data-action=duplicate)** | 🔄 Workflow | Der Menüeintrag 'Duplizieren' (data-action='duplicate', Zeilen 1866 und 2046) ist in beiden Views (Karten- und Tabellenansicht) sichtbar. Ein korrespondierender Controller-Endpunkt (duplicate-Methode oder Route) existiert nicht in MaintenanceChecklistController und nicht in routes/web.php. Der Button ist tot — kein AJAX-Handler, keine Route, kein Fehlerfeedback für den Nutzer. | `resources/views/admin/maintenance_checklists/index.blade.php:1866,2046 (data-action='duplicate'); app/Http/Controllers/Customer/Maintenance/MaintenanceChecklistController.php (keine duplicate-Methode); routes/web.php Zeilen 2166-2179 (kein duplicate-Route für maintenance_checklists)` | M | 🟡 mittel |
| 26 | **Support › Tickets › Neues Ticket** | 🔄 Workflow | Im Ticket-Erstellen-Formular (problem.blade.php:1424) wird der addNewError-Endpunkt mit employee_id: $('#employee_id').val() aufgerufen. Der Wert kommt aus $data['employee_id'] = auth()->user()->name (ProblemController:126) — d.h. users.name wird als employees.id interpretiert (bekannter Designfehler: users.name = employees.id-Hack). Die Validierung 'employee_id' => 'required\|exists:employees,id' schlägt fehl, wenn auth()->user()->name kein numerisch gültiger employees.id-Wert ist, was den Inline-Fehlercode-Anlegen-Workflow bricht. | `app/Http/Controllers/Ticket/ProblemController.php:126 / resources/views/admin/problem/problem.blade.php:1424–1432` | M | 🟡 mittel |
| 27 | **Vertrieb › Aufträge › Rechnungen — falscher Routenname im Sidebar-safeRoute** | 🧭 Routing | sidebar.blade.php ruft $safeRoute('invoices.index', 'admin/invoices') auf. Die tatsächliche Routen-Name lautet admin.invoices.index (Route ist in prefix('admin')->name('admin.') definiert, Z. 4464). Route::has('invoices.index') gibt false zurück, der safeRoute fällt auf url('admin/invoices') zurück — aktuell funktional, aber fragil: sobald sich die URL ändert (z.B. bei Umstellung auf /invoices), bricht der Link lautlos. Bestätigt via php artisan route:list: nur admin.invoices.index existiert. | `resources/views/admin/layouts/sidebar.blade.php:737 + routes/web.php:4464-4465` | S | ⚪ selten |
| 28 | **Personal › HR-Daten › Sprachen** | 🧭 Routing | Das Löschen einer Sprache schlägt mit HTTP 405 Method Not Allowed fehl. Die View emp_lang.blade.php sendet das Löschformular als POST mit @method('DELETE') (Blade-Spoof), die Route language.destroy ist jedoch als Route::get('/language_destroy/{id}', ...) registriert. Laravel's Method-Spoofing konvertiert den POST zu DELETE; der DELETE-Request findet keine GET-Route und Laravel antwortet mit 405. Das Löschen von Spracheinträgen ist damit vollständig defekt. | `resources/views/admin/employee/employee_language/emp_lang.blade.php:927-929 vs. routes/web.php:2828` | S | ⚪ selten |
| 29 | **Personal › HR-Daten › Vertragstypen / Sprachen / Feiertagskalender / Urlaubsanspruch / Länder & Nationalitäten / Steuerklassen** | 🧭 Routing | Fünf Destroy-Aktionen sind als GET-Routen registriert (contract_type_destroy, holiday_destroy, leave_day_destroy, country_destroy, tax_destroy). GET-Requests unterliegen keinem CSRF-Schutz; ein Angreifer mit gültiger ID kann Stammdatensätze durch einen einfachen Link-Aufruf löschen. Für language_destroy existiert eine weitere Inkonsistenz: die View sendet DELETE (Spoof), die Route ist GET – doppelter Fehler. Bereits als allgemeiner Cross-Cutting-Bug #25 in befunde-bestaetigt.csv erfasst; hier zur Vollständigkeit auf den HR-Daten-Bereich eingeschränkt benannt. | `routes/web.php:1618,1695,1706,2828,2837,2844` | M | ⚪ selten |
| 30 | **Personal › Organisation › Abteilungen** | 🧭 Routing | Route /departments/{department}/employees (department.employees, Zeile 2755) ist mit Route-Model-Binding registriert und liegt in web.php VOR der statischen Route /departments/filter-by-branch (departments.filterByBranch, Zeile 2759). Ein GET-Request auf /departments/filter-by-branch matcht daher den Wildcard-Parameter {department}=filter-by-branch, was zu einer ModelNotFoundException führt (Department-Modell mit slug/id=filter-by-branch existiert nicht). Die Route departments.filterByBranch ist damit in der Praxis nie erreichbar. Aktuell ist ROUTE_FILTER_BRANCH in department.blade.php (Zeile 939) zwar als JS-Konstante definiert, aber nirgendwo im JS aufgerufen, weshalb kein direkter Absturz ausgelöst wird — die Konstante selbst wird über route() korrekt aufgelöst (named routes nutzen den Index, nicht URL-Matching). Sobald die Konstante jedoch tatsächlich per AJAX aufgerufen würde, wäre die Funktion tot. | `routes/web.php:2755 und :2759` | S | ⚪ selten |
| 31 | **Personal › Organisation › Organigramm** | 🧭 Routing | GET chart/branches ist zweimal registriert: zuerst für BranchController@chartIndex (Zeile 2763, benannt branches.chart), danach für DepartmentChartController@getBranches (Zeile 2772, anonym). In Laravel gewinnt die zuerst registrierte Route — DepartmentChartController@getBranches ist damit tot und wird nie aufgerufen. Da beide Methoden Published-Branches als {success, branches} zurückgeben, ist der funktionale Unterschied gering. Beim Named-Route-Aufruf route('branches.chart') wird korrekt BranchController@chartIndex verwendet. Fragil wird es, sobald die beiden Methoden divergieren (z.B. BranchController@chartIndex filtert nach status=Published, getBranches ebenfalls — aktuell identisch). | `routes/web.php:2763 und :2772` | S | ⚪ selten |
| 32 | **Personal › Organisation › Abteilungen** | 🧭 Routing | POST /department/position/junction (assign.department.position, Zeile 2744) und POST /department/positions/assign (department.positions.assign, Zeile 2760) zeigen auf exakt dieselbe Methode DepartmentController@assignPosition. department.blade.php (Zeile 943) und organization.blade.php (Zeile 1548) verwenden beide department.positions.assign, die ältere Route assign.department.position ist damit obsolet aber bleibt kompiliert. Nicht direkt brechend, führt aber bei Routing-Audits zu Verwirrung und doppelter Pflege. | `routes/web.php:2744 und :2760` | S | ⚪ selten |
| 33 | **Artikel & Lager › Artikel-Daten › Einheiten** | 🧭 Routing | Die Lösch-Aktion für Maßeinheiten ist als GET-Route registriert (Route::get '/measure_destroy/{id}'). Die View verlinkt per <a href> auf diese Route. GET-Requests unterliegen keinem CSRF-Schutz — ein manipulierter Link in einer E-Mail oder einer externen Seite kann Maßeinheiten ohne Bestätigungs-Token löschen. Bereits in befunde-bestaetigt.csv Bug #27 erfasst, im aktuellen Code noch vorhanden. | `routes/web.php:2483; resources/views/admin/product/product/measure.blade.php:770` | S | ⚪ selten |
| 34 | **Artikel & Lager › Artikel-Daten › Rabattgruppen** | 🧭 Routing | Lösch-Aktion für Rabattgruppen ist als GET-Route registriert (Route::get '/discount_group_destroy/{id}'). View verlinkt per <a href> ohne CSRF-Schutz. Identisches Problem wie bei Einheiten — Datensatz kann durch manipulierten Link gelöscht werden. Bereits in befunde-bestaetigt.csv Bug #27 erfasst, im aktuellen Code noch vorhanden. | `routes/web.php:2852; resources/views/admin/product/discount_group/discount_group.blade.php:729` | S | ⚪ selten |
| 35 | **Finanzen › Finanzen › Ratenzahlungen** | 🧭 Routing | Lösch-Aktion für Ratenzahlungen ist als GET-Route registriert: Route::get('asset_installment_destroy/{id}', ...) (routes/web.php:2662). GET-Requests sind nicht CSRF-geschützt — die Löschaktion kann per manipulierten Link ohne Token ausgelöst werden. Zusätzlich werden Browsing-History, Proxy-Caches und Server-Logs den Lösch-Request loggen. | `routes/web.php:2662` | M | ⚪ selten |
| 36 | **Admin › System › Filialen** | 🧭 Routing | Route branch.destroy (routes/web.php:685) ist als GET registriert. Der Lösch-Link im View ist ein einfacher <a href>-Anker ohne CSRF-Token. Ein authentifizierter Benutzer kann die URL direkt aufrufen oder ein manipulierter Link löscht Filialen ohne Formular-Bestätigung und CSRF-Schutz. Das Muster ist identisch zu den bereits bestätigten GET-Destroy-Befunden (#25, #27 in befunde-bestaetigt.csv). Da Filialen systemkritische Stammdaten sind (Abrechnungsinfos, Bankverbindung), ist das Risiko relevant. | `routes/web.php:685 / resources/views/admin/branch/branch.blade.php:859` | S | ⚪ selten |
| 37 | **System / Sonstiges › Wissensdatenbank** | 🧭 Routing | Route knowledge.base ist unter GET /knowlege (Tippfehler, fehlendes 'd') registriert (routes/web.php:4543). Der Routenname selbst löst korrekt auf, sodass die Sidebar-URL funktioniert — aber direkte Browser-Links oder externe Verweise auf /knowledge landen auf einer 404-Seite. Die gesamte Route-Gruppe (Zeile 4542) nutzt nur ['middleware'=>'web'] ohne 'auth'; der Schutz kommt ausschließlich aus dem Controller-Konstruktor ($this->middleware('auth')), was inkonsistent mit den anderen System-Routen ist. Bereits als N-C6 in nav-bugs-tier-c.md dokumentiert — hier zur Vollständigkeit aufgeführt. | `routes/web.php:4543` | S | ⚪ selten |
| 38 | **Artikel & Lager › Artikel-Daten › Checklisten-Formulare** | 🔗 Verknüpfung | ProductFormulaController::getProduct() (Zeile 241) übergibt compact('formula', 'product_id') an die View, obwohl die Variable $formula nie definiert wurde — nur $data wurde auf Zeile 236 befüllt. Der Aufruf dieser Methode würde einen PHP Fatal Error (Undefined variable $formula) auslösen. Die Methode hat keine direkt registrierte Route im Navi-Bereich, kann aber aus anderen Modulen (Kundencheckliste) aufgerufen werden. Bug N-C4 aus nav-bugs-tier-c.md ist im aktuellen Code noch vorhanden. | `app/Http/Controllers/Product/ProductFormulaController.php:236,241` | S | ⚪ selten |
| 39 | **Finanzen › Finanzen › Ratenzahlungen** | 🔗 Verknüpfung | AssetInstallmentController@show(): Bei aktivem Suchbegriff wird orWhere('assets.id', LIKE, ...) (Zeile 112) ohne where()-Closure außerhalb des WHERE type='asset'-Kontexts angefügt. Die resultierende SQL-Bedingung lautet: WHERE type='asset' AND item LIKE '%s%' OR assets.id LIKE '%s%' — das OR bricht den Typ-Filter auf und liefert auch Maschinen-Zeilen in der Asset-Tabelle (und umgekehrt), was zu falschen Join-Ergebnissen führt (column ambiguity oder falsche Datensätze). | `app/Http/Controllers/Inventory/AssetInstallmentController.php:112` | S | ⚪ selten |
| 40 | **Admin › Benutzer › Mein Profil** | 🔗 Verknüpfung | UserController@index (Route GET /user, Name user.view) übergibt der View nur $data (user_rolls-Join), aber NICHT $employee. user_view.blade.php setzt $profileEmployee = $employee ?? null (Zeile 19) — da $employee nie übergeben wird, bleibt er null. Folge: Anzeigename fällt auf $user->name zurück, das die rohe employee_id (eine Zahl) enthält, statt Vor-/Nachname des Mitarbeiters. Profilbild aus Employee-Tabelle wird ebenfalls nicht geladen. Die View-Kommentar-Zeile 17 dokumentiert dies explizit: 'Controller should pass: $employee = Employee::find(auth()->user()->name);' — diese Zeile ist im Controller aber nicht implementiert. | `app/Http/Controllers/User/UserController.php:83-91 / resources/views/admin/user/user_view.blade.php:17-19` | S | ⚪ selten |
| 41 | **Admin › Benutzer › Eingeschränkte Benutzer** | 🔗 Verknüpfung | limit_user.blade.php prüft Berechtigungen für Nicht-Admin-Nutzer mit $authUserKey = auth()->user()->name (Zeile 6), das die employee_id als String enthält. user_rolls.user_id ist aber ein unsignedBigInteger FK auf users.id (Migration 2023_06_14_131732). Der WHERE-Vergleich user_rolls.user_id = employee_id-String liefert nie einen Treffer. Zusätzlich sucht die Query noch nach is_add = 'on' (Zeile 11), is_update = 'on' (Zeile 17), is_delete = 'on' (Zeile 23) — die Migration 2026_04_25_161053 hat die Spalten bereits auf TINYINT(1) konvertiert und enthält nur noch 0/1. Doppelter Fehler: falscher Join-Key + veraltetes String-Format. Praktische Wirkung: Alle Nicht-Admin-Nutzer verlieren Bearbeitungs-/Lösch-Buttons in der Eingeschränkte-Benutzer-Ansicht, auch wenn sie eigentlich Users-Berechtigung hätten. | `resources/views/admin/user/limit_user.blade.php:6-24 / database/migrations/2023_06_14_131732_create_user_rolls_table.php:16-22 / database/migrations/2026_04_25_161053_fix_user_rolls_columns.php:17-32` | S | ⚪ selten |
| 42 | **Admin › Benutzer › Mein Profil** | 🔗 Verknüpfung | Die Berechtigungstabelle in user_view.blade.php zeigt item_id statt lesbaren Modul-Labels (Zeile 738: {{ $pre->item_id }}). Die $hasPermission-Closure (Zeile 45-47) prüft $value === 'on' — nach der Migration 2026_04_25 speichert user_rolls aber 0/1. Alle Berechtigungs-Checks in der Profilansicht zeigen deshalb immer '×' (kein Recht), auch wenn der Nutzer Rechte hat. Der $data-Join aus UserController@index (Zeile 85-88) joined user_rolls korrekt auf users.id, liefert aber raw DB-Objekte mit is_read als TINYINT 0/1, nicht als String 'on'. | `resources/views/admin/user/user_view.blade.php:45-47 / resources/views/admin/user/user_view.blade.php:738-763 / database/migrations/2026_04_25_161053_fix_user_rolls_columns.php:17-32` | S | ⚪ selten |
| 43 | **System / Sonstiges › Wissensdatenbank** | 🔗 Verknüpfung | Inline-DB-Query im Blade (Zeile 155–159 von resources/views/admin/knowledge/category/index.blade.php) prüft Bearbeiten-/Löschen-Berechtigung via DB::table('user_rolls')->where('user_rolls.user_id', '=', auth()->user()->name). user_rolls.user_id ist unsignedBigInteger FK auf users.id, auth()->user()->name enthält die employee_id als String → Vergleich schlägt immer fehl → Edit- und Delete-Icons werden für keinen Nutzer gerendert → Kategorien können in der Produktion nicht bearbeitet oder gelöscht werden. Unabhängiger Bug vom bereits bekannten URL-Tippfehler (/knowlege, nav-bugs-tier-c N-C6). | `resources/views/admin/knowledge/category/index.blade.php:155` | S | ⚪ selten |
| 44 | **Finanzen › Finanzen › Ratenzahlungen** | 🔄 Workflow | installment.blade.php lädt html5-qrcode-Bibliothek (Zeile 98) und instanziiert Html5QrcodeScanner auf Element-ID 'reader' (Zeile 115–119). Kein <div id='reader'> existiert in installment.blade.php, installment_car.blade.php oder installment_asset.blade.php. Html5QrcodeScanner wirft beim Laden der Seite einen JS-TypeError ('No element found with id=reader'), was die komplette Script-Ausführung des @section('script')-Blocks beendet — Select2-Initialisierung danach schlägt ebenfalls fehl. | `resources/views/admin/product/assets/installment/installment.blade.php:115–119` | S | ⚪ selten |

## ⚪ P4 — NIEDRIG (selten genutzt / kosmetisch)

| # | Navi-Punkt | Klasse | Was bricht | Datei:Zeile | Aufw. | Nutzung |
|--:|---|---|---|---|:--:|---|
| 1 | **CRM › Leads / Kunden › Leads / Kunden (MAIN)** | 🧭 Routing | Der MAIN-Header 'Leads / Kunden' hat url='' (kein Klickziel). Ist kein klikbarer Link, kein Dashboard, keine Weiterleitung. Für Nutzer die auf den Bereichstitel klicken passiert nichts. Gleiches Muster in anderen MAIN-Headern der Navi (bekannte Eigenschaft des Navigationssystems). | `groups.json[2].items[0] url:''; navigation-konzept-audit.md:53-54` | S | 🟢 Kernbereich |
| 2 | **Projekte › Notizen › Notizen (MAIN-Header)** | 🧭 Routing | URL ist leer — kein Klickziel, kein Redirect. Nutzer, die auf den Abschnittskopf klicken, landen nirgends. Keine eigene Übersichtsseite. | `/private/tmp/claude-501/-Users-yamanuri-Documents-ticket/7882c2f8-f3e2-419f-b0bb-45d48fd04373/scratchpad/groups.json:groups[6].items[0].url=""` | S | 🟢 Kernbereich |
| 3 | **Projekte › Einzelseiten › Projektplanung** | 🧭 Routing | Die Sidebar-URL $safeRoute('planner.index', '/planner') resolved zu /planner. Diese Route (GET planner/) leitet sofort per redirect()->route('planner.projects') auf /planner/projects weiter (routes/web.php:5153-5155). Die active_routes im Sidebar-Eintrag enthält nur '/planner/projects', nicht '/planner'. Beim Klick auf den Menüpunkt besteht für einen Bruchteil einer Sekunde kein aktiver Navigationszustand (während der 302-Redirect läuft). In der Praxis kein sichtbarer Defekt für Nutzer. Alle Routen, die die View referenziert (planner.projects.data, .kanban, .store, .ensure_plan, .move, .history, .profile, .profile.data, .team.save, .team.member, .montage_work, .work_items.store) existieren und sind korrekt verknüpft. | `resources/views/admin/layouts/sidebar.blade.php:779-786; routes/web.php:5153-5155 (planner.index Redirect); resources/views/admin/planner/projects.blade.php:1937-1946` | S | 🟢 Kernbereich |
| 4 | **CRM › Kommunikation › Kommunikation** | 🧭 Routing | Der MAIN-Header 'Kommunikation' hat eine leere URL und ist damit nicht klickbar. Es gibt keine Landing-Page oder Übersichtsseite für den Bereich. Nutzer können nur durch die Untermenüpunkte navigieren. | `/private/tmp/claude-501/-Users-yamanuri-Documents-ticket/7882c2f8-f3e2-419f-b0bb-45d48fd04373/scratchpad/groups.json:1 (items[0].url = '')` | S | 🟡 mittel |
| 5 | **Vertrieb › Angebote › Angebote (MAIN-Header)** | 🧭 Routing | URL ist leer — der MAIN-Header ist nicht klickbar. Konsistent mit allen anderen MAIN-Headern der App, die ebenfalls url:'' verwenden. Kein Absturz, kein Datenverlust; Nutzer ohne Kenntnis der Untermenüs landen nirgends. Gilt für diesen Bereich wie für alle anderen Sektionsheader (z.B. Aufträge, Notizen, Termine). | `scratchpad/groups.json[4].items[0] url:''` | S | 🟡 mittel |
| 6 | **Projekte › Wartung › Wartung (MAIN-Header)** | 🧭 Routing | MAIN-Eintrag 'Wartung' hat leere URL (url=''). Kein eigenes Klick-Ziel, kein Dashboard-Einstieg für den Bereich. Reines Navigations-Label ohne Funktion — Nutzer ohne Kenntnisse der Untermenüs findet keinen Einstieg. | `resources/views/admin/layouts/sidebar.blade.php:849-868 (Wartung-Gruppe, kein url-Wert am MAIN-Eintrag); groups.json[8].items[0]: url=''` | S | 🟡 mittel |
| 7 | **Support › Tickets › Tickets (MAIN-Header)** | 🧭 Routing | Der MAIN-Header 'Tickets' (groups.json[9].items[0]) hat url: '' — kein Klickziel, kein eigenes Dashboard. Folgt dem App-weiten Muster für nicht-klickbare Gruppenheader, ist aber funktional tot. | `scratchpad/groups.json[9].items[0]` | S | 🟡 mittel |
| 8 | **Personal › Mitarbeiter › Mitarbeiter (MAIN-Header)** | 🧭 Routing | MAIN-Header hat url: '' — kein Klick-Ziel, kein Dashboard-Einstieg für den Bereich. Gleiche Situation wie Artikel, Aufträge, Lager. Nutzer ohne Kenntnis der Untermenüs kann nicht direkt in den Bereich navigieren. | `resources/views/admin/layouts/sidebar.blade.php:918 (groups.json[10].items[0].url = '')` | S | 🟡 mittel |
| 9 | **Artikel & Lager › Artikel › Artikel (MAIN-Header)** | 🧭 Routing | Der MAIN-Header-Eintrag hat url='' — kein Klickziel, keine Landing-Page, kein Redirect. Nutzer ohne Kenntniss der Untermenüs landen nirgends. Kein Absturz, reines Dead-Link-Problem. | `groups.json[13].items[0]: {"label": "Artikel", "depth": "MAIN", "url": ""}` | S | 🟡 mittel |
| 10 | **Artikel & Lager › Lager › Lager (MAIN-Header)** | 🧭 Routing | Der MAIN-Header 'Lager' hat url='' — kein Klickziel, keine Landing-Page. Reiner Navigations-Label, kein Dashboard oder Übersicht für den Gesamtbereich Lager. Nutzer ohne Kenntnis der Untermenüs erhalten kein Orientierungsziel. | `scratchpad/groups.json Index 15, items[0]: {"label": "Lager", "depth": "MAIN", "url": ""}` | S | 🟡 mittel |
| 11 | **Personal › Organisation › Organisation (MAIN-Header)** | 🧭 Routing | Der MAIN-Eintrag Organisation hat url='' (leere Zeichenkette). Er ist als toter Header ohne klickbaren Link implementiert. Für Nutzer ist nicht erkennbar, dass der Eintrag nicht anklickbar ist — kein sichtbarer Unterschied zu aktiven Einträgen. Kein Absturz, aber tote Navigation. | `scratchpad/groups.json index 12, item 0: url=''` | S | ⚪ selten |
| 12 | **Artikel & Lager › Artikel-Daten › Artikel-Daten (MAIN-Header)** | 🧭 Routing | Der MAIN-Header 'Artikel-Daten' hat eine leere URL (url: '') und ist damit nicht klickbar. Er dient nur als Navigations-Label ohne eigenes Ziel. Kein Absturz, aber kein Einstiegspunkt für den Bereich. | `scratchpad/groups.json[14].items[0] url:''` | S | ⚪ selten |
| 13 | **Finanzen › Finanzen › Finanzen (MAIN-Header)** | 🧭 Routing | Leerer URL-Wert — reiner Navigations-Label ohne eigene Route oder Landing-Page. Kein Klick-Ziel, kein Bereichs-Dashboard. Konsistentes Muster aller MAIN-Header in der App, kein Absturz. | `groups.json[16].items[0]: url: ''` | S | ⚪ selten |
| 14 | **Admin › Benutzer › Benutzer (MAIN-Header)** | 🧭 Routing | Der MAIN-Header 'Benutzer' hat url='' (groups.json Index 17, Item 0). Kein Klickziel, keine Landing-Page. Navigatorisch tot — nur als Gruppen-Label nützlich. Bei niedrigem Nutzungsgewicht des Bereichs kein akuter Schaden. | `/private/tmp/claude-501/-Users-yamanuri-Documents-ticket/7882c2f8-f3e2-419f-b0bb-45d48fd04373/scratchpad/groups.json:17.items[0].url (leer)` | S | ⚪ selten |
| 15 | **Vertrieb › Aufträge › Rechnungen (Canvas-Hinweis) — Doppelter Menüpunkt ohne Mehrwert** | 🔄 Workflow | Der Menüpunkt 'Rechnungen (Canvas-Hinweis)' führt auf dieselbe URL wie 'Rechnungen' plus ?open_canvas=1. Der Handler in der View (resources/views/admin/invoices/index.blade.php:4009-4013) öffnet KEIN Canvas automatisch, sondern zeigt nur einen alert()-Dialog: 'Klicke bei einer Rechnung auf das lila Canvas-Symbol.' Der Nutzer landet auf identischer Seite ohne erkennbaren Unterschied — der Menüpunkt ist funktionslos und verwirrend. | `resources/views/admin/layouts/sidebar.blade.php:744-749 + resources/views/admin/invoices/index.blade.php:4009-4013` | S | ⚪ selten |
| 16 | **Finanzen › Finanzen › Ratenzahlungen** | 🔄 Workflow | In store() und update() wird bei Fehler (else-Zweig) dieselbe Erfolgsmeldung wie im Erfolgsfall zurückgegeben: 'Der Datensatz wurd erfulgreich gespeichert!' (Zeilen 94, 172). Nutzer erhält keine Fehlermeldung, Tippfehler 'erfulgreich' statt 'erfolgreich' in beiden Zweigen. Kein Datenverlust, aber irreführendes Feedback. | `app/Http/Controllers/Inventory/AssetInstallmentController.php:94,172` | S | ⚪ selten |

---
**Aufwand-Legende:** S = klein · M = mittel · L = groß. **Empfehlung:** Routing-P1 zuerst (meist kleiner Fix, große Wirkung: ein Routenname/Namespace), dann gebrochene Workflow-Ketten in den Kernbereichen, dann Verknüpfungs-Fragilität.