# ticket — Stabilitäts-Fixliste (priorisiert)

**Stand:** 2026-06-28 · **Methode:** Agententeam, 1 Agent je Navigationsbereich (24 Bereiche), gezielt auf Stabilität (Crash / Datenverlust / Performance / JS-Blocker), am **aktuellen** Code verifiziert; bestehende Audits als Basis. **Reine Analyse — kein Code geändert.**

> Kontext: produktiv, 40 Nutzer, 3000 Kunden. Reihenfolge nach Schwere; innerhalb gleicher Stufe zuerst die **genutzten Kernbereiche** (Leads, Kalender, Berichte, Projektplanung, Kunden).

## Überblick
- **67 Stabilitäts-Befunde** in 24 Bereichen.
- **Schwere:** 🔴 P1 12 · 🟠 P2 23 · 🟡 P3 29 · ⚪ P4 3
- **Art:** 💥 Crash 24 · 🗄️ Datenverlust 8 · 🐢 Performance 28 · 🔌 JS 7
- **Aufwand gesamt:** klein 44 · mittel 20 · groß 3

## 🔴 P1 — KRITISCH (stürzt ab / verliert Daten / blockiert Kernfunktion)

| # | Navi-Punkt | Art | Was bricht | Datei:Zeile | Aufw. | Nutzung |
|--:|---|---|---|---|:--:|---|
| 1 | **Berichte › Einzelseiten › Tagesberichte** | 💥 Crash | DailyReportController::isAdmin() vergleicht user_rolls.user_id (unsignedBigInteger FK auf users.id) mit (int) auth()->user()->name (= employee_id). Der Join trifft nie → isAdmin() liefert immer false. In daily_report() erhalten Admins dadurch nur ihre eigene Zeile statt alle Mitarbeiter; in EmployeeListSearch() greift ->when(!$isAdmin, fn($q) => $q->where('id', $authId)) für alle Admins und sperrt die Gesamtübersicht vollständig. Kernfunktion 'Admin sieht alle Tagesberichte' ist dauerhaft defekt. | `app/Http/Controllers/Report/DailyReportController.php:1651-1656 (isAdmin), :1688 (daily_report), :1338 (EmployeeListSearch)` | S | 🟢 Kernbereich |
| 2 | **CRM › Anfragen › Veröffentlichte Anfragen / Anfrage-Profil** | 💥 Crash | POST /inquiry/{id}/reverify → InquiryController@reverify — Methode existiert nicht. Jeder Klick auf 'Neu verifizieren' in published_list.blade.php (Z.1471) und contact_profile.blade.php (Z.1572) löst einen 500-Fehler aus. Die Re-Verifizierungs-Funktion ist komplett blockiert. | `routes/web.php:4085 / app/Http/Controllers/Inquiry/InquiryController.php (Methode fehlt)` | M | 🟢 Kernbereich |
| 3 | **CRM › Anfragen › Anfrage-Profil (erreichbar aus allen Listenansichten)** | 💥 Crash | POST /inquiry/{id}/status → InquiryController@updateStatus — Methode existiert nicht. Das Kanban-Board in contact_profile.blade.php ruft updateStatus() beim Drag&Drop auf jede Spalte außer 'verified' auf (Z.1425, 1433–1444). Jede Statusänderung per Drag (Unpublished/progress/junk/Draft) endet mit einem 500-Fehler; der Kanban-Workflow ist vollständig defekt. | `routes/web.php:4021 / resources/views/admin/inquiry/contact_profile.blade.php:1425,1433 / app/Http/Controllers/Inquiry/InquiryController.php (Methode fehlt)` | M | 🟢 Kernbereich |
| 4 | **CRM › Anfragen › Meine Anfragen / Kundenanfragen / Alle Anfragen (Bearbeiten-Funktion)** | 🗄️ Datenverlust | InquiryController@update() setzt $inquiry->status = 'Unpublished' bedingungslos (Z.1360). Dasselbe in store() (Z.1072) und finalizeDraft() (Z.2519). Eine bereits verifizierte oder published Anfrage verliert beim nächsten Bearbeitungsvorgang ihren Verifizierungsstatus dauerhaft und wird in den nicht-verifizierten Zustand zurückgestuft — ohne Warnung, ohne Wiederherstellungsmöglichkeit aus dem Bearbeitungsformular. | `app/Http/Controllers/Inquiry/InquiryController.php:1360 (update), 1072 (store), 2519 (finalizeDraft)` | S | 🟢 Kernbereich |
| 5 | **Projekte › Notizen › Persönliche Notizen** | 🔌 JS-Blocker | ReferenceError beim ersten Modal-Öffnen: openNoteModal() (äußerer Scope, Zeile 1071) und loadCategories() (äußerer Scope, Zeile 1493) rufen initNoteSelect2() auf, die aber nur als lokale Funktion innerhalb $(document).ready() definiert ist (Zeile 1506). Sobald der Nutzer auf 'Neue Notiz' klickt, wirft der Browser ReferenceError: initNoteSelect2 is not defined — das Select2-Kategorie-Dropdown initialisiert sich nicht und bleibt eine leere Box. Kategorieauswahl beim Erstellen und Bearbeiten von Notizen ist vollständig blockiert. | `resources/views/admin/notes/notes/note.blade.php:1079,1493,1506` | S | 🟢 Kernbereich |
| 6 | **Artikel & Lager › Artikel › Artikel & Lager › Artikel › Favoriten** | 💥 Crash | Favoriten-Listen können nicht umbenannt, umgefärbt oder gelöscht werden: ProductFavoriteListController::authorizeOwner() liest Auth::user()->employee_id (Zeile 275), eine Spalte die in der users-Tabelle nicht existiert (Migration definiert nur id, name, email …). Der Ausdruck ergibt immer null. Da $list->employee_id immer einen gespeicherten Wert trägt, ist null !== $list->employee_id stets true → abort(403) bei jedem PUT (update) und DELETE (destroy). Alle Nutzer sind dauerhaft ausgesperrt. | `app/Http/Controllers/Product/ProductFavoriteListController.php:275 (authorizeOwner), aufgerufen von update():143 und destroy():161 — routes/web.php:2433-2434` | S | 🟡 mittel |
| 7 | **Artikel & Lager › Lager › Lagerausgaben** | 🗄️ Datenverlust | store() reduziert Inventarbestand sofort bei Anlegen einer Anfrage (status='Unpublished'). destroy() löscht den Datensatz OHNE Bestand zurückzuschreiben. Jede gelöschte Lagerausgabe-Anfrage erzeugt permanenten Inventarschwund ohne Gegenbuchung. | `app/Http/Controllers/Inventory/InventoryRequestOutController.php:256 (Bestand-Abzug in store()), :305-325 (destroy() ohne Inventory-Restore)` | S | 🟡 mittel |
| 8 | **CRM › Kommunikation › E-Mail-Konten** | 💥 Crash | Seite /email_configuration stürzt beim Laden ab: View ruft action('App\Http\Controllers\EmailConfigurationController@store') und action(...@update) auf — Klasse existiert nicht (richtig: App\Http\Controllers\Email\EmailConfigurationController). Laravel 11 wirft InvalidArgumentException beim View-Rendering. Formulare zum Anlegen und Bearbeiten von E-Mail-Konten sind damit vollständig blockiert. | `resources/views/admin/leads/configuration/configuration.blade.php:26,67,283` | S | 🟡 mittel |
| 9 | **CRM › Partner › Alle Kontakte** | 💥 Crash | Restore-Aktion für Hersteller (global-restore/brand) und Lieferanten (global-restore/distributor) wirft BadMethodCallException: Brand::withTrashed() und Distributor::withTrashed() werden aufgerufen, aber weder Brand noch Distributor nutzen den SoftDeletes-Trait, und die Tabellen brands/distributors haben keine deleted_at-Spalte (verifiziert in Migrationen 2023_06_21_101726 und 2023_07_26_082336). Jeder Klick auf Wiederherstellen für diese Typen führt zu einem 500-Fehler. | `app/Http/Controllers/Contacts/AllContactController.php:993-1003 / app/Models/Brand.php (kein SoftDeletes) / app/Models/Distributor.php (kein SoftDeletes)` | S | 🟡 mittel |
| 10 | **CRM › Partner › Externe Firmen** | 💥 Crash | Das 'Neue hinzufügen'-Formular postet an route('external.update') statt route('external.store') (resources/views/admin/employee/external/external.blade.php:63). ExternalPersonalController@update ruft ExternalPersonal::find($request->id)->update(...) auf; im Neuanlage-Formular ist kein hidden id-Feld vorhanden, daher ist $request->id null → find(null) gibt null zurück → Methodenaufruf auf null erzeugt PHP Fatal Error. Neuanlage externer Firmen ist komplett blockiert. | `resources/views/admin/employee/external/external.blade.php:63 / app/Http/Controllers/Product/Brand/ExternalPersonalController.php:118` | S | 🟡 mittel |
| 11 | **Personal › Mitarbeiter › Krankheit & Urlaub** | 🗄️ Datenverlust | Admin-Pfad zum Genehmigen von Urlaub (GET /leave_approve/{id} → LeaveController@approve) setzt leave.approved='Yes' und status='accept', dekrementiert aber employees.remaining_day NICHT. Nur der Employee-Self-Service-Pfad (save(), Zeile 1143–1146) zieht die Tage ab. Legt ein Admin ein Leave via store() an und genehmigt es sofort via approve(), werden keine Urlaubstage verbucht — der Mitarbeiter kann unbegrenzt Admin-genehmigte Urlaube akkumulieren ohne Resttage-Verbrauch. | `app/Http/Controllers/Employee/Profile/LeaveController.php:230-259 (approve), 337-367 (store ohne Decrement), 1143-1146 (save mit Decrement)` | S | 🟡 mittel |
| 12 | **Projekte › Wartung › Wartungs-Checklisten** | 🔌 JS-Blocker | Die gesamte Interaktionsschicht der Wartungs-Checklisten-Seite fehlt vollständig: kein einziger JavaScript-Event-Listener existiert in der View oder in einer externen JS-Datei für die mcl-*-UI-Komponenten. Klicks auf 'Neue Checkliste' (#mcl-btn-create), Edit-, Archiv-, Papierkorb-, Wiederherstellungs-, Duplikat- und Vorschau-Buttons lösen nichts aus — alle Aktionsbuttons sind tot. Zusätzlich ist der '#new-checklist'-Hash aus der Navi-URL (admin.maintenance_checklists.index#new-checklist) ohne Handler: die Seite lädt, aber keine Checkliste kann erstellt, bearbeitet oder gelöscht werden. Das Modul ist vollständig funktionslos. | `resources/views/admin/maintenance_checklists/index.blade.php: 0 addEventListener/jQuery-on-Aufrufe im gesamten File (grep-bestätigt). Button #mcl-btn-create Z.1602 ohne Handler. data-action='edit/archive/trash/restore/duplicate/preview/pdf' Z.1832-2069 ohne JS. @push('scripts') Z.2277-2298 enthält nur Breadcrumb-Code. Kein mcl-*-Handler in resources/js/, public/js/ oder Layout (grep ohne Treffer).` | L | 🟡 mittel |

## 🟠 P2 — HOCH (Fehler in genutzter Funktion, kein Datenverlust)

| # | Navi-Punkt | Art | Was bricht | Datei:Zeile | Aufw. | Nutzung |
|--:|---|---|---|---|:--:|---|
| 1 | **Arbeitsbereich › Einzelseiten › Dashboard** | 💥 Crash | loadTabContent() ruft $this->getProjects() (Zeile 421) und $this->getOffers() (Zeile 423) — beide Methoden existieren nicht im Controller. Wer /dashboard/load-tab?tab=projects oder ?tab=offers aufruft, bekommt BadMethodCallException. Route ist registriert und öffentlich via URL erreichbar (kein Schutz dagegen), aber die aktive View (mobile.blade.php) ruft diesen Endpunkt nicht — toter Code-Pfad, jedoch nicht entfernt. | `app/Http/Controllers/EmployeeDashboardController.php:421-423` | M | 🟢 Kernbereich |
| 2 | **Arbeitsbereich › Einzelseiten › Dashboard** | 💥 Crash | getTabCounts() ruft Task::count() und Appointment::count() auf (Zeilen 683-684). Weder App\Models\Task noch App\Models\Appointment sind importiert; App\Models\Task existiert im Projekt nicht. Jeder Aufruf von GET /dashboard/tab-counts endet mit 'Class not found'-Fatal. Endpunkt registriert aber nicht aus aktiver View aufgerufen — toter Code-Pfad. | `app/Http/Controllers/EmployeeDashboardController.php:683-684` | S | 🟢 Kernbereich |
| 3 | **Arbeitsbereich › Einzelseiten › Dashboard** | 💥 Crash | getMyData() versucht view('admin.dashboard.employee.partials.my_customers_modal') zu rendern (Zeile 1850). Diese View existiert nur unter Old/partials/my_customers_modal.blade.php — im aktiven partials/-Verzeichnis fehlt sie. Ebenso fehlt my_projects_modal (Zeile 1897). Aufruf von GET /employee/my-dashboard-data?type=customers oder ?type=projects → InvalidArgumentException (View not found). Route registriert, aber aus aktiver View nicht aufgerufen — toter Code-Pfad. | `app/Http/Controllers/EmployeeDashboardController.php:1850,1897 / resources/views/admin/dashboard/employee/Old/partials/my_customers_modal.blade.php` | M | 🟢 Kernbereich |
| 4 | **Berichte › Einzelseiten › Berichts-Übersicht** | 🐢 Performance | markAllReportNotificationsRead() holt ALLE overdue_reports IDs ohne Limit (kein whereIn-Limit, kein Datum-Filter) und führt dann pro ID ein OverdueReportRead::updateOrCreate() in einer foreach-Schleife aus — kein DB::transaction, keine Batch-Upsert. Bei wachsendem Berichtsbestand (täglich neue Einträge) verursacht jeder Klick auf 'Alle als gelesen markieren' N+1-Queries und läuft in ein PHP/DB-Timeout. | `app/Http/Controllers/Report/OverdueCenterController.php:607-629 (markAllReportNotificationsRead)` | S | 🟢 Kernbereich |
| 5 | **Berichte › Einzelseiten › Tagesberichte** | 🐢 Performance | EmployeeListSearch() lädt alle aktiven Mitarbeiter in einer Schleife und führt pro Mitarbeiter 10 separate DB-Abfragen aus (Tasks, Termine, Projekte, Angebote, Tickets, Urlaube-Jahressumme, Urlaubtag-Exists, Kranktage-Summe, Krank-Exists, Recurring-Leaves). Bei 40 Mitarbeitern entstehen ~400 Queries pro AJAX-Request. Die Tagesberichte-Seite ruft diesen Endpunkt direkt beim Laden und bei jeder Datumsänderung auf — spürbare Hänger bis Timeout. | `app/Http/Controllers/Report/DailyReportController.php:1301-1646 (EmployeeListSearch, foreach ab Z.1343); resources/views/admin/daily_report/list/list.blade.php:1444` | M | 🟢 Kernbereich |
| 6 | **CRM › Anfragen › Neue Anfrage / Alle Listenansichten (Routen-Register)** | 💥 Crash | Zwei Routen verweisen auf nicht existierende InquiryController-Methoden: (1) GET /inquiry_publish/{id} → @publish (Methode fehlt, 500-Fehler); (2) POST /admin/inquiries/ai-save → @storeFromAI (Methode fehlt, 500-Fehler). Der AI-Save-Endpoint wird aktiv aus resources/views/admin/lead_email/inbox/index.blade.php:1098 per AJAX aufgerufen. Zusätzlich: GET /admin/fusion-forms/import → FusionFormSubmissionController@importFromGoneo und POST /fusion/import/one → @importFusionEntryToInquiry fehlen, sind aber im aktiven fusion/index.blade.php nicht direkt referenziert. | `routes/web.php:4011,4045 / app/Http/Controllers/Inquiry/InquiryController.php (Methoden fehlen) / resources/views/admin/lead_email/inbox/index.blade.php:1098` | M | 🟢 Kernbereich |
| 7 | **Projekte › Notizen › Persönliche Notizen** | 🔌 JS-Blocker | Toter Button '+' (Klasse add_category) im Notiz-Erstellungs-Modal (Zeile 884) hat keinen JS-Event-Handler im gesamten View. Ein Klick darauf hat keinerlei Wirkung — der Nutzer kann keine Kategorie direkt aus dem Notiz-Modal heraus anlegen. Betrifft jeden, der beim Erstellen einer Notiz eine neue Kategorie anlegen will. | `resources/views/admin/notes/notes/note.blade.php:884` | S | 🟢 Kernbereich |
| 8 | **Projekte › Termine › Mein Kalender** | 🐢 Performance | myCalender() feuert 2 DB-Queries pro ProductPosition-Eintrag beim Seitenaufbau: ProductPosition@getPositionNamesAttribute() ruft Position::whereIn()->pluck() zweimal auf (einmal für internal, einmal für external) — unkompensiertes N+1 ohne Cache. Bei z.B. 20 ProductPositions = 40+ Zusatz-Queries bei jedem Öffnen von 'Mein Kalender'. | `app/Models/ProductPosition.php:105-122 (getPositionNamesAttribute); aufgerufen aus app/Http/Controllers/Task/PersonalTaskController.php:2282 ($pp->position_names im foreach über $productPositions)` | M | 🟢 Kernbereich |
| 9 | **Projekte › Termine › Mein Kalender** | 🐢 Performance | myCalender() lädt die gesamte Tabelle main_appointment_employees per JOIN auf employees ohne jeden Filter (kein appointment_id-Bereich, kein employee-Filter, kein Datum) komplett in PHP-Memory. Wächst linear mit Terminen × Mitarbeitern. Dieser $data['task_employee']-Block wird in der View admin.todo.personal.calendar NICHT genutzt — toter, aber teurer Query bei jedem Seitenaufruf. | `app/Http/Controllers/Task/PersonalTaskController.php:2079-2090 (MainAppointmentEmployee::query()->join('employees')->get() ohne Filter); Nutzung in View: 0 Treffer (grep task_employee resources/views/admin/todo/personal/calendar.blade.php ergibt leer)` | S | 🟢 Kernbereich |
| 10 | **Projekte › Termine › Terminübersicht** | 🐢 Performance | CustomerMainAppointmentController@data() führt ->get() ohne Pagination durch. Es gibt keinen Default-Datumsfilter — bei owner_scope=all (Admin-Ansicht) werden ALLE Termine der Tabelle geladen. Bei 40 Mitarbeitern und laufendem Betrieb akkumulieren sich tausende Einträge; jeder loadAppointments()-Aufruf (triggerrt bei Tab-Wechsel, Suche, Filter-Änderung) holt alles in memory und serialisiert zu JSON. | `app/Http/Controllers/Appointment/CustomerMainAppointmentController.php:281 ($query->get() nach switch/case owner_scope ohne Datum-Default); View-Aufruf: resources/views/admin/appointments/index.blade.php:2246+2960 (loadAppointments() via route customer.appointments.data)` | M | 🟢 Kernbereich |
| 11 | **Artikel & Lager › Artikel › Artikel & Lager › Artikel › GC Online / IDS** | 💥 Crash | Route POST /ids/search/inline-forward ist auf IdsSearchController@forwardToShopInline gebunden (routes/web.php:504), diese Methode existiert nicht im Controller (vorhandene Methoden: form, forwardToShop, back, forms, inlineBack). Jeder POST auf diesen Endpunkt endet in einem PHP-Fatal 'Action … not defined'. Der Nav-Einstieg ids.search.form selbst lädt korrekt; der fehlende Endpunkt ist in der Auth-Gruppe registriert und wird aus anderen Kontexten (eingebettete IDS-Suche in Angeboten) erreichbar. | `routes/web.php:504 — app/Http/Controllers/Product/IDS/gconline/IdsSearchController.php (Methode fehlt vollständig)` | M | 🟡 mittel |
| 12 | **Artikel & Lager › Lager › Übergaben** | 🔌 JS-Blocker | Edit-Button öffnet Modal mit vollständig leerem Body (kein Formular, keine Felder). Die gesamte Bearbeitungsfunktion für Übergaben ist dadurch blockiert — es existiert auch keine alternative Route für das Update, die von der View aufgerufen wird. | `resources/views/admin/product/handover/handover_details.blade.php:176-194 (leerer modal-body Zeilen 189-191)` | M | 🟡 mittel |
| 13 | **CRM › Kommunikation › Posteingang** | 💥 Crash | Der 'Gesendet'-Link (url('send_email_view')) im Sidebar der View führt zu HTTP 404, da kein GET-Route /send_email_view registriert ist. Nur ein POST-Route /lead_send_email_view (name: lead.email.send) existiert. Klick auf 'Sent' bricht die Navigation dauerhaft ab. | `resources/views/admin/leads/email/email_view.blade.php:57 / routes/web.php:3254 (kein GET für send_email_view)` | S | 🟡 mittel |
| 14 | **CRM › Partner › Alle Kontakte** | 🐢 Performance | getAllContacts() lädt alle Datensätze aus 5 Tabellen (new_leads ~3000 Zeilen, inquiries, employees, brand_departments, distributor_departments) per ungegrenztem .get() in den PHP-Speicher, sortiert und paginiert dann in PHP. Jeder Seitenaufruf von 'Alle Kontakte' transferiert alle 3000+ Kundendatensätze plus alle weiteren Einträge ohne Limit in den Arbeitsspeicher des Web-Prozesses. Bei 40 gleichzeitigen Nutzern kann das zu Memory-Exhaustion und Timeouts führen. | `app/Http/Controllers/Contacts/AllContactController.php:61-65 (queryCustomers/queryBrands/queryDistributors/queryInquiries/queryEmployees je ohne limit vor .get())` | M | 🟡 mittel |
| 15 | **Vertrieb › Angebote › Übersicht (admin/offers)** | 🐢 Performance | OfferController@data lädt alle Angebote ohne Pagination (->latest()->get()) und ruft dann appendOfferIndexTotalDataToOffers() auf, die in einer Schleife pro Angebot resolveOfferIndexDetailPayload() aufruft — mindestens 2 DB-Queries pro Datensatz (N+1). Zusätzlich läuft syncOfferLeadProducts() synchron im selben Request (eine Offer::withTrashed()->exists()-Abfrage + ggf. INSERT/UPDATE pro lead_product_list-Zeile mit status=offer). Bei wachsender Datenmenge (viele Angebote) führt das zu Timeouts und blockierten Seitenaufrufen der meistgenutzten Ansicht dieses Bereichs. | `app/Http/Controllers/Customer/Offer/OfferController.php:581-593 (->latest()->get() ohne Pagination), :743-764 (N+1 in appendOfferIndexTotalDataToOffers), :294-403 (syncOfferLeadProducts mit N+1 pro lead_product_list-Zeile)` | L | 🟡 mittel |
| 16 | **Vertrieb › Angebote › Vorlagen (offer-templates)** | 💥 Crash | OfferTemplatePickerController@searchProducts filtert mit ->where('name', 'like', ...) und ->orWhere('title', 'like', ...) auf der article_groups-Tabelle. Diese Tabelle hat weder eine 'name'- noch eine 'title'-Spalte (einzige Textspalte: 'article_group'). MySQL wirft 'Unknown column' beim DB-Aufruf, sobald ein Nutzer im Gewerk/Produkt-Suchfeld der Vorlage-Auswahl irgendetwas eintippt. Solange das Feld leer bleibt, trifft der when()-Block nicht zu und kein Fehler. | `app/Http/Controllers/Customer/Offer/OfferTemplatePickerController.php:212-228 (searchProducts: ->where('name',...)->orWhere('title',...) auf article_groups; Migrationsprüfung: database/migrations/2023_06_22_085600_create_article_groups_table.php — nur Spalte 'article_group')` | S | 🟡 mittel |
| 17 | **Vertrieb › Angebote › Vorlagen (offer-templates)** | 💥 Crash | OfferTemplatePickerController@useTemplate gibt nach erfolgreichem Erstellen von Angebot/Ordner eine redirect_url zurück, die url('/wizard') verwendet. url('/wizard') erzeugt APP_URL/wizard — diese Route existiert nicht (die Route ist offers/wizard). Der JS-Client, der dieser URL folgt, landet auf einem 404. Das Kernszenario — Vorlage anwenden und direkt in den Angebotseditor springen — ist damit funktional blockiert. | `app/Http/Controllers/Customer/Offer/OfferTemplatePickerController.php:413 (url('/wizard') statt route('offers.wizard')); routes/web.php:3259 (Route ist unter prefix 'offers' registriert); routes.txt:1753 (offers/wizard, nicht /wizard)` | S | 🟡 mittel |
| 18 | **Admin › Benutzer › Admin › Benutzer › Eingeschränkte Benutzer** | 💥 Crash | Alle Aktionsbuttons (Hinzufügen/Bearbeiten/Löschen) für Nicht-Admins werden nie angezeigt, weil die View-seitige Berechtigungsprüfung auth()->user()->name (String-employee_id) gegen user_rolls.user_id (Integer-FK auf users.id) vergleicht — der Join ergibt immer 0 Treffer. Nicht-Admin-Benutzer können die Seite sehen, aber keinerlei Verwaltungsaktion ausführen. | `resources/views/admin/user/limit_user.blade.php:6-24 — $authUserKey = auth()->user()->name; DB::table('user_rolls')->where('user_rolls.user_id', '=', $authUserKey)` | S | ⚪ selten |
| 19 | **Artikel & Lager › Artikel-Daten › Rabattgruppen** | 💥 Crash | Nach jedem Speichern oder Aktualisieren einer Rabattgruppe wirft Laravel RouteNotFoundException: store() und update() rufen route('discount.group.info') auf, aber der einzige existierende Routenname ist discount_group.info (Unterstrich statt Punkt). Anlegen und Bearbeiten von Rabattgruppen schlägt immer fehl. | `app/Http/Controllers/Product/DiscountGroupController.php:59 (store) und :94 (update) — routes/web.php:2851 registriert nur 'discount_group.info'` | S | ⚪ selten |
| 20 | **Personal › HR-Daten › Urlaubsanspruch** | 💥 Crash | Alle 3 Formulare in leave_day.blade.php verwenden action() mit falschem Namespace 'App\Http\Controllers\LeaveDayController' (existiert nicht; korrekt: Employee\Profile\LeaveDayController). Jeder Submit-Versuch (Suche, Anlegen, Bearbeiten) wirft eine BadMethodCallException zur Laufzeit. | `resources/views/admin/employee/holiday/leave_day.blade.php:26,54,158` | S | ⚪ selten |
| 21 | **Personal › HR-Daten › Urlaubsanspruch** | 🗄️ Datenverlust | LeaveDayController::update() setzt $data->status = 'Not published' fest codiert (Zeile 71). Wenn ein aktives (Published) Urlaubsanspruch-Jahr bearbeitet wird, verliert es seinen Published-Status still und ohne Warnung. Aktivierung muss danach manuell wiederholt werden. | `app/Http/Controllers/Employee/Profile/LeaveDayController.php:71` | S | ⚪ selten |
| 22 | **System / Sonstiges › Datenbankbereinigung** | 💥 Crash | GarbageController::ensureDeletePermission() vergleicht user_rolls.user_id (unsignedBigInteger FK auf users.id) mit auth()->user()->name (String = employee_id). Kein Benutzer matcht → abort_unless(false, 403) → die gesamte Seite (GET index) wirft 403 für alle Nutzer. Das Modul ist komplett unzugänglich. | `app/Http/Controllers/Admin/GarbageController.php:29 (->where('user_id', $user->name)); bestätigt durch befunde-bestaetigt.csv id=12; user_rolls.user_id als unsignedBigInteger FK verifiziert in database/migrations/2023_06_14_131732_create_user_rolls_table.php:16,24` | S | ⚪ selten |
| 23 | **System / Sonstiges › Feedback** | 💥 Crash | FeedbackController::isProgrammer() verwendet dieselbe fehlerhafte Abfrage: user_rolls.user_id (Integer-FK) gegen auth()->user()->name (String). Immer false → update() (Antwort schreiben), changeStatus() und destroy() geben 403 für alle Nutzer zurück. Kein Admin kann Feedback beantworten, Status ändern oder löschen. index() zeigt jedem Nutzer nur seine eigene Feedback-Liste statt der Gesamtliste. | `app/Http/Controllers/System/FeedbackController.php:27-38 (isProgrammer()), :140 (update), :205 (changeStatus), :248 (destroy), :356 (destroy); gleicher Wurzelfehler wie befunde-bestaetigt.csv id=12` | S | ⚪ selten |

## 🟡 P3 — MITTEL (Performance / Instabilität unter Last)

| # | Navi-Punkt | Art | Was bricht | Datei:Zeile | Aufw. | Nutzung |
|--:|---|---|---|---|:--:|---|
| 1 | **Arbeitsbereich › Einzelseiten › Dashboard** | 🐢 Performance | DashboardCompanyController::departmentPerformance() (Zeilen 315-331) und departmentTable() (Zeilen 335-380) führen pro Abteilung je 2-4 separate DB-Abfragen aus (Offer::count, Deal::count, departmentProductIds, departmentCustomerIdsByProducts). Mit N Abteilungen entstehen pro Aufruf von GET /dashboard/company/overview ca. 4N+const Abfragen. Zusätzlich lädt NewLeads::query()->pluck('id') (Zeile 58) alle ~3000 Kunden-IDs ungefiltert in den PHP-Speicher. Company-Overview-Widget wird beim Dashboard-Start aktiv geladen. | `app/Http/Controllers/Dashboard/DashboardCompanyController.php:58-79,296-382` | M | 🟢 Kernbereich |
| 2 | **Arbeitsbereich › Einzelseiten › Lead-Kanban** | 🐢 Performance | LeadOverviewController@searchForm() (List-View-Tab im Kanban, AJAX-Endpunkt /lead/kanban/ajax) führt pro paginiertem Lead-Datensatz eine separate customer_histories-Abfrage aus (Zeilen 796-808 innerhalb ->each()). Bei 15 Zeilen pro Seite: 16 DB-Abfragen pro Seitenaufruf. Endpunkt wird aktiv aus kanban.js (APP.endpoints.listSearch) aufgerufen wenn der Listenreiter geöffnet wird. | `app/Http/Controllers/Customer/Kanban/LeadOverviewController.php:796-808 / public/js/kanban.js:86,352928` | S | 🟢 Kernbereich |
| 3 | **Berichte › Einzelseiten › Berichts-Übersicht** | 🐢 Performance | recentReportsEmployeeSummary() (AJAX-Endpunkt recent-reports.employee-summary, eingebunden in Berichts-Übersicht) iteriert über alle aktiven Mitarbeiter per map() und ruft pro Mitarbeiter 5 separate Count-Abfragen auf (remainingTicketCountForEmployee, remainingTaskCountForEmployee, remainingLeadCountForEmployee, remainingInquiryCountForEmployee, remainingAppointmentCountForEmployee). Bei 40 Mitarbeitern = 200 DB-Queries; jede Count-Query enthält weitere Subqueries für Ausschluss bereits gemeldeter Datensätze. | `app/Http/Controllers/Report/OverdueCenterController.php:114-140 (recentReportsEmployeeSummary map-Block), :4380-4590 (5 Count-Methoden)` | M | 🟢 Kernbereich |
| 4 | **CRM › Leads / Kunden › Leadliste** | 🐢 Performance | index() lädt 5 unbegrenzte ->get()-Abfragen auf lead_product_lists (join über alle new_leads, article_groups, departments, phase_sections) komplett in PHP-Speicher, bevor die paginierten 20 Leads gerendert werden. Bei 3000 Kunden mit je mehreren Produktzeilen entstehen Zehntausende Zeilen in PHP — jeder Leadlisten-Aufruf verursacht mehrere Sekunden Ladezeit und Speicherspitzen. | `app/Http/Controllers/Customer/NewLeadsController.php:3581,3619,3644,3655,3665 (NewLeadsController@index)` | M | 🟢 Kernbereich |
| 5 | **CRM › Leads / Kunden › Leadliste** | 🐢 Performance | hydrateLeadProductTeams() ruft für JEDE Zeile der unbegrenzten $customer_product_lists-Collection decorateTeamAssignments() auf, das seinerseits Employee::query()->whereIn()->get() abfeuert — ein N+1 auf der Gesamtmenge aller Produktzeilen. Index() und junks() sind beide betroffen (je ein call). Bei 3000+ Produktzeilen entstehen tausende Employee-Queries pro Seitenaufruf. | `app/Http/Controllers/Customer/NewLeadsController.php:3657-3658 (index), 11484 (junks); decorateTeamAssignments() definiert ab Zeile 13142` | M | 🟢 Kernbereich |
| 6 | **CRM › Leads / Kunden › Warteschleife** | 🐢 Performance | waiting_leads() klont die Basisabfrage und ruft ->get() auf dem Vollresultat aller wartenden Lead-Produktzeilen auf (analyticsRows, Zeile 3857), um Analytics-Kennzahlen in PHP zu berechnen (Stücke, Städte, Top-Produkte). Anschließend wird erst paginiert. Bei wachsender Warteschleife werden alle Zeilen doppelt aus der DB geladen (einmal für Analytics, einmal für Pagination). Außerdem lädt $customer_product_lists alle lead_product_lists-Zeilen ungepaginiert (Zeile 3987-4004) + hydrateLeadProductTeams N+1 (Zeile 11484 analog). | `app/Http/Controllers/Customer/NewLeadsController.php:3857 ($analyticsRows = (clone $baseQuery)->get()); 3987-4004 ($customer_product_lists->get()); 4004+11484 (hydrateLeadProductTeams auf vollem Datensatz)` | M | 🟢 Kernbereich |
| 7 | **Projekte › Einzelseiten › Projektplanung** | 🐢 Performance | Jede projectCockpitData-Anfrage (AJAX beim Laden der Projektliste) ruft countModuleByProject() 6-mal auf. Jeder Aufruf enthält 4–6 safeColumn()-Aufrufe; jedes safeColumn() feuert Schema::hasTable() + Schema::hasColumn() = 2 ungecachte DB-Queries gegen information_schema. Pro Seitenaufruf entstehen so ~50–70 Schema-Introspektions-Queries zusätzlich zu den eigentlichen Datenabfragen. Laravel's Schema::Builder::getTables() und getColumns() cachen nicht request-übergreifend (verifiziert in vendor/laravel/framework/src/Illuminate/Database/Schema/Builder.php:180–184). Unter Last mit mehreren gleichzeitigen Nutzern erzeugt dies spürbare Latenz und DB-Verbindungsdruck. | `app/Http/Controllers/Planner/PlannerPlanController.php:180-183 (safeColumn ohne Cache), :4494-4535 (countModuleByProject — 6+ safeColumn-Aufrufe), :4463-4486 (projectCockpitCounts ruft countModuleByProject 6×)` | S | 🟢 Kernbereich |
| 8 | **Projekte › Einzelseiten › Allgemeine Aufgaben** | 🐢 Performance | GeneralTaskController::index() lädt alle soft-deleted GeneralTasks ungefiltert und uneingeschränkt mit ->get() (ohne Limit, ohne Employee-Filter, ohne Pagination). Die Live-Tasks sind zwar auf den aktuellen Mitarbeiter/Sichtbarkeit gefiltert, der archivedTasks-Query hingegen holt alle jemals gelöschten Aufträge aus der Tabelle (GeneralTask::with(...)->onlyTrashed()->latest()->get()). Das Modul ist neu (Migration 2026-06-03), daher derzeit kein Problem — wächst aber mit der Zeit. Bei mehreren hundert archivierten Tasks mit eager-loaded Relations (8 Relationen inkl. verschachtelter) entsteht ein signifikantes Memory- und Query-Problem. | `app/Http/Controllers/Task/GeneralTaskController.php:88-92 (onlyTrashed()->latest()->get() ohne Filter), :1023-1037 (taskRelations: 8 Relations inkl. steps.assignees, dependsOn.assignees etc.)` | S | 🟢 Kernbereich |
| 9 | **Projekte › Termine › Terminübersicht** | 🐢 Performance | CustomerMainAppointmentController@index() (Seitenaufruf von customer/appointments) lädt $data['task_employee'] via MainAppointmentEmployee::join('employees')->get() — vollständige Tabelle, kein Filter — in PHP-Memory. Die Variable wird in der View admin.appointments.index weder als $task_employee noch als anderem Bezeichner genutzt (0 Treffer). Toter, skalierender Query als fixer Overhead bei jedem Seitenaufruf. | `app/Http/Controllers/Appointment/CustomerMainAppointmentController.php:90-105 (MainAppointmentEmployee::join->get() ohne WHERE); grep-Beleg: resources/views/admin/appointments/index.blade.php enthält kein 'task_employee' (0 Treffer)` | S | 🟢 Kernbereich |
| 10 | **Artikel & Lager › Lager › Inventar** | 🐢 Performance | InventoryController@index lädt alle Datensätze der new_leads-Tabelle (~3000 Einträge) ungepaginiert per NewLeads::query()->select(...)->get() in den Speicher, nur um ein Kunden-Dropdown zu befüllen. Bei Wachstum spürbare Page-Load-Verzögerung und erhöhter RAM-Verbrauch. | `app/Http/Controllers/Inventory/InventoryController.php:71-75` | S | 🟡 mittel |
| 11 | **CRM › Kommunikation › Posteingang** | 🐢 Performance | Email\LeadsController@index lädt Leads::orderBy('id','desc')->get() — alle Datensätze der leads-Tabelle ohne Pagination. In der View feuert jede Zeile zusätzlich DB::table('new_leads')->where('email',...)->first() (N+1 pro E-Mail) sowie DB::table('leads')->where('recipient_name',...)->count() (N+1 pro Absender-Bucket in der Sidebar). Bei wachsendem E-Mail-Bestand spürbare Verlangsamung. | `app/Http/Controllers/Email/LeadsController.php:121,126 / resources/views/admin/leads/email/email_view.blade.php:45,109` | M | 🟡 mittel |
| 12 | **CRM › Kommunikation › Bitrix24-Chat** | 🐢 Performance | MessageController@index lädt alle Zeilen aus 'messages' und alle Zeilen aus 'bitrix_chats' ohne Pagination oder Limit (DB::table('messages')->select(...)->get() und DB::table('bitrix_chats')->select(...)->get()). Die Batch-Import-Funktion des Controllers legt bis zu 1000×20 = 20.000 bitrix_chats-Einträge an. Bei Volllast blockiert die Seite den Request. | `app/Http/Controllers/MessageController.php:74-83 (index-Methode)` | S | 🟡 mittel |
| 13 | **CRM › Partner › Externe Firmen** | 🐢 Performance | Suchfilter in ExternalPersonalController@index verknüpft company_name AND admin_name (Zeilen 27-29): WHERE company_name LIKE '%x%' AND admin_name LIKE '%x%'. Suche nach Firmennamen allein liefert fast immer leere Ergebnisse, da admin_name den Suchterm ebenfalls enthalten muss. Kernfunktion der Suche (Firma finden) ist faktisch unnutzbar — Nutzer erhalten leer statt Treffer. | `app/Http/Controllers/Product/Brand/ExternalPersonalController.php:26-29` | S | 🟡 mittel |
| 14 | **Personal › Mitarbeiter › Lohn & Vollkosten** | 🐢 Performance | SalaryController@index ruft bei jedem GET-Request synchron seedMissingSheetsForPeriod() (Zeile 24) und recomputeZeroSheetsFromEmployeeRate() (Zeile 25) auf. Beim ersten Aufruf eines Monats werden für alle aktiven Mitarbeiter Salary-Sheets per chunkById() in einer Loop geschrieben (DB-Writes innerhalb eines HTTP-Requests). Kein Hintergrundprozess, kein Caching. Bei vielen Mitarbeitern oder langsamer DB verlängert das die Antwortzeit des Salary-Index spürbar; der Request kann in einen Timeout laufen. | `app/Http/Controllers/Employee/Profile/SalaryController.php:18-49 (index), 106-211 (seedMissingSheetsForPeriod via chunkById), 213-295 (recomputeZeroSheetsFromEmployeeRate via get+foreach)` | M | 🟡 mittel |
| 15 | **Projekte › Wartung › Wartungsverträge** | 🐢 Performance | CustomerMaintenanceContractController@index führt bei jedem Seitenaufruf zwei Datenbankabfragen aus: (1) paginate(20) für die angezeigte Seite, (2) (clone $baseQuery)->get(['id','status','next_service_date','end_date']) für Statistik-Kacheln (Z.85). Die zweite Abfrage lädt ALLE passenden Datensätze ungepaginiert in den PHP-Speicher. Bei wachsendem Vertragsbestand (3000 Kunden × Wartungsverträge) vervielfacht sich Speicher- und Datenbankbelastung auf jedem Index-Aufruf. | `app/Http/Controllers/Customer/Maintenance/CustomerMaintenanceContractController.php:85 — `$statsItems = (clone $baseQuery)->get(['id', 'status', 'next_service_date', 'end_date']);` ohne Limit oder separate COUNT-Abfragen.` | S | 🟡 mittel |
| 16 | **Projekte › Wartung › Wartungsverträge** | 🐢 Performance | kanbanFeed() lädt alle CustomerMaintenanceContract-Datensätze mit 4 eager-geladenen Relationen (lead, alternative, asset, responsibleEmployee) ohne jedes Limit (->get(), Z.1621). Bei vielen Verträgen wächst diese AJAX-Antwort unbegrenzt und kann zum Timeout oder Speicher-Erschöpfung des PHP-Prozesses führen. Gleiches gilt für calendarFeed() ohne Datum-Filter (Z.1583): wenn start/end-Parameter fehlen, werden alle Verträge geladen. | `app/Http/Controllers/Customer/Maintenance/CustomerMaintenanceContractController.php:1608-1621 (kanbanFeed ohne Limit), Z.1566-1583 (calendarFeed ohne Pflicht-Datum-Filter).` | S | 🟡 mittel |
| 17 | **Support › Tickets (Index 9) — 3 Navigationspunkte: Neues Ticket (problem_create), Ticket-Übersicht (problem_view), Fehlerkatalog (error) › Neues Ticket (problem_create)** | 🐢 Performance | ProblemController@create lädt bei JEDEM Seitenaufruf alle Kunden-Datensätze (new_leads JOIN lead_product_lists, DISTINCT, ->get() ohne LIMIT) in $data['customer']. Die Variable wird in der View nicht verwendet — der Kunden-Select2 lädt per AJAX (getAllCustomer, limit 30). Mit ~3000 Kunden erzeugt das einen vollständigen Tabellen-Scan plus Join bei jedem Aufruf der Neues-Ticket-Seite. | `app/Http/Controllers/Ticket/ProblemController.php:112-117` | S | 🟡 mittel |
| 18 | **Support › Tickets (Index 9) — 3 Navigationspunkte: Neues Ticket (problem_create), Ticket-Übersicht (problem_view), Fehlerkatalog (error) › Ticket-Übersicht (problem_view) — Kanban-Ansicht** | 🐢 Performance | ProblemController@kanban lädt alle gefilterten Tickets via ->get() ohne Pagination (Zeile 1408). Mit filter='all' (Tab 'Alle Tickets') werden sämtliche nicht-gelöschten Tickets zurückgegeben; hydrateTickets() iteriert danach über alle Zeilen, führt 3 weitere Batch-Queries aus und serialisiert das Gesamtergebnis als JSON. Bei wachsendem Ticket-Bestand führt das zu Speicherüberlauf und Response-Timeout. | `app/Http/Controllers/Ticket/ProblemController.php:1408` | M | 🟡 mittel |
| 19 | **Support › Tickets (Index 9) — 3 Navigationspunkte: Neues Ticket (problem_create), Ticket-Übersicht (problem_view), Fehlerkatalog (error) › Fehlerkatalog (error)** | 🔌 JS-Blocker | error_type.blade.php prüft Session::has('update_msg') (Zeile 1187) korrekt, liest aber session('updated_msg') (Zeile 1188) — falscher Key mit nachgestelltem 'd'. Der Toastr-Erfolgs-Toast nach einem Bearbeiten-Speichern bleibt immer leer/stumm. Nutzer erhalten keine Rückmeldung ob das Speichern erfolgreich war. (Identisches Muster wie befunde-bestaetigt.csv Befund 15, dort für anderen Bereich dokumentiert.) | `resources/views/admin/error_type/error_type.blade.php:1187-1188` | S | 🟡 mittel |
| 20 | **Admin › Benutzer › Admin › Benutzer › Eingeschränkte Benutzer** | 🗄️ Datenverlust | Die Statistik-Karten (Gesamt, Aktiv, Inaktiv, Neu 7 Tage) zeigen immer die Zahlen der Admin-Benutzer (is_admin=1), nicht der eingeschränkten Benutzer (is_admin=2). $statsBase ist in adminUsersFetch() fest auf ->where('is_admin', 1) codiert, unabhängig davon welcher admin-Parameter vom Client übergeben wird. Administratoren sehen irreführende Zahlen und können den Bestand eingeschränkter Benutzer nicht korrekt einschätzen. | `app/Http/Controllers/User/UserController.php:625 — $statsBase = DB::table('users')->where('is_admin', 1); (hardcoded, ignoriert $admin-Parameter aus dem Request)` | S | ⚪ selten |
| 21 | **Admin › Benutzer › Admin › Benutzer › Mein Profil** | 🗄️ Datenverlust | UserController@index übergibt $data (user_rolls-Join), aber NICHT $employee an die View. Die View setzt $profileEmployee = $employee ?? null — da $employee nie gesetzt wird, ist der Wert immer null. Display Name fällt auf users.name zurück, das die rohe employee_id-Zahl enthält (z.B. '42'). Profilbild des Mitarbeiters wird ebenfalls nicht geladen. Benutzer sehen im Profil eine ID statt ihres Namens. | `app/Http/Controllers/User/UserController.php:83-91 (index() — kein $employee im with()-Array); resources/views/admin/user/user_view.blade.php:19 ($profileEmployee = $employee ?? null)` | S | ⚪ selten |
| 22 | **Admin › Benutzer › Admin › Benutzer › Mein Profil** | 🗄️ Datenverlust | Die Berechtigungstabelle im Profil zeigt alle Rechte als 'Nein' (×), weil $hasPermission prüft ob der Wert === 'on' ist. UserRollController speichert jedoch boolesche Werte (true/false, also 1/0 in der DB). Der String 'on' kommt aus dem alten Legacy-System und stimmt nicht mehr mit dem aktuellen Speicherformat überein. Benutzer können ihre eigenen Berechtigungen nicht erkennen. | `resources/views/admin/user/user_view.blade.php:45-47 ($hasPermission = function ($value) { return $value === 'on'; }); Kontrast: app/Http/Controllers/User/UserRollController.php:160 ($request->boolean('is_read'))` | S | ⚪ selten |
| 23 | **Finanzen › Finanzen › Ratenzahlungen** | 🔌 JS-Blocker | Html5QrcodeScanner('reader', ...) wird beim Laden der Ratenzahlungen-Listenseite sofort (außerhalb DOMContentLoaded, ohne Guard) aufgerufen. Das #reader-div existiert weder in installment.blade.php noch in den inkludierten Sub-Views installment_car.blade.php und installment_asset.blade.php. html5-qrcode wirft beim Versuch, das fehlende Element zu mounten, einen JS-Fehler. Die dreifache Initialisierung (parent + beide includes haben eigene Script-Blöcke) erzeugt bei jedem Seitenaufruf mehrere Konsolenfehler. QR-Scan-Funktion ist vollständig blockiert; die Tabelle selbst lädt noch. | `resources/views/admin/product/assets/installment/installment.blade.php:115-119, installment_car.blade.php:244-248, installment_asset.blade.php:233-237 — kein id="reader" in diesen Views (nur in assets_create.blade.php:82 vorhanden)` | S | ⚪ selten |
| 24 | **Finanzen › Finanzen › Ratenzahlungen** | 🗄️ Datenverlust | show()-Suche: orWhere('assets.id', 'LIKE', "%$search%") ist nicht in eine Closure gekapselt. SQL-Ergebnis: WHERE type='asset' AND assets.item LIKE ? OR assets.id LIKE ? — der type-Filter wird aufgehoben. Bei numerischer Suche (z.B. ID '5') gibt die Asset-Abfrage auch machine-Zeilen zurück, die über den assets-JOIN keine Daten haben; deren item/model-Spalten sind NULL. Gleichzeitig kann eine Maschinen-Ratenzahlung in der Asset-Tabelle erscheinen und eine falsche Zuweisung beim Bearbeiten auslösen (type-Feld wird nicht neu geprüft). Bereits in befunde-bestaetigt.csv #73 getrackt, im aktuellen Code unverändert vorhanden. | `app/Http/Controllers/Inventory/AssetInstallmentController.php:109-112` | S | ⚪ selten |
| 25 | **Finanzen › Finanzen › Ratenzahlungen** | 💥 Crash | store() und update() geben im else-Zweig (Fehlerfall) identischen Text 'Der Datensatz wurd erfulgreich gespeichert!' als delete_msg (roter Flash) aus. Beim Scheitern eines Speichervorgangs sieht der Nutzer eine rote Box mit Erfolgsmeldung — irreführend, aber kein echter Absturz. Als zusätzliches Problem ignoriert update() das Route-Model-Binding ($assetInstallment) und lädt stattdessen per AssetInstallment::find($request->id) nach, was bei fehlendem id-Feld im POST einen Fatal Error auslöst. Bereits in befunde-bestaetigt.csv #72 getrackt, im aktuellen Code unverändert vorhanden. | `app/Http/Controllers/Inventory/AssetInstallmentController.php:92-94 (store), 167-173 (update)` | S | ⚪ selten |
| 26 | **Personal › Einzelseiten › Stellenbesetzung** | 🐢 Performance | Schema::hasColumn('department_positions','working_hours') wird in assignmentPayload() pro Assignment-Zeile aufgerufen (Zeile 435). Bei jedem GET /employee-organization/data feuert diese Methode N-mal, wobei N = Anzahl aller aktiven Zuordnungen (department_positions). Jeder Schema::hasColumn-Aufruf löst eine INFORMATION_SCHEMA-Abfrage aus (bekanntes Muster, identisch mit Befund #79 im Planner). Ebenfalls: Schema::hasColumn in applyAssignmentValues() (Zeile 324) wird bei bulkAssign pro Schleifendurchlauf aufgerufen. Bei 40 Mitarbeitern mit je mehreren Positionen entstehen 100–200 redundante Schema-Abfragen pro Seitenaufruf. | `app/Http/Controllers/Employee/Position/EmployeeOrganizationController.php:435 (assignmentPayload), :324 (applyAssignmentValues)` | S | ⚪ selten |
| 27 | **Personal › HR-Daten › Gesetzliche Feiertage** | 💥 Crash | Die Route-Gruppe fuer public-holidays verwendet nur ['middleware' => 'web'] ohne 'auth'. PublicHolidayController hat keinen __construct mit Middleware-Aufruf. Alle Endpunkte (index, fetch, store, delete, show) sind ohne Authentifizierung abrufbar und schreibbar. Zusaetzlich sind zwei registrierte Routen (POST /import → importCSV, GET /sample → downloadSample) ohne entsprechende Controller-Methoden registriert — ein direkter Aufruf wuerfe einen PHP-Fatal-Error (MethodNotAllowedException / BadMethodCallException). | `routes/web.php:1748-1761 (middleware 'web' only); app/Http/Controllers/Employee/Profile/PublicHolidayController.php (Methoden importCSV und downloadSample fehlen)` | S | ⚪ selten |
| 28 | **Vertrieb › Aufträge › Übersicht / Junk / Gelöscht (alle 3 Deal-Listen)** | 🐢 Performance | DealController::sidebarData() lädt bei jedem Seitenaufruf alle ~3000 NewLeads-Kunden per NewLeads::query()->get() ohne Limit und rendert sie als <option>-Elemente in einem <select>-Dropdown. Gleichzeitig lädt statsFromQuery() alle passenden Deals (mit 6-Table-JOIN) per ->get() in PHP-Speicher, um Statuszähler client-seitig zu berechnen. Beide Methoden werden bei jedem GET auf deal_all_list, deal_junk_list und deal_delete_list ausgeführt. | `app/Http/Controllers/Customer/Deal/DealController.php:483 (sidebarData, NewLeads::query()->get()), :753 (statsFromQuery, clone $baseQuery->get()), :810/:831/:853/:872 (Aufruf in allen 4 Listen-Methoden); resources/views/admin/deal/customer_view.blade.php:2674 (@foreach $customers als <option>)` | M | ⚪ selten |
| 29 | **Vertrieb › Aufträge › Feinaufmaß-Kanban** | 🐢 Performance | DealMeasurementController::kanban() kombiniert drei Probleme auf einem einzigen GET-Request: (1) ->get() ohne Pagination lädt alle DealMeasurement-Zeilen mit 12 eager-geladenen Relationen (deal, customer, alternative, pvWpDetail, roofs, product, items, offer, detail, editDetail, histories, itemsCount). (2) Pro Zeile werden in einer foreach-Schleife findMeasurementAppointment() (bis zu 3 DB-Abfragen), findMeasurementPersonalTask() (bis zu 2 Abfragen) und findEmployeeById() (1 Abfrage) aufgerufen — N×6 Abfragen. (3) createMissingMeasurementsFromOfferDetails() führt synchron bis zu 100 DB-Schreibvorgänge (DealMeasurement::create + N×DealMeasurementItem::create) auf jedem GET aus. | `app/Http/Controllers/Customer/Deal/DealMeasurementController.php:1226 (createMissingMeasurementsFromOfferDetails Aufruf), :1234-1307 (->get() ohne Pagination mit 12 with-Relationen), :1309-1313 (foreach-Schleife mit 3 DB-Methoden je Zeile), :1007-1040 (findActiveMeasurementAppointment: bis zu 3 Queries), :1068-1095 (findActiveMeasurementPersonalTask: bis zu 2 Queries), :1119-1128 (findEmployeeById: 1 Query), :142-321 (createMissingMeasurementsFromOfferDetails: Writes in Transaktion)` | L | ⚪ selten |

## ⚪ P4 — NIEDRIG (kosmetisch / selten genutzt)

| # | Navi-Punkt | Art | Was bricht | Datei:Zeile | Aufw. | Nutzung |
|--:|---|---|---|---|:--:|---|
| 1 | **Artikel & Lager › Lager › Übergaben** | 🔌 JS-Blocker | Success-Toast nach Update zeigt immer leere Meldung: View prüft Session::has('update_msg') (korrekt), liest aber session('updated_msg') (falscher Key mit angehängtem 'd'). Nutzer sieht leeren Toastr-Dialog. | `resources/views/admin/product/handover/handover_details.blade.php:230-231` | S | 🟡 mittel |
| 2 | **Admin › System (Arbeitsschritte / Projekt-Struktur / Filialen) › Arbeitsschritte** | 💥 Crash | TaskPhaseController@clone (POST /task_phase_clone) ruft TaskSubTasks::create() (Plural) auf — importiert ist nur TaskSubTask (Singular, Zeile 9). Klasse TaskSubTasks existiert nicht im Projekt. Führt zu PHP Fatal Error "Class not found" bei jedem Aufruf dieser Methode. Route ist in keiner aktuellen View referenziert (nur Legacy-Registrierung), aber als POST /task_phase_clone direkt erreichbar. | `app/Http/Controllers/Phase/TaskPhaseController.php:453` | S | ⚪ selten |
| 3 | **Admin › System (Arbeitsschritte / Projekt-Struktur / Filialen) › Arbeitsschritte** | 💥 Crash | TaskPhaseController@destroy (GET /task_phase_destroy/{id}) ruft $data->delete() ohne Null-Prüfung: TaskPhase::find($id) gibt null zurück wenn ID nicht existiert, danach null->delete() → Fatal Error. Route in keiner aktuellen View referenziert (Legacy-GET-Route), aber direkt erreichbar. | `app/Http/Controllers/Phase/TaskPhaseController.php:517-518` | S | ⚪ selten |

---
**Aufwand-Legende:** S = klein · M = mittel · L = groß. **Empfehlung:** P1 in genutzten Kernbereichen zuerst (kleiner Aufwand, große Wirkung), dann P2, dann P3-Performance vor der nächsten Wachstumsstufe.